# Curator: Build Tracker

**Owner:** Jake Leese
**Maintained by:** Claude (updated each implementation session)
**Cross-references:** `DESIGN.md` (spec), `DESIGN_PHASE_DELTA.md` (post-1.0 roadmap), `Github/CURATOR_RESEARCH_NOTES.md` (decisions), `Github/PROCUREMENT_INDEX.md` (deps), `docs/ROADMAP.md` (Phase Gamma+ planned features), `CHANGELOG.md` (per-version notes)

---

## [v0.1.0 RELEASED] curatorplug-atrium-citation ✅

**Drafted:** 2026-05-08 during Continue iteration 2.
**Scaffolded:** 2026-05-08 (commit `e301a4f` on GitHub).
**P1 SHIPPED:** 2026-05-08 (commit `f472006`, tag `v0.1.0` on GitHub).
**Severity:** Functional Beta release — sweep + CLI + audit emission all implemented; 34/34 unit tests passing. v0.2 work captured under Known limitations below.

### What it is

A new constitutional plugin matching the structural pattern of `curatorplug-atrium-safety` v0.3.0, but enforcing Atrium **Principle 3 (Citation Chain Preservation)** instead of Principle 2 (Hash-Verify-Before-Move). Verifies that cross-source writes produce corresponding `lineage_edges` rows; surfaces gaps via a deferred `curator citation sweep` CLI subcommand.

DESIGN doc lives at:
- Workspace authoritative: `C:\Users\jmlee\Desktop\AL\curatorplug-atrium-citation-DESIGN.md` (~20 KB)
- Git-backed copy: `Curator/docs/CURATORPLUG_ATRIUM_CITATION_DESIGN.md` (identical)

### Six DMs ratified 2026-05-08

Full text in the DESIGN doc §3. All ratified by Jake's chat reply "1c":

| DM | Question | Recommended |
|---|---|---|
| **DM-1** | Enforcement mode | **Advisory** (log + audit, allow write to proceed). Refusing on missing lineage would turn debuggability problems into user-facing failures, violating Principle 4 transitively. |
| **DM-2** | Scope of citation requirement | **Cross-source writes only.** Same-source writes (initial scans) don't have a parent file in the index; flagging them would generate noise. |
| **DM-3** | Verification mechanism | **Deferred sweep** (`curator citation sweep` CLI). Real-time verification would require invasive Curator core changes; sweep ships immediately and matches the Atrium `<product> status` introspection convention. |
| **DM-4** | Audit channel | **Curator's existing audit log** via `actor='curatorplug.atrium_citation'`. Same pattern as atrium-safety DM-3 (ratified). |
| **DM-5** | Plugin registration | **Setuptools entry point** under `[project.entry-points.curator]`. Same as atrium-safety DM-4 (ratified). |
| **DM-6** | Cross-product scope | **Curator-only for v0.1.** Cross-product enforcement waits for SIP v1.0 per Atrium Constitution Article IV. |

### Bootstrap (v0.1.0a0, commit `e301a4f`)

Scaffolded 2026-05-08. `pyproject.toml` with entry-point declaration, namespace-package layout matching atrium-safety, `plugin.py` with hookimpl scaffolding (methods log "P1 stub" markers), `exceptions.py` empty per DM-1 advisory, 6 smoke tests, DESIGN.md/README.md/CHANGELOG/.gitignore. Curator auto-discovery verified.

### P1 (v0.1.0, commit `f472006`, tag `v0.1.0`)

Shipped 2026-05-08, ~4h after scaffolding. Three new modules + 28 new tests:

- **`sweep.py`** — `CitationSweep` class with `run() -> SweepReport`. Walks `audit_repo` for `migration.move` + `migration.copy` events, queries `lineage_repo.get_edges_to(curator_id)` for each, records `CitationGap` entries for files with no inbound edges. Stateless and side-effect-free; the CLI handles audit emission separately. Records errors per-event rather than aborting.
- **`audit.py`** — `emit_sweep_started` / `emit_gap` / `emit_sweep_completed` helpers + `emit_full_sweep` convenience. All emissions actor=`curatorplug.atrium_citation` per DM-4 RATIFIED. All emissions best-effort: exceptions logged but never raised per DM-1 advisory mode.
- **`cli.py`** — Typer-based CLI exposed as `curator-citation` console script. Three subcommands: `sweep` (flags `--db`, `--limit`, `--quiet`, `--no-audit`), `status`, `version`. Exit codes: 0 (clean), 1 (gaps found), 2 (errors). Per DM-3 RATIFIED, ships as separate console script rather than `curator citation` subcommand because Curator's CLI doesn't yet have a plugin-extension hook.

Configuration: `pyproject.toml` version `0.1.0a0` → `0.1.0`, Dev Status `Alpha` → `Beta`, added `[project.scripts]` for `curator-citation`, added `typer>=0.9` (transitively present via Curator). `__init__.py` version bumped accordingly.

Tests: 13 sweep tests + 8 audit-emission tests + 7 CLI tests + 6 existing smoke tests = **34/34 passing in 0.36s**. CLI verified manually: `curator-citation version` returns `0.1.0`; `--help` displays subcommand listing; `sweep --help` shows full option set.

### Conservative scope decision (documented for v0.2 refinement)

DM-2 RATIFIED says "cross-source writes only" should require lineage. However, Curator's `migration.move`/`migration.copy` audit details only contain `src_path`, `dst_path`, `size`, `xxhash3_128` — NOT a `cross_source` flag. Determining cross-source-ness retroactively from audit logs would require correlating each path against registered source root paths, which is brittle. **For v0.1, the sweep flags every migration event lacking a lineage edge** (slightly broader than DM-2's stated scope). May produce false positives for legitimate same-source moves. Documented in `sweep.py` docstring + CHANGELOG known-limitations. v0.2 can refine once Curator's audit log carries the flag explicitly.

### What's pending (v0.2+ candidates)

- Cross-source detection refinement (filter to true cross-source events once Curator's audit log carries the flag).
- Real-time enforcement option (toggle, default off). v0.1's `curator_source_write_post` hook is scaffolding only; v0.2 may add observation recording for live citation-state tracking.
- Cross-product enforcement. Per DM-6 RATIFIED, this waits for SIP v1.0.
- Curator subcommand integration. When Curator's CLI gains a plugin-extension API, the citation CLI can register as `curator citation sweep` rather than the standalone `curator-citation sweep`.
- P2 cycle: integration tests against a real Curator runtime (vs. the P1 unit tests' StubAuditRepo / StubLineageRepo).

### Relationship to other plugins

- **`curatorplug-atrium-safety`** (v0.3.0 stable) implements Principle 2 (Hash-Verify-Before-Move). Refuses non-compliant writes.
- **`curatorplug-atrium-reversibility`** (DESIGN-only at `84ee978`, on GitHub at `https://github.com/KULawHawk/curatorplug-atrium-reversibility`) implements Aim 2 (Reversibility, an Article I aim, not an Article II principle). P1 implementation paused.
- **`curatorplug-atrium-citation`** (this entry, **v0.1.0 RELEASED** at `f472006`, tag `v0.1.0` on GitHub at `https://github.com/KULawHawk/curatorplug-atrium-citation`) implements Principle 3 (Citation Chain Preservation). Observes citation gaps via deferred sweep; advisory mode only.

The three plugins are independent: install any subset; each remains uninstallable per Aim 3 (Self-sufficiency).

### Why deferred from immediate implementation

~~The design needs Jake's DM ratification before code is written.~~ ✅ Resolved 2026-05-08. DMs ratified.
~~Scaffolding shipped. P1 implementation is the next deliverable.~~ ✅ Resolved 2026-05-08. P1 shipped same day at tag `v0.1.0`.

v0.1.0 (Beta) is the current release. v0.2.0 (cross-source filter refinement + integration tests) is the next milestone but not on the immediate roadmap; it depends on Curator carrying the cross-source flag in audit details (Curator-side change, separate work).

---

## [v1.5.0 RELEASED] MCP HTTP-auth (Bearer-token authentication for `curator-mcp --http`) ✅

**Drafted:** 2026-05-08 (`docs/CURATOR_MCP_HTTP_AUTH_DESIGN.md` v0.1).
**Ratified:** 2026-05-08 (Jake replied "1" affirming all 6 DMs; design v0.2 RATIFIED).
**P1 SHIPPED:** 2026-05-08 (`721af2b`) — auth.py module + 42 unit tests.
**P2 SHIPPED:** 2026-05-08 (`e793f8b`) — `curator mcp keys` CLI (4 subcommands) + 24 tests.
**P3 SHIPPED:** 2026-05-08 — BearerAuthMiddleware + server.py wiring + 23 integration tests; v1.5.0 tagged.
**Severity:** Minor release — HTTP transport auth is a real API change for non-stdio users; stdio integrations untouched.
**Effort actual:** ~3.5h across the three phases (matched the design estimate).

### What it is

Bearer-token authentication for `curator-mcp --http`. Adds:

* `~/.curator/mcp/api-keys.json` storage (sha256 hashes; plaintext shown once at generate time).
* `curator mcp keys generate / list / revoke / show` CLI for management.
* `BearerAuthMiddleware` (Starlette) that validates `Authorization: Bearer <key>` against the keys file.
* Audit emission for both successful and failed auth attempts under `actor='curator-mcp'`.
* `--no-auth` opt-out (loopback-only) for local development.
* Removes the v1.2.0 hard-refusal of non-loopback `--host`; non-loopback now allowed when auth is configured.

stdio transport (the default for Claude Desktop / Claude Code) is unchanged.

### Six DMs ratified 2026-05-08

All six recommended options ratified by Jake's affirmative reply ("1"). Full text in `docs/CURATOR_MCP_HTTP_AUTH_DESIGN.md` §3.

| DM | Decision |
|---|---|
| DM-1 | `Authorization: Bearer <key>` (RFC 6750 standard) |
| DM-2 | JSON file at `~/.curator/mcp/api-keys.json`, 0600 (Unix) / ACL inheritance (Windows) |
| DM-3 | `curm_<40-char-random>` format-prefixed keys |
| DM-4 | Multiple named keys with metadata, individually revocable |
| DM-5 | Both successful + failed audit emission; successful throttled to 1/key/minute |
| DM-6 | Auth required by default; `--no-auth` opts out (loopback-only) |

### Three-phase implementation (per DESIGN §6)

**P1 (auth module, ~90 min, commit `721af2b`):**
* `src/curator/mcp/auth.py` (~340 LOC): constants, `default_mcp_dir()` / `default_keys_file()` honoring `CURATOR_HOME`, `generate_key()` via `secrets.token_urlsafe(30)`, `hash_key()` via SHA-256, `StoredKey` dataclass, `KeyFileError` + `DuplicateNameError`, `_now_iso()`, `_set_secure_permissions()` (Unix 0600), atomic `save_keys()` via tempfile-then-rename, `load_keys()` with corrupt/schema-mismatch handling, `add_key`/`remove_key`/`validate_key`/`update_last_used`.
* `tests/unit/test_mcp_auth.py` (42 tests, 1 skip).
* All 7 DESIGN §5.1 acceptance criteria met.

**P2 (CLI, ~75 min, commit `e793f8b`):**
* `src/curator/cli/mcp_keys.py` (~310 LOC): `mcp_app` typer group with `keys` subgroup; four subcommands (`generate`/`list`/`revoke`/`show`) honoring `--json` and using Rich tables for non-JSON output.
* `src/curator/cli/main.py`: imports + registers `mcp_app`.
* `tests/unit/test_mcp_keys_cli.py` (24 tests).
* Smoke-tested: `curator mcp --help` shows the `keys` subgroup; `curator mcp keys --help` shows all 4 subcommands.

**P3 (middleware + server wiring, ~75 min):**
* `src/curator/mcp/middleware.py` (~280 LOC): `BearerAuthMiddleware` (Starlette `BaseHTTPMiddleware`) extracts header, validates token, returns 401 + `WWW-Authenticate: Bearer` on failure, forwards on success. In-memory throttling for successful auth events. `make_audit_emitter(audit_repo)` factory bridges middleware events to Curator's `AuditRepository.log()`.
* `src/curator/mcp/server.py` rewrite: split `main()` and `_run_http()`; new `--no-auth` flag with loopback-only enforcement; `--host` non-loopback now legal when auth configured; `_has_configured_keys()` gate; wraps `streamable_http_app()` with middleware before handing to `uvicorn.run`.
* `tests/unit/test_mcp_http_auth.py` (23 integration tests).
* CHANGELOG `[1.5.0]` entry under Added/DM-trace/Test-coverage/Compatibility/Why-minor sections.
* Version bump 1.4.1 → 1.5.0 in `pyproject.toml` and `__init__.py`.
* Git tag `v1.5.0` pushed.

### Acceptance criteria (all met)

1. ✅ HTTP request with valid Bearer key returns the tool result. (`TestSuccessfulAuth.test_valid_key_returns_200`)
2. ✅ HTTP request with invalid Bearer key returns 401. (`TestHeaderRejections.test_invalid_key_returns_401`)
3. ✅ HTTP request without Authorization header returns 401. (`TestHeaderRejections.test_missing_authorization_header_returns_401`)
4. ✅ HTTP request with malformed Authorization header returns 401. (`TestHeaderRejections.test_wrong_scheme_returns_401`, `test_empty_bearer_token_returns_401`)
5. ✅ Auth events land in audit log under `actor='curator-mcp'`. (`TestMakeAuditEmitter.test_emitter_calls_audit_repo_log`)
6. ✅ Successful-auth audit emission throttling works. (`TestAuditEmission.test_success_throttled`)
7. ✅ Failed-auth audit emission is NOT throttled. (`TestAuditEmission.test_failure_never_throttled`)
8. ✅ `--no-auth --host 0.0.0.0` exits 2 with refusal. (`TestRunHttpArgValidation.test_no_auth_with_non_loopback_exits_2`)
9. ✅ `--no-auth --host 127.0.0.1` works without auth (loopback dev mode preserved). (`TestRunHttpArgValidation.test_no_auth_with_loopback_runs`)

### What's pending (v1.6.0+ candidates)

* TLS termination guidance (currently users put nginx/Caddy in front for HTTPS). v1.5.0 ships HTTP-with-auth; HTTPS is an infrastructure concern.
* Per-key scopes (read-only key vs. future write-tool key). v1.5.0 is single-permission.
* Key rotation automation. v1.5.0 ships generate/list/revoke; rotation is manual.
* Windows ACL hardening via `icacls` (currently relies on home-dir ACL inheritance). Documented as a known v1.5.0 limitation.

---

## [v1.4.1 RELEASED] API hardening: sentinel-default for `apply()` + `run_job()` policy kwargs ✅

**Discovered:** 2026-05-08 during Tracer Phase 4 P3 e2e test pass.
**Shipped:** 2026-05-08 as v1.4.1 patch (single-session implementation).
**Severity:** DX (developer experience) only — no functional bugs, but a real footgun.
**Effort actual:** ~50 min (matched estimate).

### Problem (resolved)

``MigrationService.apply()`` and ``run_job()`` both took ``max_retries`` and ``on_conflict`` as kwargs with documented defaults (``3`` and ``'skip'`` respectively). Both called ``self.set_max_retries(...)`` and ``self.set_on_conflict_mode(...)`` at entry, which meant any prior call to the corresponding ``set_*`` method was silently overwritten by the apply/run_job kwarg default if the caller didn't explicitly pass the kwarg.

Result: a caller who did ``svc.set_on_conflict_mode('overwrite-with-backup'); svc.apply(plan)`` got ``skip`` semantics, not the mode they set. Surprising and quietly wrong.

This was caught by the Phase 4 P3 e2e tests in ``tests/unit/test_migration_cross_source.py::TestPhase4CrossSourceConflictResolution`` (which originally used the ``set_*`` pattern, then were rewritten to pass ``on_conflict`` directly to ``apply()``). v1.4.0 docstrings warned about it; v1.4.1 closes the underlying issue.

### Fix shipped (sentinel default)

```python
_UNCHANGED: Any = object()  # module-level sentinel

def apply(
    self, plan, *, ...,
    max_retries: Any = _UNCHANGED,
    on_conflict: Any = _UNCHANGED,
) -> MigrationReport:
    if max_retries is not _UNCHANGED:
        self.set_max_retries(max_retries)
    if on_conflict is not _UNCHANGED:
        self.set_on_conflict_mode(on_conflict)
```

``run_job()`` follows the same pattern with three-tier resolution: explicit kwarg > persisted ``job.options`` > current ``self._max_retries``/``self._on_conflict_mode``. The previous magic-default check (``max_retries == 3``) is replaced with explicit sentinel comparison.

### Backward-compat verified

- **CLI:** unaffected. The CLI passes both kwargs explicitly (driven by argparse defaults), so apply/run_job behavior is identical.
- **Existing tests that pass kwargs:** unaffected. 150/150 migration regression tests pass (was 135 + 15 new).
- **Existing tests that use the ``set_*`` pattern WITHOUT passing kwargs:** behavior changes — they now correctly use the set value. None of the existing test suite was relying on the buggy reset, so 0 test changes were needed in pre-existing files.
- **Library/agent callers:** behavior changes (in their favor). Sticky setters now stick.

### Acceptance criteria (all met)

1. ✅ New unit tests prove ``set_on_conflict_mode('X'); apply(plan)`` actually uses mode X (not the default). See `tests/unit/test_migration_v141_sentinel_defaults.py::test_set_on_conflict_mode_persists_through_apply_when_kwarg_omitted`.
2. ✅ Same proven for ``set_max_retries`` + ``apply``, both setters + ``run_job``. 15 total tests in the new file.
3. ✅ Resume semantics for ``run_job`` preserved: a job's persisted ``options['max_retries']`` and ``options['on_conflict']`` are used when the caller doesn't pass kwargs. Tests `test_run_job_persisted_options_used_when_kwarg_omitted` and `test_run_job_explicit_kwarg_beats_persisted_options`.
4. ✅ CHANGELOG entry under ``## [1.4.1]`` heads ``### Fixed`` and notes the fix as a behavior change for library callers (CLI users see no change).
5. ✅ Docstring warnings in ``set_max_retries`` and ``set_on_conflict_mode`` removed and replaced with positive guidance describing two equivalent patterns: explicit kwarg per call, or sticky setter once.
6. ✅ Full Curator regression slice green: 150/150 migration tests pass; preexisting `test_photo.py` PIL-import failures are unrelated environment issues.

### Why patch (1.4.0 → 1.4.1) and not minor

The change is internally a *bug fix* with no API surface modifications: method signatures still accept the same kwargs by name; explicit values still produce identical behavior; only the omitted-kwarg semantics change (and only when the user previously called ``set_*`` — a code path the v1.4.0 docstrings warned was buggy). Patch versioning is appropriate.

Originally documented as a v1.5.0 candidate (alongside MCP HTTP-auth) for release-ceremony bundling, but the fix shipped on its own as v1.4.1 because it's small, self-contained, and isolating it makes the changelog cleaner.

---

## [v1.6.0 candidate] (no current items)

Future candidate items (TLS guidance for MCP HTTP, per-key scopes, key rotation automation, Windows ACL hardening) will populate this section as they arrive.

---

## v1.1.0a1 — Migration tool Phase 1 (alpha) — 2026-05-08 🚚

**Phase 2 cleared (added 2026-05-08):** Jake ratified all 5 DM recommendations (DM-1 through DM-5) in `docs/TRACER_PHASE_2_DESIGN.md` §3 as written. Phase 2 Session A (schema + `MigrationJobRepository` + service core + CLI core) is unblocked. The design doc is now v0.2 with status "RATIFIED." Estimated path to v1.1.0 stable: 3 sessions (~8.75h total) per the ratified spec.

**Brand note (added 2026-05-08):** Jake assigned the user-facing name **Tracer** to Curator's Migration capability, and **Ad Astra** as the suite name covering Curator + APEX + Synergy + future products. Per `Atrium\NAMES.md` §1, names are brands for existing components — code paths (`curator.services.migration`) and the CLI command (`curator migrate`) remain unchanged; "Tracer" is user-facing only. See `Atrium\NAMES.md` and `Atrium\INSTALL_PATH_DESIGN.md`.

**Headline:** Feature M (Migration tool) Phase 1 ships. First post-1.0
feature on the v1.1 minor cycle. Same-source local→local file relocation
with hash-verify-before-move, ``curator_id`` constancy, audit integration.
**1002 default-suite passing + 9 opt-in (1 GUI smoke + 6 watch + 2 safety) =
1011 total. 8 correctly-skipped, 0 failures, 61s.** Was 969 pre-Migration
→ +33 new (25 unit + 8 CLI integration).

### What shipped

- ``src/curator/services/migration.py`` (~430 LOC):
  ``MigrationService`` + ``MigrationPlan`` / ``MigrationReport`` /
  ``MigrationMove`` / ``MigrationOutcome``. Plan walks SAFE files via
  ``FileQuery(source_ids=[...], source_path_starts_with=..., deleted=False,
  order_by='source_path ASC')``, partitions by ``SafetyService.check_path``
  verdict, applies optional case-insensitive extension filter, refuses if
  ``dst_root`` is inside ``src_root``. Apply does per-file Atrium Constitution
  Hash-Verify-Before-Move: hash src (cached if available) → mkdir parents →
  ``shutil.copy2`` → hash dst → verify match → update ``FileEntity.source_path``
  via ``file_repo.update`` → trash src via vendored send2trash → audit with
  ``actor='curator.migrate'`` / ``action='migration.move'``. Skips CAUTION/
  REFUSE files, pre-existing collisions, and the file at ``db_path_guard``.
- ``src/curator/services/__init__.py``: exports ``MigrationService`` +
  ``MigrationMove`` / ``MigrationOutcome`` / ``MigrationPlan`` /
  ``MigrationReport``.
- ``src/curator/cli/runtime.py``: adds ``migration: MigrationService``
  field; constructed in ``build_runtime`` after safety + audit are ready.
- ``src/curator/cli/main.py``: ``@app.command(name="migrate")`` (~210 LOC
  including ``_render_migration_plan`` + ``_render_migration_report``
  helpers) supporting ``curator migrate <src_source_id> <src_root>
  <dst_root> [--ext .mp3,.flac] [--apply] [--verify-hash/--no-verify-hash]``.
  Plan-only by default; ``--apply`` runs moves. Honors ``rt.json_output``.
  DB guard uses ``rt.db.db_path`` automatically when applying.
- ``tests/unit/test_migration.py`` (25 tests across ``TestComputeDstPath``
  / ``TestXxhashOfFile`` / ``TestMigrationPlanDataclass`` / ``TestPlan`` /
  ``TestApply`` / ``TestLineagePreservation`` / ``TestErrorHandling``).
- ``tests/integration/test_cli_migrate.py`` (8 CLI tests: command listed
  in help, plan-only doesn't mutate, plan JSON shape, apply moves files
  end-to-end, no-SAFE returns moved=0, dst-inside-src exits 2, extension
  filter narrows, ``--apply`` gate).
- ``docs/v100a1_migration_demo.txt`` (4,098 bytes): real-world end-to-end
  demo transcript. 5 files / 14,265 bytes / 0.31s, all 5 verified, sources
  trashed, FileEntity rows re-pointed, 5 audit entries.

### Headline invariants proven

- **curator_id constancy.** Lineage edges and bundle memberships persist
  across moves (``test_lineage_edges_survive_move``,
  ``test_bundle_membership_survives_move``).
- **Hash mismatch leaves source intact.** When the dst hash doesn't match
  src after copy, dst is unlinked and src is preserved
  (``test_hash_mismatch_leaves_source_intact``).
- **DB-guard.** The runtime's own ``curator.db`` file is never migrated
  (``test_db_guard_skipped``).
- **Audit per-move.** ``actor='curator.migrate'`` /
  ``action='migration.move'`` with src_path + dst_path + size + xxhash3_128
  in details (``test_audit_entries_written_on_success``).
- **Copy failure preserves source.** OSError mid-copy leaves src on disk
  and FileEntity row pointing at src
  (``test_copy_failure_marks_failed_preserves_src``).

### Mid-build catches

- ``LineageEdgeKind`` is actually ``LineageKind``; ``LineageEdge.kind``
  is actually ``LineageEdge.edge_kind``. Caught at first test-collection.
- ``FileRepository.list_active(limit=N)`` doesn't exist — use
  ``file_repo.query(FileQuery(deleted=False, limit=N))``.
- ``FileEntity`` requires ``mtime`` field for construction.
- ``SafetyReport`` constructor params are ``path, level, concerns,
  holders, project_root`` (not just ``level``).
- **pytest's ``tmp_path`` lives under ``%LOCALAPPDATA%\Temp`` on Windows.**
  SafetyService correctly returns CAUTION (app-data) for everything there.
  Most tests stub ``SafetyService.check_path`` to return SAFE for
  migration-mechanics testing; one explicit test
  (``test_caution_files_appear_in_plan_but_marked_caution``) exercises
  real CAUTION verdict.
- **Unicode arrows crash Typer's CliRunner on Windows cp1252 console.**
  Same lesson as v0.22. ASCII-fied ``→`` to ``->`` and ``↔`` to ``<->``
  in the migrate CLI docstring.
- **No module-level ``console`` or ``err`` in ``cli/main.py``.** Per-command
  pattern is ``console = _console(rt)`` + ``err = _err_console(rt)``.
  Initial CLI tests had 2 NameError failures fixed by adopting the helper
  pattern in ``migrate_cmd`` and threading ``console`` kwarg through
  ``_render_migration_plan`` + ``_render_migration_report`` helpers.
- **Version bump correction.** First attempted bump went from ``1.0.0rc1``
  → ``1.0.0a1``, which would REGRESS per PEP 440 (alpha < rc). Caught and
  corrected to ``1.1.0a1`` (alpha of next minor) since Migration is post-1.0
  NEW functionality per ``DESIGN_PHASE_DELTA.md`` Phase Δ+ Roadmap.

### Phase 2 (deferred to next ship; required for v1.1.0 stable)

- Cross-source migration via the v0.40 ``curator_source_write`` plugin hook
  (local↔gdrive).
- Resume tables (``migration_jobs`` + ``migration_progress`` per
  ``DESIGN_PHASE_DELTA.md`` §M.4).
- Worker pool (``--workers N`` flag).
- ``curator migrate --resume <job_id>`` / ``--list`` / ``--abort``.
- GUI "Migrate" tab.
- ``--keep-source`` / ``--delete-source`` source-action flags (Phase 1
  hardcodes ``trash``).
- Opt-in CAUTION migration via ``--include-caution``.

### Atrium GATE-PM-013 status

Git/backup risk gate (highest-priority CRITICAL from CLAUDE_LOGIC_GATES):
remains "substantially mitigated" by the v1.0.0rc1 git_init ceremony.
This v1.1.0a1 commit lands on ``main`` on top of the existing v1.0.0rc1
tag. Single-disk SPOF still applies; remote push deferred.

---

## Phase Alpha — feature-complete ✅

All 11 implementation steps (0-10) complete. Q13 + Q14 + Q18 + Q22 + broad CLI coverage (post-Alpha) all resolved.
**Phase Alpha closed at 149 passing.** Phase Beta gate 4 v0.41 (Lineage Graph view) brings the total to **897 default-suite passing + 9 opt-in passing (6 watch smoke + 2 safety handle-detection + 1 GUI smoke). 7 correctly-skipped (5 symlink-creation tests skip on Windows without dev mode + 2 from earlier milestones), 0 xfailed, 0 failures, 0 warnings.** 4 LSH perf benchmarks remain available via `pytest tests/perf -m slow -v`.

**Phase Beta is now at ~98% complete. Both gate 4 (GUI, all 7 DESIGN.md §15.2 views shipped) and gate 5 (gdrive plugin with full read/write/delete contract) are functionally complete.** Remaining gaps are bundle creation/editing UI and `curator gdrive auth` interactive helper — both Phase γ polish.

**Phase Beta gate #1 (LSH) COMPLETE through v0.15.** Headline numbers: 1.6x at 100 files, 21.5x at 1k, 196.7x at 10k.

**Phase Beta gate #2 (cross-platform send2trash) v0.18 SHIPPED.** mac (AppleScript) + Linux (freedesktop home trash) backends; platform dispatcher; 20 tests.

**Phase Beta gate #3 (file watcher Tier 6) v0.16 + v0.17 + v0.18 SHIPPED.** WatchService + `curator watch [--apply]` + `ScanService.scan_paths` + `examples/watch_demo.py` + README section.

**Phase Beta gate #5 (Google Drive source plugin) v0.18 SCAFFOLDING.** Plugin module + register/enumerate/stat/read_bytes/delete hooks + 27 mock-based tests. Live OAuth + actual API calls deferred.

**Phase Gamma Milestone Gamma-1 (SafetyService + Organize plan-mode) v0.19+v0.20 SHIPPED.** v0.19 shipped the safety primitives. v0.20 shipped ``OrganizeService`` + ``curator organize <source>`` plan-mode CLI — the first surface that USES SafetyService at scale. Walks indexed files, runs each through SafetyService, buckets into SAFE/CAUTION/REFUSE with per-concern breakdowns. Plan mode only — no moves yet (those come with v0.21+ stage / apply modes + F2 type-specific templates). 21 unit + 5 flow integration + 5 CLI integration tests.

**Phase Gamma Milestone Gamma-2 (Music tagging + destination template) v0.21 SHIPPED (start).** First type-specific organization pipeline. ``MusicService`` reads audio tags via ``mutagen`` (lazy-imported) and proposes canonical ``Artist/Album/NN - Title.ext`` destinations with strict filesystem-safe sanitization (illegal-char replacement, length cap, Windows reserved names). ``curator organize <source> --type music --target <dir>`` extends the plan with proposed destinations on SAFE-bucket audio files. 56 music unit tests + 5 organize-music integration tests. v0.22+ adds Stage / Apply modes (actual moves) + MusicBrainz fallback for missing tags + AcoustID fingerprinting.

**Phase Gamma v0.22 (Stage mode + organize-revert) SHIPPED.** First Curator command that actually moves files in pursuit of organize. ``curator organize <source> --type music --target <dir> --stage <staging>`` moves SAFE-bucket files with proposals into a staging tree mirroring the proposed layout, writes a JSON manifest at ``<staging>/.curator_stage_manifest.json``, and audits each move. New ``curator organize-revert <staging>`` reads the manifest and restores every file to its original location. Per-move outcomes (MOVED / SKIPPED_NO_PROPOSAL / SKIPPED_COLLISION / FAILED for stage; RESTORED / SKIPPED_ORIGINAL_OCCUPIED / SKIPPED_STAGED_MISSING / FAILED for revert) so partial outcomes are reported clearly. Real round-trip demo confirmed: 2 mock-tagged files → staged with sanitized layout (AC_DC/Back in Black/01 - Hells Bells.mp3, Pink Floyd/The Wall/06 - Comfortably Numb.mp3) → reverted → originals restored → manifest deleted. Caught + fixed Windows cp1252 console crash from Unicode arrow chars (←, →) in renderers — replaced with ASCII (``->``, ``<-``). 20 unit + 5 CLI integration tests.

**Phase Gamma v0.23 (Apply mode) SHIPPED.** Closes F2 end-to-end. ``curator organize <source> --type music --target <dir> --apply`` moves SAFE-bucket files with proposals directly to their FINAL destinations under ``--target`` (no staging tree). Mutually exclusive with ``--stage``. ``OrganizeService.apply(plan)`` is a thin wrapper over ``stage(plan, stage_root=plan.target_root, mode="apply")``. The ``stage()`` method gained an optional ``mode: str = "stage"`` parameter that controls audit actor/action: stage produces ``organize.stage.move`` entries, apply produces ``organize.apply.move`` entries. The on-disk manifest format and revert semantics are identical so ``curator organize-revert <target>`` works to undo an apply. Renderer + JSON output indicate which mode was used (heading says ``Apply`` vs ``Stage``; JSON adds ``mode`` field under ``stage``). Real-world demo: 3 mock-tagged files (including AC/DC slash-sanitization + Various Artists compilation handling) moved directly to library tree, audit log confirmed ``organize.apply.move`` action, ``organize-revert`` restored all 3 + cleaned manifest. 12 tests (apply unit + stage with mode parameter + 5 CLI integration including mutual-exclusivity, round-trip, JSON mode field, renderer wording).

**Phase Gamma Milestone Gamma-3 (Photos via EXIF) v0.24 SHIPPED (start).** Second type-specific organization pipeline. ``PhotoService`` (~280 lines) extracts EXIF dates via ``Pillow`` and proposes canonical ``YYYY/YYYY-MM-DD/<original_filename>`` destinations. EXIF date resolution tries DateTimeOriginal → DateTimeDigitized → DateTime → filesystem mtime, with the fallback labeled in ``taken_at_source`` for diagnostics. 25 photo extensions covered (consumer JPEG/PNG/HEIC, professional TIFF, RAW formats from Canon/Nikon/Sony/Olympus/etc.). ``OrganizeService.plan`` and CLI ``--type`` validation extended to accept ``photo``. Reuses Stage/Apply/Revert plumbing entirely. Real-world demo: 5 mixed photos (3 EXIF-tagged, 1 no-EXIF scan with mtime fallback, 1 screenshot) all organized into ``YYYY/YYYY-MM-DD/`` subtrees correctly; 5 audit entries; clean revert. **Caught + fixed mid-build:** new ``photo`` parameter on ``OrganizeService.__init__`` broke 4 existing CLI tests whose ``patched_init`` had a hardcoded signature — swapped to ``*args, **kwargs`` for future-proofing. 44 photo unit tests + 5 CLI integration tests.

**Phase Gamma Milestone Gamma-4 (Cleanup utilities) v0.25 SHIPPED.** F6 closed. ``CleanupService`` (~430 lines) with three independent sub-pipelines sharing a common report shape: ``find_empty_dirs`` (bottom-up walk, treats Thumbs.db / .DS_Store-only dirs as effectively empty by default with a ``--strict`` opt-out, cascades so parents become empty after children), ``find_broken_symlinks`` (catches both symlinks and Windows junctions; uses ``Path.is_symlink`` + ``Path.exists`` for the broken-detection), ``find_junk_files`` (curated platform-junk patterns: Thumbs.db, ehthumbs.db, desktop.ini, .DS_Store, ._*, ~$* Office locks, *.tmp, *.bak, *~ vi/emacs backups, .Trash-*, .nfs* silly-renames; custom patterns supported). Apply mode strategy-picks per kind: ``rmdir`` for empty dirs (system-junk inside is unlinked first), ``unlink`` for broken symlinks, vendored ``send2trash`` for junk files (recoverable via Recycle Bin / Trash) with permadelete fallback. **SafetyService consulted on every apply target** — REFUSE-tier paths (under OS_MANAGED registries) skipped without filesystem touch. New ``curator cleanup`` Typer subcommand group with ``empty-dirs``, ``broken-symlinks``, ``junk`` subcommands; each takes a root + ``--apply`` flag (plan-only by default). ``junk`` adds ``--no-trash`` for permadelete; ``empty-dirs`` adds ``--strict`` for zero-entry rule. Real-world demo: 9-file tree with 7 junk files (Thumbs.db x2 in different dirs, .DS_Store, ~$meeting.md, meeting.md.bak, export.tmp, ._photo.jpg) + 1 always-empty dir + 1 dir-becomes-empty-after-junk-cleanup. After applying both pipelines: 9 audit entries (7 junk_file.delete + 2 empty_dir.delete); the 2 real content files (meeting.md, photo.jpg) preserved untouched along with their containing dirs. 31 unit tests + 7 CLI integration tests; 5 symlink-related tests correctly skip on Windows without admin/dev mode.

**Phase Gamma Milestone Gamma-5 (Documents via PDF/DOCX metadata) v0.26 SHIPPED.** F4 closed. Third type-specific organization pipeline. ``DocumentService`` (~340 lines) extracts dates from a four-source priority chain: PDF embedded metadata (``/CreationDate`` then ``/ModDate``) via ``pypdf``, DOCX/XLSX/PPTX core.xml (``dcterms:created`` then ``dcterms:modified``) via stdlib ``zipfile`` + ``xml.etree`` (no python-docx dep), filename patterns (ISO ``2024-03-15`` / ``2024_03_15`` / ``2024.03.15`` / compact ``20240315`` / year-month ``2024-03`` / year-only ``2024`` with 1900-2099 year-bounds to reject version numbers), and finally filesystem mtime. Default template ``{year}/{year_month}/{filename}`` produces e.g. ``2024/2024-03/Invoice.pdf`` (year-month coarser than photo's year-month-day because docs typically aggregate that way). 24 document extensions covered (.pdf, .doc/.docx, .odt, .rtf, .txt, .md, .rst, .epub/.mobi/.azw/.azw3, .xls/.xlsx/.ods, .csv/.tsv, .ppt/.pptx/.odp/.key, .pages/.numbers). ``OrganizeService.__init__`` extended with optional ``document: DocumentService | None = None``. CLI ``--type`` validation extended from ``{music, photo}`` to ``{music, photo, document}``. Reuses Stage/Apply/Revert plumbing entirely. Real-world end-to-end demo with 6 mixed documents: Invoice.pdf (PDF /CreationDate → ``2024/2024-03/``), Tax Return.pdf (PDF /CreationDate → ``2024/2024-04/``), Q1 Report.docx (DOCX dcterms:created → ``2024/2024-01/``), Meeting Minutes.docx (DOCX → ``2023/2023-09/``), notes_2022-06-15_brainstorm.txt (filename pattern beat its 2025 mtime → ``2022/2022-06/`` -- proving priority order), random_thoughts.md (no metadata, mtime fallback → ``2020/2020-12/``). 6 audit entries (organize.apply.move). All four date sources exercised end-to-end. 58 unit + 5 CLI integration tests. Real PDF + DOCX fixtures built programmatically via pypdf + zipfile in-process — no binary blobs in repo. pypdf added to ``[organize]`` extras (>=4.0).

**Phase Gamma v0.27 (Music enrichment: filename heuristic + MusicBrainz client) SHIPPED.** Closes Gamma-2's last gap. Two pieces, both opt-in shaped: (1) **Filename heuristic** in ``MusicService.read_tags`` runs automatically when mutagen returns ``has_useful_tags=False`` — parses common patterns (``NN - Artist - Title``, ``NN - Title``, ``Artist - Title``, underscore-separator variants like ``Artist_-_Title``) and fills ONLY the blank fields (never overwrites real mutagen tags). Marker ``meta.raw["_filename_source"]="true"`` lets callers tell when the data came from filename inspection. Conservative: requires 1-3 digit track numbers (so ``2024 - Best of Year.mp3`` doesn't get treated as track #2024); falls through patterns by specificity (track+artist+title > track+title > artist+title). (2) **MusicBrainz client** in new ``src/curator/services/musicbrainz.py`` (~250 lines) wraps ``musicbrainzngs`` for canonical lookups. NOT invoked automatically from ``read_tags`` because: network calls are slow (5000-track library = 85min at MB's 1-req/sec rate limit); MB's TOS requires a custom User-Agent identifying app + contact; empty/wrong matches are common for obscure releases. ``MusicBrainzMatch`` dataclass with ``recording_mbid``, ``artist``, ``title``, ``album``, ``album_mbid``, ``year``, ``track_number``, ``score`` fields plus ``is_high_confidence`` property (score>=80). ``MusicBrainzClient.lookup_recording(artist, title)`` queries MB recording index, filters by ``min_score`` (default 70), enriches the best hit with release info (album, year, track number from medium-list). Defensive: catches every error type ``musicbrainzngs`` can raise + returns None rather than propagating; lazy-imports the library so ``import curator.services.musicbrainz`` doesn't pull it in at module load; configures User-Agent exactly once per client instance regardless of call count. Real-world demo with 5-file messy ripped collection (no embedded tags, just filenames): 4 of 5 files correctly organized via filename heuristic alone (``06 - Pink Floyd - Comfortably Numb.mp3`` → ``Pink Floyd/Unknown Album/06 - Comfortably Numb.mp3``; ``Daft Punk - Around the World.mp3`` → ``Daft Punk/Unknown Album/Around the World.mp3``; ``Radiohead_-_Creep.mp3`` underscore-normalized → ``Radiohead/Unknown Album/Creep.mp3``; ``01 - Hells Bells.mp3`` → ``Unknown Artist/Unknown Album/01 - Hells Bells.mp3``); randomfile.mp3 correctly skipped because no pattern matched. **Without v0.27 all 5 would have been silently dropped at the organize step.** ``musicbrainzngs>=0.7`` added to ``[organize]`` extras. 37 unit tests (12 normalize + parse-filename / 5 read_tags integration / 4 MusicBrainzMatch / 16 MusicBrainzClient with mocked sys.modules['musicbrainzngs']). NO live network calls in tests — all MB tests use ``monkeypatch.setitem(sys.modules, ...)`` to inject a fake module.

**Phase Gamma Milestone Gamma-6 (F7 dedup-aware cleanup) v0.28 SHIPPED.** Fourth pipeline added to ``CleanupService``, leveraging Curator's existing ``xxhash3_128`` index from Phase Alpha — no filesystem walk, no rehashing. New ``CleanupKind.DUPLICATE_FILE`` enum value. New module constant ``KEEP_STRATEGIES = ("shortest_path", "longest_path", "oldest", "newest")``. ``CleanupService.__init__`` extended with optional ``file_repo: FileRepository | None = None`` parameter (required for ``find_duplicates``; the other find_* methods don't need it). New method ``find_duplicates(*, source_id=None, root_prefix=None, keep_strategy="shortest_path", keep_under=None) -> CleanupReport`` queries the index via ``FileQuery(has_xxhash=True, source_ids=[source_id] if source_id else None, source_path_starts_with=root_prefix, deleted=False)``, groups results in Python by ``xxhash3_128``, and for each group of size > 1 picks a canonical keeper via the strategy. **keep_under is composable with keep_strategy**: when set, files matching the prefix are preferred, then the strategy breaks ties among them; when no file matches, the strategy applies to the whole group. Each finding's ``details`` records ``kept_path`` (absolute), ``kept_reason`` (strategy + optional ``+ keep_under`` suffix), ``dupset_id`` (the xxhash3_128 itself, used by the renderer for grouping), ``hash``, ``mtime``, ``source_id``. ``_delete_one`` extended: ``DUPLICATE_FILE`` shares the ``JUNK_FILE`` send2trash-with-permadelete-fallback path (single elif branch matching both kinds). Trash mode is the safety net if the dedup heuristic picks the wrong keeper — user can recover from Recycle Bin / Trash. **Runtime wires file_repo into CleanupService automatically**; the CLI gets dedup capability without test setup. New ``curator cleanup duplicates`` CLI subcommand with ``--source SOURCE``, ``--root PREFIX``, ``--keep-strategy STRATEGY``, ``--keep-under PATH``, ``--apply``, ``--no-trash`` flags. Custom rendering: ``_render_cleanup_report`` dispatches DUPLICATE_FILE to ``_render_duplicate_report`` which groups findings by ``dupset_id`` and prints "Set N: kept X (reason)" headers followed by indented duplicate lists with sizes; caps visible groups at 20 with overflow message. Real-world end-to-end demo with 7-file tree containing 2 duplicate sets (3 copies of song.mp3 across Library/Music, Downloads, Backup/Old/2023/Music + 2 copies of beach.jpg across Library/Photos and Downloads) plus 2 unique files: cleanup duplicates plan correctly identified both groups (3 files freeable, 850 B); shortest_path strategy picked Downloads/song.mp3 (18 chars) over Library/Music/song.mp3 (22) and Backup path (30); for the same-length tie between Downloads/beach copy.jpg and Library/Photos/beach.jpg, alphabetic source_path tiebreaker correctly picked Downloads. After --apply --no-trash: 3 files removed, 4 preserved (2 keepers + 2 unique files), 3 audit entries with ``actor=curator.cleanup``, ``action=cleanup.duplicate_file.delete``, ``details`` including ``kept_path``, ``dupset_id``. **Tests: 18 unit at ``tests/unit/test_cleanup_duplicates.py`` (4 guard rails / 2 empty-or-no-dupes / 4 keep-strategy variants / 3 keep_under composability / 1 three-file group / 2 source_id+root_prefix filter narrowing / 3 apply path including audit + safety-REFUSE) + 6 CLI integration at ``tests/integration/test_cli_cleanup_duplicates.py`` (subcommand-listed-in-help / strategies-listed-in-help / plan-after-real-scan / apply-keeps-keeper-removes-dupes / keep_under-flips-keeper / no-duplicates-clean-plan).** Mid-build catches: tests initially imported ``Database`` from ``curator.storage.database`` (module is actually ``CuratorDB`` from ``curator.storage``); SourceRepository.upsert takes a ``SourceConfig`` dataclass not kwargs; one assertion miscounted path lengths (``C:/Music/song.mp3`` is 18 chars, shorter than ``C:/Downloads/song.mp3`` at 21).

**Phase Gamma F8 (index sync for cleanup) v0.29 SHIPPED.** Closes the phantom-file gap left open by v0.25 (cleanup) and v0.28 (dedup). When ``CleanupService.apply`` removes a file from disk, the corresponding ``FileEntity`` (if present in the index) is now soft-deleted via ``file_repo.mark_deleted(curator_id)`` so subsequent queries don't return phantoms. New private method ``_mark_index_deleted(finding)`` is called from ``apply`` after each successful deletion, **before** the audit log (so deletes are atomic from the index's perspective even if audit fails later). Best-effort throughout — NEVER raises and NEVER fails the apply: if ``file_repo`` is None (legacy callers / older tests), if the file isn't in the index (typical for junk files like Thumbs.db that classification filters out), if ``find_by_path`` raises, or if ``mark_deleted`` raises (DB locked etc.). Empty-dir findings are explicitly short-circuited because directories aren't ``FileEntity`` rows. Source resolution: DUPLICATE_FILE findings carry ``source_id`` in ``finding.details`` (set by ``find_duplicates`` in v0.28); other kinds default to ``"local"`` since that's the only source whose ``source_path`` is an absolute filesystem path. Real-world end-to-end demo with 2-file dup pair: first ``cleanup duplicates`` plan returns count=1; ``--apply --no-trash`` removes the duplicate from disk AND marks its FileEntity ``deleted_at=<now>``; second ``cleanup duplicates`` plan returns **count=0** (vs. count=1 in v0.28 — the phantom-file gap is provably closed). Index after the cycle: 5 active rows + 1 soft-deleted row (the recently-removed duplicate's record is preserved for audit/lineage but filtered from active queries via ``FileQuery(deleted=False)``). Tests: 8 unit at ``tests/unit/test_cleanup_index_sync.py`` covering: duplicate-apply marks dup deleted + leaves keeper untouched / subsequent-find_duplicates-does-not-re-flag (the headline test that proves the gap is closed) / junk-apply-marks-indexed-file-deleted / junk-apply-on-unindexed-file-no-error (common Thumbs.db case) / empty-dir-apply-does-NOT-touch-index (proven via MagicMock spec=FileRepository asserting find_by_path/mark_deleted call counts == 0) / apply-works-with-no-file_repo (legacy null-safety) / mark_deleted-exception-does-not-fail-apply (best-effort confirmation) / find_by_path-exception-does-not-fail-apply. **All four cleanup pipelines (junk + empty_dir + broken_symlink + duplicate_file) now keep the index consistent automatically.** Default suite: **650 passing, 7 correctly-skipped, 35.5s.** Opt-in suite: **658 total passing.**

**Phase Gamma F9 (fuzzy near-duplicate detection via LSH) v0.30 SHIPPED.** Adds a ``match_kind="fuzzy"`` mode to ``CleanupService.find_duplicates`` that uses the Phase Beta LSH service (``FuzzyIndex``) to catch near-duplicates that don't share an xxhash3_128 — re-encoded JPEGs, re-compressed MP3s, re-OCR'd PDFs, edited text files, etc. New module constant ``MATCH_KINDS = ("exact", "fuzzy")`` and ``DEFAULT_FUZZY_SIMILARITY_THRESHOLD = 0.85`` (intentionally **stricter than the lineage default of 0.5** because cleanup is destructive — false positives mean the user accidentally deletes a file they wanted to keep). ``find_duplicates`` gains two new keyword-only parameters: ``match_kind: str = "exact"`` and ``similarity_threshold: float = 0.85`` (only consulted in fuzzy mode). Validation raises ``ValueError`` for unknown ``match_kind``; ``FuzzyIndexUnavailableError`` propagates up if datasketch isn't installed (rather than silently returning empty — the user explicitly asked for fuzzy mode). New private method ``_find_fuzzy_duplicates`` does the work: pulls files via ``FileQuery(has_fuzzy_hash=True, ...)``, builds a ``FuzzyIndex(threshold=similarity_threshold)``, adds every fuzzy_hash, then for each file queries for LSH neighbors and builds an adjacency map. Walks **connected components** via iterative BFS so transitive groups (A~B and B~C imply {A,B,C} are one set, even if A~C isn't a direct LSH bucket hit) collapse into one duplicate group. Component IDs use synthetic ``"fuzzy:0"``, ``"fuzzy:1"``, ... format so the renderer can group findings by set. Each fuzzy finding's ``details`` records ``match_kind="fuzzy"``, ``dupset_id``, ``fuzzy_hash``, ``kept_fuzzy_hash``, ``similarity_threshold`` so callers can audit exactly which threshold fired and compare hashes. Defensive: malformed fuzzy_hashes are recorded as report.errors but don't abort; LSH self-matches are filtered out; the index is built once per call (not per query). Existing exact-mode path got one new field too — ``match_kind="exact"`` in details — so the renderer can label every report unambiguously. CLI in ``src/curator/cli/main.py``: ``cleanup duplicates`` gains ``--match-kind exact|fuzzy`` (default exact) and ``--similarity-threshold FLOAT`` (default 0.85) flags; help text warns about fuzzy false-positive risk. Custom renderer ``_render_duplicate_report`` reads ``findings[0].details["match_kind"]`` and prints "Cleanup (duplicate file, fuzzy / near-duplicate)" vs "... exact" so users can't mistake fuzzy mode for exact at a glance. Real-world end-to-end demo with 3 text files (2 derived from the same 4000-char varied-prose base with a tiny edit at the end + 1 unrelated): EXACT mode found **0 duplicates** (xxhash3_128 differs because the bytes differ); FUZZY mode at threshold 0.5 found **1 group / 1 freeable file** (3.9 KB) correctly identifying ``Downloads/document_v2.txt`` as a duplicate of ``Library/document.txt``; FUZZY mode at strict threshold 0.99 found **0 duplicates** (threshold rejected even the near-identical pair, proving the knob works). The unrelated file was correctly never flagged. Tests: 10 unit at ``tests/unit/test_cleanup_fuzzy_duplicates.py``: 3 validation (MATCH_KINDS exposes both / invalid match_kind raises / DEFAULT_FUZZY_SIMILARITY_THRESHOLD is 0.85), 2 empty cases (no fuzzy hashes returns empty / isolated unrelated files not flagged), 3 near-duplicate detection (two-file group with all metadata correctly recorded / threshold tightening at 0.99 suppresses loose matches that 0.5 catches / three-file transitive grouping via union-find produces 2 findings + 1 keeper sharing one dupset_id), 2 strategy interactions in fuzzy mode (oldest strategy correctly picks earlier mtime / keep_under flips keeper to the prefix-matching file). Test fixtures use ``ppdeep.hash`` over **varied pseudo-random prose** (4000 chars from `string.ascii_lowercase + ' '` with a deterministic seed) — critical because repeated text like ``"foo " * 100`` produces degenerate fuzzy hashes (``YYYYYYYY``-style) whose n-gram MinHash signatures are nearly identical regardless of source content (initial test attempt with repeated phrases failed at jaccard ~0.20; varied prose gives 0.81-0.85 for near-dupes and 0.0 for unrelated, exactly the dynamic range needed). All apply-path machinery (send2trash via ``_delete_one``, audit logging, safety REFUSE checks, **v0.29 index sync**) works identically for fuzzy duplicates because the kind is still ``CleanupKind.DUPLICATE_FILE`` — only the discovery method changed. Default suite: **660 passing, 7 correctly-skipped, 38.4s.** Opt-in suite: **668 total passing.**

**Phase Gamma F5 (code project organization) v0.31 SHIPPED.** Fourth type-specific organize pipeline. New ``src/curator/services/code_project.py`` (~360 lines) with ``CodeProjectService`` and ``CodeProject`` dataclass. Discovery: ``find_projects(root)`` walks the tree looking for **VCS marker dirs** (``.git``, ``.hg``, ``.svn``, ``.bzr``, ``_darcs``); when a project root is found the walk **prunes** that subtree so nested submodules absorb into the parent project rather than fragmenting. Skips configured noise dirs (``node_modules``, ``__pycache__``, ``.venv``, ``target``, ``build``, ``dist``, ``.idea``, ``.vscode``, etc.) **during the discovery descent** so a code library buried inside vendor dirs doesn't pollute results. Analysis: ``analyze_project(root)`` walks the project body, counts files by extension, picks the dominant ``primary_language`` from a curated mapping (29 languages incl. Python/JS/TS/Go/Rust/Java/Kotlin/Scala/Clojure/Ruby/C/Cpp/Csharp/Swift/Haskell/OCaml/Elixir/Lua/R/Julia/Nim/Zig/Vlang/Dart). **Language inference is by file count, not size** — a 50 MB binary blob can't outvote 200 .py files. Markdown / Documentation files don't get weighted (they're registered but their count contributes 0 to the dominance vote) so a doc-heavy project still gets classified by its real code. ``IGNORED_EXTENSIONS`` filters .pyc / .lock / images / archives / binaries / etc. Tiebreaker for equal counts is alphabetic on language name for determinism. Files in ``SKIP_DIR_NAMES`` (which includes ``node_modules``) are skipped during analysis too — critical for correctness because a JS project with a node_modules full of vendored .py installer scripts must still be classified as JavaScript. Destinations: ``propose_destination(project, file_path, target_root) -> {target}/{language}/{project_name}/{rel_subpath}``, preserving each project's internal layout (``project/src/main.py`` lands at ``Python/project/src/main.py``, NOT flattened). ``find_project_containing(file_path, projects)`` returns the most-specific project whose root contains the file (longest matching root_path wins; relevant if find_projects ever changed its pruning behavior). Wired into ``OrganizeService.__init__`` as optional ``code: CodeProjectService | None = None``; into ``OrganizeService.plan`` as a new ``elif organize_type == "code"`` branch. **Critical detail discovered during integration**: project files inherently land in CAUTION (the .git/ marker triggers SafetyService's ``project_file`` concern — by design, so users don't accidentally reorganize active git repos). For music/photo/document modes, only SAFE files get proposals (don't auto-reorganize app-data music). For code mode, that guard is loosened: CAUTION files DO get proposals because the user passing ``--type code`` IS the explicit consent to organize project files. The bucketing stays the same; only the proposal computation widens. Performance: code projects are discovered ONCE per ``plan()`` call and the result list is reused for per-file ``find_project_containing`` lookups, so a 1000-file scan with 50 projects walks the tree exactly once. CLI in ``src/curator/cli/main.py``: ``--type`` validation extends from ``{music, photo, document}`` to ``{music, photo, document, code}``; help text updated to describe the code pipeline. Real-world end-to-end demo with 22 files across 4 projects + 2 loose files: **scrapbook** (Python, git, 6 files including app/models.py + tests/), **rusty-tool** (Rust, git, 3 files), **frontend-app** (TypeScript, git, 9 files including 5 adversarial .py files inside node_modules — **language correctly inferred as TypeScript not Python because node_modules was skipped during analysis**), **legacy-thing** (Go, **.hg** marker not just .git, 2 files). Plan output: 20 CAUTION (correctly flagged "project_file"), 2 SAFE (the loose files). Loose files at the lib root were correctly **not** proposed (the ``find_project_containing`` returned None for them). Each project's internal subpath was preserved in the proposal: ``<target>/Python/scrapbook/app/models.py``, ``<target>/Rust/rusty-tool/src/main.rs``, etc. Tests: 36 unit at ``tests/unit/test_code_project.py`` (7 is_project_root including .git/.hg/.svn detection + non-VCS rejection + .git-as-file-not-dir guard / 8 find_projects incl. multi-project / pruning at boundaries / depth tolerance / SKIP_DIR_NAMES skipping / search-root-IS-a-project / nonexistent-root-empty / 11 analyze_project incl. file count + size + language inference for Python/TypeScript / node_modules adversarial test / __pycache__ skipping / Unknown-for-empty / Unknown-for-only-ignored-extensions / alphabetic tiebreaker / Markdown-doesnt-outvote-real-code / records last_modified / records vcs_type / 3 propose_destination incl. basic + subpath-preservation + outside-project-returns-None / 2 find_project_containing incl. correct-project + None-for-unrelated / 5 module-level constant tests). 5 CLI integration at ``tests/integration/test_cli_organize_code.py`` (help-lists-code-type / invalid-type-rejected / code-type-requires-target / plan-proposes-Python-and-Rust-destinations-for-real-projects / loose-file-outside-project-not-proposed). **Mid-build catch worth documenting**: initial CLI integration test looked for proposals in the SAFE bucket (assumption from how music/photo/document tests work). They actually land in CAUTION because of the project-root concern. Probe revealed `safe=0 caution=2` for a 2-file Python project. Fix was twofold: (1) loosen the proposal guard in OrganizeService.plan to compute proposals for CAUTION files when organize_type="code", (2) update integration tests to look in CAUTION bucket for code-mode proposals. The trade-off: code mode now relies on the user's --type code as their consent for moving project files, rather than safety-bucketing them out. The CAUTION verdict is still recorded, just no longer blocking the proposal. Default suite: **701 passing, 7 correctly-skipped, 37.3s.** Opt-in suite: **709 total passing.**

**Phase Gamma MB auto-enrichment wiring v0.32 SHIPPED.** Wires the v0.27 ``MusicBrainzClient`` (built but never invoked) into the ``OrganizeService.plan`` pipeline as an opt-in enrichment step, gated by a new ``--enrich-mb`` CLI flag. Source changes: (1) ``MusicService`` gains a new ``enrich_via_musicbrainz(metadata, mb_client) -> MusicMetadata`` method that **fills only blank fields** (album / year / track_number) and never overwrites existing real data — a user-tagged ``year=1980`` survives even when MB says 1979. Skip cases: complete tags (album AND year AND track present — no point in a network call), missing artist or title (nothing to query), MB returning None (records ``raw["_mb_no_match"]="true"``), MB raising any exception (swallowed via try/except, metadata returned unchanged). Successful enrichments record ``raw["_mb_enriched"]="true"``, ``raw["_mb_recording_mbid"]=<mbid>``, and ``raw["_mb_score"]=<score>`` so callers can audit which lookups fired and at what confidence. (2) ``OrganizeService.__init__`` gains optional ``mb_client: MusicBrainzClient | None = None`` parameter (None by default — MB enrichment is strictly opt-in). ``plan()`` gains ``enrich_mb: bool = False`` keyword parameter; when True AND ``self.mb_client is not None`` AND the file is audio AND ``meta.raw.get("_filename_source") == "true"`` AND at least one of album/year/track is missing, calls ``self.music.enrich_via_musicbrainz(meta, self.mb_client)`` BEFORE computing the destination. The destination then uses the canonical album name (e.g. ``The Wall``) instead of ``Unknown Album``. MB exceptions in the plan loop are caught and logged at debug level so a single failed lookup doesn't poison the entire plan. (3) CLI in ``src/curator/cli/main.py``: ``curator organize`` gains ``--enrich-mb`` flag and ``--mb-contact <email-or-url>`` flag. Validation: ``--enrich-mb`` requires ``--type music`` (rejected with exit code 2 if user combines it with photo/document/code), and requires ``--mb-contact`` because MusicBrainz TOS mandates a contact in the User-Agent. The contact must come from the user explicitly — we don't ship a default-contact that would attribute every Curator user's MB traffic to the same address. CLI also checks ``_musicbrainzngs_available()`` before constructing the client and prints an actionable install hint if missing. The MusicBrainzClient is constructed only when --enrich-mb is set and attached directly to ``rt.organize.mb_client`` for the duration of the invocation (no permanent runtime mutation). Real-world end-to-end demo with a real OrganizeService and stubbed read_tags returning a filename-heuristic-flavor MusicMetadata for ``06 - Pink Floyd - Comfortably Numb.mp3`` (artist=Pink Floyd, title=Comfortably Numb, track=6, no album, no year, ``_filename_source=true``): WITHOUT --enrich-mb the proposed destination is ``<target>/Pink Floyd/Unknown Album/06 - Comfortably Numb.mp3`` (0 MB calls made); WITH --enrich-mb returning a canned MB match for The Wall/1979/track 6, the proposed destination becomes ``<target>/Pink Floyd/The Wall/06 - Comfortably Numb.mp3`` (1 MB call made). The Unknown Album fallback is replaced with the real album name. Tests: **10 unit** at ``tests/unit/test_music_mb_enrichment.py`` (3 skip cases: complete-tags-skip / missing-artist-skips / missing-title-skips / 4 fills-blanks: filename-only-gets-album-year-track / partial-only-fills-actual-blanks / existing-year-not-overwritten / existing-track-not-overwritten / 3 no-match-and-errors: no-match-records-marker / mb-exception-swallowed / partial-match-with-only-album). **8 integration** at ``tests/integration/test_organize_mb_enrichment.py`` (5 plan integration: enrichment-fires-for-filename-only-track / skipped-when-enrich_mb=False / skipped-for-files-with-real-tags / no-mb-client-doesnt-crash / mb-exception-doesnt-fail-plan / 3 CLI validation: help-lists-enrich-mb / requires-mb-contact / requires-music-type). **All 18 tests use mocked MB clients — no live network calls.** Default suite: **719 passing, 7 correctly-skipped, 36.2s.** Opt-in suite: **727 total passing.**

**Phase Gamma organize index sync v0.33 SHIPPED.** Closes the phantom-file gap left open by stage / apply / revert: when an organize move physically relocates a file on disk, the corresponding ``FileEntity.source_path`` is now updated to match. Without this, ``find_by_path(original)`` would have returned a stale entity pointing at a path that no longer exists, and the next scan would have treated the new location as a brand-new file while the old location appeared as a deletion — even though it's the same file. New private method ``OrganizeService._sync_index_after_move(curator_id_str, new_path)`` handles all three flows: stage moves (called with the staged path), apply moves (called with the target path — apply delegates to stage internally), and revert moves (called with the original path, restoring the FileEntity to where it started). Best-effort throughout: NEVER raises and NEVER fails the parent move. Skip conditions documented and tested: ``self.files`` is None (defensive), malformed curator_id, ``files.get`` raises (DB unreachable), ``files.update`` raises (DB locked), ``get`` returns None (entity missing from index for some reason — shouldn't happen because plan pulled it from the index, but guarded against the race). Wire-in is minimal: a single ``self._sync_index_after_move(cid, str(staged))`` call after ``report.moves.append(StageMove(... outcome=MOVED))`` in ``stage()`` and an analogous call after ``RevertMove(... outcome=RESTORED)`` in ``revert_stage()``. Real-world end-to-end demo: indexed an untagged.mp3 file at ``library/untagged.mp3``; before apply, ``find_by_path(original)`` returned the entity and its source_path was the original path. After ``org.apply(plan)`` (which moved the file to ``Music/Pink Floyd/The Wall/06 - Comfortably Numb.mp3``): on disk, the original path no longer existed and the new location did; in the index, the FileEntity's source_path was updated to the new location, ``find_by_path(original)`` returned None (no phantom), and ``find_by_path(new_location)`` returned the entity. **The phantom-file gap is provably closed for organize moves**, completing the sync story that v0.29 started for cleanup deletes. Tests: **9 unit** at ``tests/unit/test_organize_index_sync.py`` covering: apply-updates-source_path-to-target (the headline case) / find_by_path-works-for-new-path / find_by_path-OLD-returns-None-after-apply (the headline regression check) / stage-mode-also-updates / revert-resets-source_path-to-original / skipped-no-proposal-does-NOT-update / skipped-collision-does-NOT-update / update-exception-does-not-fail-move (uses MagicMock file_repo with update.side_effect=RuntimeError; verifies the on-disk move still succeeds AND that update WAS attempted, proving the exception path was actually exercised) / get-exception-does-not-fail-move (same pattern but failure at lookup). **All four cleanup pipelines (v0.29) AND all organize move flows (v0.33) now keep the index consistent automatically; no manual ``curator scan`` re-run needed after any destructive operation.** Default suite: **728 passing, 7 correctly-skipped, 39.5s.** Opt-in suite: **736 total passing.**

**Zero open implementation questions remaining.**

---

## Phase Alpha implementation sequence

### ✅ Step 0 — Project scaffold
pyproject.toml, README, BUILD_TRACKER, .gitignore, src/ layout, py.typed.

### ✅ Step 1 — Tier 1: pydantic models
DESIGN.md §3. 10 model files under `src/curator/models/`.

### ✅ Step 2 — Tier 1: storage layer
DESIGN.md §4. Connection, schema_v1.sql, migrations, queries, exceptions, helpers + 8 repositories.

### ✅ Step 4 — Tier 3: plugin framework + 5 core plugins
DESIGN.md §5. Plugin manager singleton, 15-hook hookspec catalog, LocalFSSource, classify_filetype, lineage_hash_dup, lineage_filename, lineage_fuzzy_dup. (Pulled forward before Step 3 because hash pipeline depends on `curator_source_read_bytes`.)

### ✅ Step 3 — Tier 2: hash pipeline + classification + audit
DESIGN.md §7, §17.3, §2.2.1. AuditService, ClassificationService, HashPipeline.

### ✅ Step 5 — Tier 2: lineage, bundle, trash, scan services
DESIGN.md §8.4, §9.3, §10, §2.2. LineageService, BundleService, TrashService, ScanService.

### ✅ Step 6 — Configuration
DESIGN.md §16. `defaults.py` + `Config` class (TOML load/save, `platformdirs` auto-paths, dotted-path access, deep-merge).

### ✅ Step 7 — Tier 7: CLI
DESIGN.md §11. Typer app, `CuratorRuntime` container, 9 commands.

### ✅ Step 8 — Vendoring
ppdeep (Apache-2.0, full file with `hash_buf` alias) + send2trash (BSD-3-Clause, Windows-only subset). Datasketch deferred to Phase Beta. Doctor reports both as `vendored`.

### ✅ Step 10 — End-to-end smoke test against project itself

Run on May 6, 2026 against `C:\Users\jmlee\Desktop\AL\Curator\src\curator`:

```
files seen      |     60
  new           |     60
files hashed    |     60
bytes read      | 294,187 (287 KB)
classifications |     59
lineage edges   |      0  (no dups within Curator's own source)
errors          |      0
duration        | 0.47s
```

Re-scan: `files_hashed=0`, `cache_hits=60`, `bytes_read=0` — cache short-circuit works.

`inspect` on `services/hash_pipeline.py` shows xxhash + md5 + **fuzzy hash** (`192:rmgy50DvhmsSyaINoxTQBy8DrQ...`), confirming the vendored ppdeep is wired into the real pipeline.

`doctor` shows 5 plugins, `ppdeep: vendored`, `send2trash: vendored`, all 60 files in cache, 1 source enabled.

### ✅ Step 9 — Testing (pytest harness)

Promoted all session smoke tests into a proper pytest suite. **64 tests passing, 0 xfailed, 0 failures, 4.89s wall-clock** (after Q13 fix).

Files added under `tests/`:
- `conftest.py` — fixtures: `db`, `db_path`, `tmp_tree`, `make_file`, `repos`, `plugin_manager`, `services`, `local_source`. Also typed namespaces `Repos` and `Services`. Critical fix during this step: `tmp_tree` and `db_path` are now SIBLINGS inside `tmp_path` (`<tmp>/tree` and `<tmp>/db/curator_test.db`) — previously they shared `tmp_path`, so scan tests picked up the SQLite DB and WAL/SHM as "files in the tree".
- `tests/unit/__init__.py` (placeholder)
- `tests/unit/test_models.py` — 13 tests covering FileEntity, LineageEdge, BundleEntity, BundleMembership, TrashRecord, AuditEntry, SourceConfig invariants.
- `tests/unit/test_storage.py` — 21 tests covering all 8 repositories.
- `tests/unit/test_lineage_detectors.py` — 11 tests, one set per detector.
- `tests/integration/__init__.py` (placeholder)
- `tests/integration/test_scan_flow.py` — 6 tests covering ScanService.
- `tests/integration/test_trash_restore.py` — 6 tests covering TrashService.
- `tests/property/__init__.py` (placeholder)
- `tests/property/test_lineage_normalization.py` — 3 hypothesis-based property tests for the symmetric-edge invariant. **All 3 pass after Q13 fix.**

**Test breakdown:**
| Layer | Tests | Status |
|---|---|---|
| Unit — Models | 13 | ✅ All pass |
| Unit — Storage | 21 | ✅ All pass |
| Unit — Lineage detectors | 11 | ✅ All pass |
| Integration — Scan flow | 6 | ✅ All pass |
| Integration — Trash/restore | 6 | ✅ All pass |
| Property — Lineage normalization | 3 | ✅ All pass (Q13 invariant enforced) |
| **Total** | **64** | **64 passing, 0 xfailed, 0 failures** |

Issues caught during the smoke→pytest conversion:
1. **conftest tmp_path collision.** Three scan tests failed because the SQLite DB + WAL/SHM showed up as 3 extra "files" in scan results. Fixed by making `tmp_tree` = `<tmp_path>/tree` and `db_path` = `<tmp_path>/db/curator_test.db` (siblings).
2. **AuditEntry default sentinel.** Test assumed `audit_id` defaults to `None`; actual sentinel is `-1`. Test corrected.
3. **lineage_filename detector requires both filenames matched.** Test corrected to use two parenthesized copies.

---

## Q13 fix (post-Alpha, 2026-05-06)

Symmetric-edge normalization. Both `lineage_hash_dup` and
`lineage_fuzzy_dup` detectors now canonicalize direction before
constructing the `LineageEdge`:

```python
# Canonicalize direction (Q13). Symmetric edge — emit from = min, to = max.
if str(file_a.curator_id) > str(file_b.curator_id):
    file_a, file_b = file_b, file_a
```

This makes the unique constraint `(from, to, kind, detector)` correctly
deduplicate when the detector runs on `(A, B)` and later on `(B, A)`.
Both calls now produce the same canonical edge tuple.

The test `test_dup_detector_emits_canonical_direction_for_symmetric_edges`
was previously `@xfail`; the marker is now removed and the test enforces
the invariant via 50 hypothesis-generated UUID pairs.

---

## Progress

| Step | Status | Files | Tests |
|---|---|---|---|
| 0 — Scaffold | ✅ | 5 | — |
| 1 — Models | ✅ | 11 | 13 |
| 2 — Storage | ✅ | 14 | 21 |
| 3 — Hash/classify/audit | ✅ | 4 | (covered in integration) |
| 4 — Plugins | ✅ | 9 | 11 (detectors) |
| 5 — Lineage/bundle/trash/scan | ✅ | 4 | 12 (integration) |
| 6 — Config | ✅ | 2 | (deferred to phase beta) |
| 7 — CLI | ✅ | 3 | (deferred — exercised end-to-end via Step 10) |
| 8 — Vendoring | ✅ | 9 | (covered in integration scan flow) |
| 9 — Testing | ✅ | 9 | 64 passing |
| 10 — Smoke test | ✅ | — | Real project scan against `src/curator` |
| Q13 — Symmetric edges | ✅ | (2 detectors patched) | property test enforces invariant |

**Source files: ~40. Test files: 9. Total LOC: ~5,500.**

---

## Open implementation questions

1. **Resolved (Step 2):** Repositories take a `CuratorDB`; tests use temp-path `CuratorDB` per test.
2. **Resolved (Step 2):** Flex attrs loaded eagerly.
3. **Resolved (Step 8):** Vendor under `_vendored/ppdeep/` with attribution preserved.
4. **Resolved (Step 2):** Custom datetime adapters for Python 3.12+.
5. **Resolved (Step 2):** Lineage edge insert defaults to `on_conflict="ignore"`.
6. **Resolved (Step 4):** Plugin manager built before hash pipeline.
7. **Resolved (Step 3):** Phase Alpha sequential hash pipeline.
8. **Resolved (Step 3):** Hash pipeline hashes isolated files for change detection.
9. **Resolved (Step 3):** Single-pass file read computes xxhash + md5 simultaneously.
10. **Resolved (Step 3):** Audit service mirrors every entry to Loguru.
11. **Resolved (Step 4):** lineage_filename detector requires same-directory siblings.
12. **Resolved (Step 5):** TrashService refuses to trash without send2trash.
13. **Resolved (post-Alpha, 2026-05-06):** Symmetric DUPLICATE and NEAR_DUPLICATE detectors now normalize direction so `from = min(curator_id), to = max(curator_id)`. Property test enforces the invariant.
14. **Resolved (post-Alpha, 2026-05-06):** TrashService now tracks ``os_trash_location`` on Windows by parsing ``$IXXXXXX.ext`` metadata files in ``<drive>:\$Recycle.Bin\<SID>\`` after ``send2trash`` returns. Both v1 (Vista–Win 8) and v2 (Win 10+) formats are supported. ``restore --apply`` now actually moves the file back from the Recycle Bin instead of raising ``RestoreImpossibleError``. The recycle-bin reader lives at ``src/curator/_vendored/send2trash/win/recycle_bin.py``. 9 parser tests + 2 service-level Q14 tests + 1 CLI round-trip test enforce the contract.
15. **Resolved (Step 6):** TOML parsing uses stdlib `tomllib`; `tomli` fallback for 3.10. Auto-resolution of `db_path` / `log_path` at `Config.load()` time.
16. **Resolved (Step 7):** CLI single-file pattern. Identifier resolution accepts UUID / absolute path / path prefix.
17. **Resolved (Step 7→8):** `bundles_show` JSON output uses an explicit `if f is not None` guard.
18. **Resolved (post-Alpha, 2026-05-06):** ``curator sources list/show/add/enable/disable/remove`` subcommands shipped. The ``add`` command takes ``--type`` (default ``local``), ``--name``, ``--disabled``. The ``remove`` command requires ``--apply`` and respects FK RESTRICT (refuses if files still reference the source). All mutating actions write audit entries with actor ``cli.sources``. 23 pytest tests under ``tests/integration/test_cli_sources.py`` cover happy paths and error modes via Typer's ``CliRunner`` (first CLI test file in the suite — pattern for further CLI coverage in Phase Beta).
19. **Resolved (Step 8):** `doctor` checks vendored before PyPI for both ppdeep and send2trash.
20. **Resolved (Step 9):** `tmp_tree` and `db_path` fixtures are siblings (not the same dir).
21. **Resolved (Step 9):** AuditEntry's default `audit_id` is `-1` (not None).
22. **Resolved (post-Alpha, 2026-05-06):** ``asyncio_mode = "auto"`` removed from ``[tool.pytest.ini_options]`` in pyproject.toml. We have no async tests in Phase Alpha; Phase Beta can re-add this when ``pytest-asyncio`` is installed and async tests exist. Eliminates the ``PytestConfigWarning`` that was firing on every test run.

---

## Phase Alpha → Phase Beta gates

Remaining gates (Q13, Q14, Q18, Q22, broad CLI coverage all cleared post-Alpha):

1. **Vendor datasketch** for LSH-based fuzzy candidate selection (currently O(n) over `find_with_fuzzy_hash()`). Heavy deps (numpy + scipy) make full vendoring unwise; better to add to ``[beta]`` extras and import normally. **Status: gate #1 COMPLETE through v0.15.** v0.13 — standalone ``FuzzyIndex`` class. v0.14 — wired into ``LineageService`` and ``CuratorRuntime``; LSH path produces identical edges to O(n) baseline (4 equivalence tests). v0.15 — ``tests/perf/`` benchmark proves **1.6x speedup at 100 files, 21.5x at 1k, 196.7x at 10k**. See ``docs/PHASE_BETA_LSH.md``.
2. **Cross-platform send2trash vendoring** — expand from Windows-only to mac + linux. **Status: v0.18 SHIPPED.** mac backend uses AppleScript via ``osascript`` (no pyobjc dep). Linux backend implements freedesktop.org Trash spec 1.0 home trash (cross-filesystem trash deferred to Phase Gamma). Platform dispatcher routes by ``sys.platform``. 20 tests covering helpers + freedesktop live moves + dispatcher + mac platform guard.
3. **File watcher service** — `watchfiles` integration (Tier 6 in DESIGN §2.1). **Status: v0.18 SHIPPED (gate fully complete).** v0.16: ``WatchService`` + ``PathChange`` types + ``_Debouncer`` + default ignore patterns; ``curator watch [SOURCE]`` CLI command with ``--json`` output. v0.17: ``ScanService.scan_paths(source_id, paths)`` for incremental scans; ``curator watch --apply`` flag pipes events into ``scan_paths`` so the index stays live. v0.18: ``examples/watch_demo.py`` + README section on reactive scanning. 22 watch unit tests + 6 real-filesystem smoke tests + 12 scan_paths integration tests. See ``docs/PHASE_BETA_WATCH.md``.
4. **GUI (Tier 7)** — likely PyQt6 per DESIGN §15.
5. **Cloud source plugins** (Tier 3 expansion) — Google Drive (`PyDrive2`), OneDrive (`msgraph-sdk`), Dropbox (`dropbox-sdk-python`). **Status: gdrive scaffolding SHIPPED.** Plugin module ``src/curator/plugins/core/gdrive_source.py`` with register / enumerate / stat / read_bytes / delete hooks, lazy-imports PyDrive2, registers in ``register_core_plugins``, returns None from ``register`` when PyDrive2 isn't installed (so ``gdrive:`` source IDs aren't claimed in that case). 27 unit tests using injected fake Drive clients. **Live OAuth flow + actual API calls deferred** — user must run PyDrive2's interactive auth separately to populate credentials.json before using. OneDrive + Dropbox plugins not started.

---

## Revision log

- **2026-05-05 v0.1** — Steps 0+1.
- **2026-05-05 v0.2** — Step 2.
- **2026-05-05 v0.3** — Step 4.
- **2026-05-05 v0.4** — Step 3.
- **2026-05-05 v0.5** — Step 5. Phase Alpha core feature-complete (services).
- **2026-05-06 v0.6** — Steps 6 + 7. CLI working.
- **2026-05-06 v0.7** — Step 8. Vendoring (ppdeep + send2trash). Q17 + Q19 fixed in passing.
- **2026-05-06 v0.8** — Steps 9 + 10. Phase Alpha complete. 63-test pytest harness (1 xfail documenting Q13). End-to-end smoke against `src/curator` (60 files / 0.47s / fuzzy hashes computed). Q20 + Q21 surfaced and fixed during the test conversion.
- **2026-05-06 v0.9** — **Q13 fixed.** Both symmetric detectors (`lineage_hash_dup`, `lineage_fuzzy_dup`) now canonicalize direction (smaller curator_id → `from`). xfail removed from property test; test now enforces the invariant via 50 hypothesis trials per run. **64 passing, 0 xfailed, 0 failures.**
- **2026-05-06 v0.10** — **Q18 fixed.** Six ``curator sources`` subcommands shipped (list/show/add/enable/disable/remove). 23 CLI tests added under ``tests/integration/test_cli_sources.py`` via Typer's ``CliRunner`` — first CLI test file in the suite. **87 passing, 0 xfailed, 0 failures.**
- **2026-05-06 v0.11** — **Q22 fixed + broad CLI coverage shipped.** ``asyncio_mode`` removed from pyproject (eliminates `PytestConfigWarning`). ``runner`` and ``cli_db`` fixtures promoted to top-level conftest. Four new test files added: ``test_cli_bundles.py`` (16 tests, list/show/create/dissolve incl. Q17 path-None-guard regression), ``test_cli_trash_restore.py`` (12 tests, --apply gating + audit + Phase Alpha RestoreImpossibleError exit-code-2 contract), ``test_cli_read_commands.py`` (12 tests, inspect/lineage/audit JSON paths), ``test_cli_scan_group_doctor.py`` (12 tests, --ignore option, primary-pick strategies, doctor output sections). **139 passing, 0 xfailed, 0 failures, 0 warnings.**
- **2026-05-06 v0.12** — **Q14 fixed.** Wrote a Windows Recycle Bin reader at ``src/curator/_vendored/send2trash/win/recycle_bin.py`` that parses ``$IXXXXXX.ext`` metadata files (both v1 and v2 binary formats) to recover ``(original_path, $R-content-path, deleted_at, file_size)``. ``TrashService._derive_os_trash_location`` now calls ``find_in_recycle_bin`` after ``send2trash`` returns; ``_restore_from_os_trash`` actually moves the ``$R`` file back to the target and cleans up the matching ``$I`` companion. ``restore --apply`` now succeeds end-to-end on Windows. Tests: 8 parser tests with synthetic ``$I`` bytes (v1 + v2 + unicode + malformed), 1 live Windows test that actually round-trips a real file through the bin, 2 service-level Q14 tests, 1 CLI round-trip test. The Phase Alpha ``RestoreImpossibleError`` contract is preserved on non-Windows. **Zero open implementation questions remain.** **149 passing, 1 correctly-skipped, 0 xfailed, 0 failures, 0 warnings.**
- **2026-05-06 v0.13** — **Phase Beta gate #1 starts: ``FuzzyIndex`` shipped.** New module ``src/curator/services/fuzzy_index.py`` provides MinHash-LSH candidate selection for NEAR_DUPLICATE detection. The class wraps ``datasketch.MinHashLSH`` over MinHash signatures built from 3-grams of the ssdeep hash body (skipping the bimodal block-size prefix). Default threshold 0.5 ≈ ssdeep ``compare()`` >= 70 (calibration table in design doc). ``add`` is idempotent; ``remove`` is silent no-op on unknown ids; ``query`` validates input eagerly even on empty index (no silent acceptance of malformed hashes). ``datasketch`` lazy-imports inside the module so ``import curator`` still works without it; added to ``[beta]`` extras in ``pyproject.toml``. Design doc: ``docs/PHASE_BETA_LSH.md``. Tests: 25 unit tests at ``tests/unit/test_fuzzy_index.py`` covering parsing helpers, n-gram construction, empty-index behavior, add/query/remove/clear, idempotency, threshold tuning, input validation, and container protocol. Module-level ``pytest.importorskip("datasketch")`` makes the suite degrade cleanly when the optional dep is absent. **v0.14 wires ``FuzzyIndex`` into ``LineageService.find_candidates``; v0.15 benchmarks.** **174 passing, 1 correctly-skipped, 0 xfailed, 0 failures.**
- **2026-05-06 v0.14** — **Phase Beta v0.14: ``FuzzyIndex`` wired into ``LineageService``.** ``LineageService.__init__`` now accepts an optional ``fuzzy_index: FuzzyIndex | None``. When set and non-empty, ``_find_candidates`` routes the fuzzy candidate path through ``FuzzyIndex.query`` (O(1) average) instead of the O(n) ``find_with_fuzzy_hash`` DB scan. The service self-maintains the index: every file processed by ``compute_for_file`` is added afterwards so subsequent calls find it via LSH. ``CuratorRuntime.build_runtime`` instantiates a populated ``FuzzyIndex`` when ``datasketch`` is installed (silent skip when not, preserving Phase Alpha behavior). Pre-population walks the DB once at startup; new files added during a scan flow through the self-maintenance path. Tests: 4 new integration tests at ``tests/integration/test_lineage_lsh_equivalence.py`` — the headline ``test_lsh_path_matches_baseline`` runs the same controlled corpus through both paths and asserts the edge set is identical (the v0.14 correctness contract). Plus a fallback test (no fuzzy_index regresses to Phase Alpha behavior), a self-maintenance test, and a runtime-wiring test. **v0.15 — ``tests/perf/`` infrastructure with documented improvement factor at 100/1k/10k synthetic files.** **178 passing, 1 correctly-skipped, 0 xfailed, 0 failures.**
- **2026-05-06 v0.15** — **Phase Beta gate #1 COMPLETE: LSH benchmark proves the optimization pays off.** New ``tests/perf/`` directory with ``test_lineage_throughput.py`` benchmark. End-to-end timing of the (candidate-select → ``ppdeep.compare``) pipeline on synthetic corpora at 100, 1k, 10k entries; both baseline (O(n)) and LSH paths run on the same fixed query, results compared for correctness AND speed. **Speedups: 1.6x at 100 files, 21.5x at 1k, 196.7x at 10k.** LSH query latency stays ~2ms regardless of N (constant-time bucket lookup); baseline scales linearly. Index build cost ≈ 2.4ms per entry (≈ 23s for 10k files at startup; Phase Gamma persistent backend will amortize this). 4 benchmark tests: 3 parametrized corpus sizes + 1 build-scaling diagnostic. Per-run JSON results saved under ``tests/perf/results/`` for historical tracking. ``pyproject.toml`` updated: ``addopts = "-ra --strict-markers --ignore=tests/perf -m 'not slow'"`` so benchmarks are opt-in via ``pytest tests/perf -m slow -v`` and don't slow down the default invocation. Default suite: **178 passing, 1 correctly-skipped, 24.6s** (faster than before perf tests existed). Opt-in suite: **178 + 4 = 182 passing total.**
- **2026-05-06 v0.16** — **Phase Beta gate #3 v0.16: file watcher (Tier 6) SHIPPED.** New ``src/curator/services/watch.py`` introduces a long-running ``WatchService`` that wraps ``watchfiles.watch()`` and yields typed ``PathChange`` events for enabled local sources. Includes a per-(path, kind) ``_Debouncer`` (1s default window; ``DELETED`` events bypass it), a default ignore-pattern set covering ``.git``, ``__pycache__``, ``*.pyc``, vim/emacs/JetBrains temp files, and OS-metadata noise (``.DS_Store``, ``Thumbs.db``). Lazy-imports ``watchfiles`` so ``import curator`` still works without it; ``watchfiles>=0.21`` added to ``[beta]`` extras. New ``curator watch [SOURCE]`` CLI command with ``--json`` JSON-lines output and ``--debounce-ms`` tuning; blocks until Ctrl+C. CLI command help renders cleanly; ``--json`` mode produces one JSON event per line for piping to other processes. Tests: 22 unit tests at ``tests/unit/test_watch.py`` (PathChange dataclass + Debouncer semantics + ignore-pattern matching, no ``watchfiles`` dep needed) + 6 real-filesystem smoke tests at ``tests/integration/test_watch_smoke.py`` marked ``@pytest.mark.slow`` covering ADDED on new file, MODIFIED on existing file, ignored-pattern filtering with mixed real/ignored creation, error paths (NoLocalSourcesError when no sources, disabled sources skipped). Smoke tests use a producer thread + ``threading.Event`` stop signal to terminate cleanly within 5s deadlines. Design doc: ``docs/PHASE_BETA_WATCH.md`` with cut-off plan v0.16 → v0.17 (wire to ``ScanService.scan_paths``) → v0.18 (examples + README). **v0.17 will integrate events into ``ScanService`` for incremental scanning.** Default suite: **200 passing, 1 correctly-skipped, 17.8s.** Opt-in suite (slow + perf): **210 total passing.**
- **2026-05-06 v0.17** — **Phase Beta gate #3 v0.17: incremental scanning wired up.** New ``ScanService.scan_paths(source_id, paths)`` method processes a specific list of file paths instead of walking a root — same hash + classification + lineage pipeline as a full scan, just different enumeration. Three behaviors per path: existing file → ``_upsert_from_info`` updates mutable fields and invalidates hashes if mtime/size changed; new file → inserted with fresh ``curator_id``; vanished file → ``_mark_path_deleted_if_known`` soft-deletes the FileEntity (no OS-trash interaction; that path goes through ``TrashService.send_to_trash``). Defensive: dedupes repeated paths in one call, skips directories silently, treats vanished-but-not-indexed as a no-op. New ``ScanReport.files_deleted`` counter. ScanJob rows for incremental scans use synthetic root marker ``<paths:N>`` so audit queries can distinguish them from full scans. Audit entries tagged ``kind="incremental"``. ``curator watch`` CLI gains a ``--apply`` flag: each watcher event triggers a single-path ``scan_paths`` call, with a one-line summary of what changed (new=N, updated=N, deleted=N, edges=N). Errors during scan_paths are logged but don't terminate the watch loop. Tests: 12 integration tests at ``tests/integration/test_scan_paths.py`` covering added / modified (with mtime sleep)/ unchanged (cache hit) / deleted / unknown-vanished / idempotent-re-delete / mixed batches / directory skip / dedup / audit + job row writes. Default suite: **212 passing, 1 correctly-skipped, 23.6s.** Opt-in suite (slow + perf): **222 total passing.**
- **2026-05-06 v0.18** — **Triple deliverable: gate #3 polish + gate #2 cross-platform + gate #5 gdrive scaffolding.** Three gates touched in one pass:
  * **Gate #3 polish:** ``examples/watch_demo.py`` runnable demo (pure-Python, copy-pasteable starting point for users embedding reactive scanning); README section on reactive scanning with `curator watch` examples; README "Optional features" section documenting the [beta]/[cloud]/[dev] extras. Gate #3 now fully complete v0.16+v0.17+v0.18.
  * **Gate #2 (cross-platform send2trash):** New ``src/curator/_vendored/send2trash/mac/`` (AppleScript via ``osascript``, no pyobjc dep — same Finder behavior, slight per-file overhead from process startup) + new ``plat_freedesktop.py`` (XDG Trash spec 1.0 home trash: writes ``.trashinfo`` metadata + moves to ``$XDG_DATA_HOME/Trash/files/``, with name-collision disambiguation as ``<stem>.<N>.<suffix>`` per GNOME convention; cross-filesystem trash deferred to Phase Gamma). Platform dispatcher in ``__init__.py`` updated to route win32/darwin/linux/bsd/other. 20 unit tests at ``tests/unit/test_send2trash_cross_platform.py`` covering helpers (xdg_data_home, unique_trash_name, write_trashinfo, on_same_filesystem) + live freedesktop moves on a fake HOME (works on every host via monkeypatched sys.platform) + dispatcher backend selection + mac platform guard. Gate #2 SHIPPED.
  * **Gate #5 (Google Drive plugin scaffolding):** New ``src/curator/plugins/core/gdrive_source.py`` (~330 lines). Implements the source-plugin contract for ``gdrive`` and ``gdrive:<alias>`` source IDs. Hooks: ``register`` (returns None when PyDrive2 missing), ``enumerate`` (BFS over folder children with cycle detection for multi-parent folders), ``stat``, ``read_bytes``, ``delete`` (Drive's native trash). ``move`` raises NotImplementedError (Drive moves operate on parent IDs, not paths — Phase Gamma). Lazy-imports ``pydrive2`` inside hook bodies. Drive metadata → Curator types: handles both Drive API v2 (``title``, ``modifiedDate``, ``fileSize``) and v3 (``name``, ``modifiedTime``, ``size``) field naming; Google-native files (Docs/Sheets/etc.) get size=0 since the API doesn't expose byte counts for them. Dependency injection seam ``Plugin.set_drive_client(source_id, client)`` lets tests inject a fake Drive client without any OAuth setup. 27 unit tests at ``tests/unit/test_gdrive_source.py``: static contract (SOURCE_TYPE, _owns logic, mime-type constants) + datetime parsing (Z-suffix, missing, malformed) + Drive-metadata-to-Curator-types conversion + registration (returns None when pydrive2 unavailable, returns SourcePluginInfo when present) + enumerate (yields files / skips folders / handles self-referencing folders without infinite loop) + stat + read_bytes (with offset+length window) + delete (trash + permanent variants) + move (raises NotImplementedError for owned, returns None for non-owned). Plugin registered in ``register_core_plugins`` so it auto-loads when curator starts. **Live OAuth + real API calls explicitly deferred** — user must run PyDrive2's interactive auth flow separately to populate credentials.json before using.
  Default suite: **259 passing, 1 correctly-skipped, 24.5s.** Opt-in suite (slow + perf): **269 total passing.**
- **2026-05-06 v0.19** — **Phase Gamma Milestone Gamma-1: SafetyService SHIPPED.** First Phase Gamma deliverable; foundation for the smart drive organizer (F1). New ``src/curator/services/safety.py`` (~430 lines) introduces the safety primitives that gate every future organize action. Five concern types aggregated into a single :class:`SafetyReport`: ``OPEN_HANDLE`` (psutil-based scan of running processes, optional, slow), ``PROJECT_FILE`` (walks up looking for project markers — ``.git``, ``pyproject.toml``, ``package.json``, ``Cargo.toml``, ``Gemfile``, ``*.sln``, ``*.xcodeproj``, etc.; ~30 markers + 4 glob patterns), ``APP_DATA`` (prefix match against platform-specific app-data paths: Windows ``%APPDATA%``/``%LOCALAPPDATA%``/``%PROGRAMDATA%``/``Program Files``; macOS ``~/Library/Application Support``, ``~/Library/Caches``, ``~/Library/Containers``, ``/Library``; Linux ``~/.config``, ``~/.cache``, ``~/.local/share``, XDG paths), ``OS_MANAGED`` (hard-refusal paths: Windows ``%SystemRoot%`` + ``Boot``/``Recovery``/``$Recycle.Bin``/``System Volume Information``; macOS ``/System``/``/private``/``/Volumes``/``/usr``/``/sbin``/``/bin``/``/dev``; Linux ``/boot``/``/sys``/``/proc``/``/dev``/``/etc``/``/usr``/``/var``/``/sbin``/``/bin``/``/lib``), and ``SYMLINK``. Three-level verdict aggregation: SAFE → CAUTION → REFUSE, where REFUSE short-circuits other checks (you can't make an OS_MANAGED file safe). API: ``SafetyService.check_path(path, *, check_handles=False)`` returns a ``SafetyReport``; ``check_paths`` for batches; ``is_app_data`` / ``is_os_managed`` predicate helpers. SafetyService wired into ``CuratorRuntime`` so all CLI commands have access. New ``[organize]`` extras group in ``pyproject.toml`` containing ``psutil>=5.9``. New CLI subcommands: ``curator safety check <path> [--handles]`` (prints verdict + concerns + project root + holders) and ``curator safety paths`` (lists the platform's app-data + OS-managed registries). Both commands support ``--json`` for piping. Two real bugs caught + fixed during implementation: (1) ``Path("C:") / "Boot"`` produces ``"C:Boot"`` (drive-relative current dir) not ``"C:\Boot"`` — fixed by appending explicit ``os.sep``; (2) ``list(empty_list or default)`` returns the default because empty list is falsy — fixed with explicit ``is not None`` guard so callers can pass ``[]`` to disable a registry. Tests: 37 unit tests at ``tests/unit/test_safety.py`` covering ``find_project_root`` (no marker / git / pyproject / package.json / sln pattern / multi-level walk / max-depth cap / nonexistent / dir input), default path lists, ``_is_under`` prefix matching, ``find_handle_holders`` (psutil-unavailable / nonexistent / detects-self), ``SafetyReport`` aggregation (level lifting, refuse-then-caution stays refuse, etc.), full ``check_path`` matrix (SAFE / CAUTION-app-data / REFUSE-os-managed / REFUSE-short-circuits-other-checks / CAUTION-project / CAUTION-symlink / nonexistent / handles-flag-default-off / extra-paths-extend-defaults), ``check_paths`` batch, predicate helpers. 2 of these tests are opt-in via ``@pytest.mark.slow`` (the ones that iterate running processes — ~40s each on Windows). Plus 6 integration tests at ``tests/integration/test_cli_safety.py`` for the CLI commands using Typer's ``CliRunner``. Default suite: **302 passing, 2 correctly-skipped, 23.8s.** Opt-in suite: **310 total passing.**

- **2026-05-06 v0.20** -- **Phase Gamma v0.20: organize plan-mode preview SHIPPED.** First surface that USES SafetyService at scale. New `src/curator/services/organize.py` (~190 lines) introduces `OrganizeService.plan(source_id, root_prefix=None, check_handles=False, limit=None) -> OrganizePlan`. The plan partitions files into three buckets: `safe` (eligible to organize), `caution` (review needed; concerns broken down per kind), `refuse` (hard-blocked; OS-managed paths). Each `OrganizeBucket` aggregates count + total_size + per-concern file lists. Plan mode only -- no moves happen; v0.21+ adds stage / apply with F2 type-specific templates. Service operates on Curator's existing index -- users run `curator scan` first. Defensive: SafetyService failures route to REFUSE (conservative); FileRepository failures return empty plan with logged error rather than raising. New `curator organize <source>` CLI command with `--root <prefix>`, `--handles`, `--limit N`, `--show-files`. Both human-readable (Rich-styled) and JSON output. Wired into `CuratorRuntime`. Real-world demo: scanning `src/curator` (131 files, 868 KB) and running `curator organize local` correctly identifies all 131 as CAUTION (project_file) because the tree is under `pyproject.toml` -- nothing inside Curator's own source would be auto-moved. Tests: 21 unit at `tests/unit/test_organize.py` (mocked FileRepository + SafetyService; covers OrganizeBucket aggregation, OrganizePlan totals, plan() level routing / source_id filter / root_prefix / limit / check_handles propagation / safety-failure-routes-to-refuse / repo-failure-returns-empty / concern aggregation) + 5 flow integration at `tests/integration/test_organize_flow.py` (real scan -> organize round trip with controlled SafetyService config) + 5 CLI integration at `tests/integration/test_cli_organize.py` (unknown source / scan-then-organize / JSON / --show-files / --root). Default suite: **333 passing, 2 correctly-skipped, 27.3s.** Opt-in suite: **341 total passing.**
- **2026-05-06 v0.21** -- **Phase Gamma Milestone Gamma-2 starts: music tagging + destination template SHIPPED.** First type-specific organization pipeline (F2 from ROADMAP.md). New src/curator/services/music.py (~280 lines): AUDIO_EXTENSIONS frozenset (17 audio extensions: mp3, flac, m4a/m4b, aac, ogg/oga, opus, wav, wma, ape, alac, aiff/aif, dsf, dff); sanitize_path_component(s, fallback) doing illegal-char replacement (Windows-illegal chars + control chars 0x00-0x1f), whitespace collapse, leading/trailing dot+space strip, 200-char component cap (safe under Windows MAX_PATH), Windows reserved name handling (CON/PRN/AUX/NUL/COM1-9/LPT1-9 prefixed with underscore even with extension); MusicMetadata dataclass with ffective_artist (album_artist preferred for compilations), ffective_album, has_useful_tags; _to_str / _to_int helpers for fuzzy mutagen tag values (handles list-wrapped, '3/12' track-number, '2023-01-15' date->year); MusicService with is_audio_file, 
ead_tags (lazy-imports mutagen, returns None for non-audio / nonexistent / mutagen-unavailable / mutagen-returns-None / exception cases; uses easy=True for cross-format tag normalization), propose_destination (default template {artist}/{album}/{track:02d} - {title}{ext} with track-prefix dropped when track_number is None, falls back to filename stem when title missing, strict sanitization on all components). OrganizeService.plan extended with organize_type="music" + 	arget_root parameters; only proposes destinations for SAFE-bucket audio files. OrganizeBucket gains proposals: dict[curator_id, dest_path]; dd() accepts optional proposed_destination kwarg. CLI: curator organize <source> --type music --target <dir> extends existing command; validates --type requires --target and currently only music is supported (clean error messages otherwise). JSON + human renderer both updated to surface proposed destinations. [organize] extras now include mutagen>=1.47 + 	inytag>=2.0. Tests: 56 unit at 	ests/unit/test_music.py covering sanitize_path_component (8 cases) + _to_str (5) + _to_int (6) + MusicMetadata (6) + is_audio_file (parametrized 9 + 1) + propose_destination (8 cases including illegal-char sanitization, album_artist preference for compilations, extension lowercase) + read_tags (6 cases via mocked mutagen.File). 5 integration at 	ests/integration/test_organize_music.py covering --type-without-target errors / unknown-type errors / proposes-destinations-for-SAFE-audio (with mocked tag layer) / no-proposals-for-CAUTION-audio (project_file gates) / basic-organize-unaffected-by-music-addition. Default suite: **394 passing, 2 correctly-skipped, 32.8s.** Opt-in suite: **402 total passing.**
- **2026-05-06 v0.22** -- **Phase Gamma v0.22: Stage mode + organize-revert SHIPPED.** First Curator command that actually moves files. New types in src/curator/services/organize.py: STAGE_MANIFEST_NAME constant (.curator_stage_manifest.json), OrganizePlan.target_root field (recorded when organize_type was passed to plan() so stage knows where proposals are rooted), StageOutcome enum (MOVED / SKIPPED_NO_PROPOSAL / SKIPPED_COLLISION / FAILED), StageMove dataclass (curator_id, original, staged, outcome, error), StageReport (stage_root, started_at/completed_at, moves[], moved_count/skipped_count/failed_count properties, duration_seconds), parallel RevertOutcome (RESTORED / SKIPPED_ORIGINAL_OCCUPIED / SKIPPED_STAGED_MISSING / FAILED), RevertMove, RevertReport. OrganizeService.__init__ accepts optional udit: AuditRepository; runtime wires udit_repo from existing storage. OrganizeService.stage(plan, *, stage_root) -> StageReport requires plan.target_root (else ValueError); for each SAFE file with a proposal, computes stage_path = stage_root / proposal.relative_to(target_root), mkdirs parents, shutil.moves the file, audits each successful move with ctor="curator.organize.stage" ction="organize.stage.move" ntity_type="file", then writes/merges the manifest at <stage_root>/.curator_stage_manifest.json (JSON list of {curator_id, original, staged, moved_at}; pre-existing manifest is read + appended to so reruns dont lose history). OrganizeService.revert_stage(stage_root) -> RevertReport reads the manifest (raises FileNotFoundError if missing), for each entry: skips with SKIPPED_STAGED_MISSING if staged file is gone, skips with SKIPPED_ORIGINAL_OCCUPIED if original path now has something at it, otherwise shutil.move back + audit (ctor="curator.organize.revert" ction="organize.revert.move"). Manifest is updated in place: successfully restored entries removed, file deleted entirely when empty, kept with unresolved entries when partial. CLI: --stage <dir> flag added to curator organize (validates --stage requires --type and --target); new curator organize-revert <stage_dir> command (handles FileNotFoundError cleanly with exit code 1, RuntimeError with exit code 2). Both human-readable (Rich-styled with stage summary line + skip/failure breakdown + revert hint) and JSON output modes. Real-world end-to-end demo: 2 mock-tagged audio files staged into staging/AC_DC/Back in Black/01 - Hells Bells.mp3 + staging/Pink Floyd/The Wall/06 - Comfortably Numb.mp3 (note slash sanitization on AC/DC), manifest written with both entries, originals removed; curator organize-revert staging restored both files to source paths and deleted the manifest cleanly. **Real bug surfaced + fixed during the demo:** Unicode arrow characters (left arrow / right arrow, U+2190 and U+2192) crashed the Rich renderer on Windows cp1252 consoles with UnicodeEncodeError; replaced with ASCII -> and <- in stage + revert + plan renderers. (Tests didnt catch it because Typers CliRunner uses utf-8 buffer, not the legacy Windows console encoding.) Tests: 20 unit at 	ests/unit/test_organize_stage.py covering StageOutcome enum stable values + StageMove/StageReport dataclass + count properties + duration tracking + RevertOutcome / RevertMove / RevertReport parallel suite + stage() with no-target-root-raises / moves-safe-with-proposal / writes-manifest / safe-without-proposal-skipped / collision-skipped / shutil-failure-recorded / audit-entry-per-move / no-audit-when-None + 
evert_stage() with missing-manifest-raises / full-round-trip-restores-and-deletes-manifest / skipped-when-original-occupied / skipped-when-staged-missing / partial-revert-leaves-remaining-entries / revert-writes-audit-entries. 5 CLI integration at 	ests/integration/test_cli_stage.py covering --stage without --type errors / --stage without --target errors / full stage+revert CLI round trip with manifest verification / unknown stage-dir errors cleanly / human render shows stage summary. Default suite: **419 passing, 2 correctly-skipped, 28.9s.** Opt-in suite: **427 total passing.**
- **2026-05-06 v0.23** -- **Phase Gamma v0.23: Apply mode SHIPPED -- F2 closed end-to-end.** New OrganizeService.apply(plan) -> StageReport is a thin wrapper that calls stage(plan, stage_root=plan.target_root, mode="apply") -- files move directly to their final destinations under plan.target_root instead of a transient staging tree. Backwards-compat: stage() gained an optional mode: str = "stage" parameter that controls only the audit actor/action strings (stage -> organize.stage.move, apply -> organize.apply.move). The on-disk effect is identical between the two; what differs is audit semantics so consumers can tell a final apply apart from a preview stage. Manifest format unchanged so curator organize-revert <target> works to undo an apply just as it works for a stage (the manifest filename keeps its v0.22 .curator_stage_manifest.json name for backward compat with existing manifests on disk). CLI: new --apply boolean flag on curator organize. Validation: --apply requires --type and --target; --apply and --stage are mutually exclusive (clean error messages with exit code 2 in all error paths). Renderer differentiates: section heading says "Apply" vs "Stage"; trailing hint after a successful apply says "Files moved to their final destinations" with the revert command suggestion (apply mode), vs "Review the staged tree, then either: move ... or revert" (stage mode). JSON output includes a mode field under stage so programmatic consumers can branch behavior. Real end-to-end demo: 3 mock-tagged files (Pink Floyd / The Wall / 06 - Comfortably Numb, AC/DC / Back in Black / 01 - Hells Bells with slash sanitized to AC_DC, Various Artists / Best of 80s / 12 - Take On Me using album_artist for the compilation) moved directly into library/ tree; curator audit --action organize.apply.move confirmed all 3 audit entries had the apply action (not stage); curator organize-revert library/ restored all 3 to source paths and cleaned the manifest -- library directory empty after revert. Tests: 12 at 	ests/unit/test_organize_apply.py: TestApply (raises-without-target / moves-to-target-root-not-separate-dir / writes-manifest-at-target-root / audit-action-is-apply-not-stage with verification of details["mode"]=="apply" / apply-then-revert-round-trip), TestStageWithApplyMode (mode-param-changes-audit-actor / default-mode-is-stage-for-backward-compat verifying v0.22 callers still get stage semantics), TestApplyCli (--apply-without-type errors / --apply-without-target errors / --apply-and-stage-mutually-exclusive / full apply+revert round trip with JSON mode field assertion / renderer-says-apply-not-stage with "final destinations" hint check). Default suite: **431 passing, 2 correctly-skipped, 29.8s.** Opt-in suite: **439 total passing.** **Phase Gamma Milestone Gamma-2 (Music) is now feature-complete for the local case** (plan + propose-destinations + stage + apply + revert all working end-to-end). Remaining Gamma-2 stretch goals: MusicBrainz fallback for files with empty tags + AcoustID fingerprinting -- both deferrable.
- **2026-05-06 v0.25** -- **Phase Gamma Milestone Gamma-4: Cleanup utilities SHIPPED. F6 closed.** New src/curator/services/cleanup.py (~430 lines) introduces CleanupService with three independent sub-pipelines sharing a common CleanupReport -> pply() -> ApplyReport shape. Module-level constants: DEFAULT_JUNK_PATTERNS (15 fnmatch globs covering Windows: Thumbs.db / ehthumbs.db / ehthumbs_vista.db / desktop.ini, macOS: .DS_Store / ._* AppleDouble sidecars / .AppleDouble / .LSOverride / .Spotlight-V100 / .Trashes, Office lock files: ~$*, temp files: *.tmp / *.temp / *.bak / *~, Linux: .Trash-* / .nfs*); SYSTEM_JUNK_NAMES (smaller set of files that count as no-content for empty-dir detection). Types: CleanupKind enum (EMPTY_DIR / BROKEN_SYMLINK / JUNK_FILE), CleanupFinding dataclass (path, kind, size, details), CleanupReport (kind, root, findings, errors, started_at/completed_at, count/total_size/duration_seconds properties), ApplyOutcome enum (DELETED / SKIPPED_REFUSE / SKIPPED_MISSING / FAILED), ApplyResult (finding, outcome, error), ApplyReport (parallel structure with deleted_count/skipped_count/failed_count). CleanupService.find_empty_dirs(root, ignore_system_junk=True) walks bottom-up (so parent dirs containing only soon-to-be-empty children are also flagged), excludes the root itself from deletion candidates, distinguishes meaningful files from system-junk files via SYSTEM_JUNK_NAMES set, records which junk files are present in details["system_junk_present"] so apply can unlink them before rmdir. ind_broken_symlinks(root) walks via os.walk(followlinks=False), checks both files AND subdirs (a dir-symlink to a deleted dir is also broken), uses Path.is_symlink() and not Path.exists() for the broken-detection (catches Windows junctions too), records readlink target in details where readable. ind_junk_files(root, patterns=None) walks via os.walk, applies fnmatch-style globs to each filename basename (not full path), records matched_pattern + size per finding. pply(report, use_trash=True) strategy-picks deletion: EMPTY_DIR -> unlinks any system-junk files present in finding details, then Path.rmdir() (refuses non-empty by definition); BROKEN_SYMLINK -> Path.unlink() (removes link, not nonexistent target); JUNK_FILE -> vendored send2trash if available + use_trash=True (recoverable via OS Recycle Bin / Trash), falls back to Path.unlink() on send2trash failure or use_trash=False. **SafetyService consulted on every apply target** -- REFUSE-tier paths (under OS_MANAGED registries: Windows %SystemRoot%, /Boot/, macOS /System, etc.) skipped with concern_summary formatted as comma-joined concern_kind: detail pairs. Per-finding outcomes (DELETED / SKIPPED_REFUSE / SKIPPED_MISSING / FAILED) so partial outcomes are reported clearly. SKIPPED_MISSING handles the race where plan finds a file but user removes it before apply runs. Audit: every successful deletion writes ctor=curator.cleanup, ction=cleanup.{kind}.delete, ntity_type=path, ntity_id=path, details={kind, size, ..finding details, use_trash}. Defensive: per-finding safety check failures fall through to SKIPPED_REFUSE rather than risking an unsafe deletion. CLI: new curator cleanup Typer subcommand group with three subcommands -- cleanup empty-dirs <root> [--apply] [--strict], cleanup broken-symlinks <root> [--apply], cleanup junk <root> [--apply] [--no-trash]. Plan-only by default for all three. JSON output via --json includes both plan and (when --apply) pply sections. Human renderer caps finding lists at 20 with overflow message, formats junk-file output with matched_pattern + size, formats broken-symlink output with target arrow, formats empty-dir output with system_junk_present list. Bug caught at compile time: initial code used a erdict.summary() method that doesnt exist on SafetyReport -- fixed by formatting concerns inline as comma-joined pairs. Real-world demo against a 9-file messy tree (7 junk files distributed across 4 dirs: Thumbs.db x2 / .DS_Store / ~.md / meeting.md.bak / export.tmp / ._photo.jpg, plus always-empty empty_leaf/ and old_export/ which becomes empty after its export.tmp is removed): junk plan found all 7, empty-dirs plan found 1 (cascade detection runs on the live filesystem each time, so the second find_empty_dirs after junk apply correctly identified 2). After both applies: 9 audit entries (7 junk + 2 empty_dir); the 2 real content files (meeting.md, photo.jpg) and their containing dirs (notes/, pic_dump/, pic_dump/subfolder/) preserved untouched. Tests: 31 unit at 	ests/unit/test_cleanup.py: TestFindEmptyDirs (9 cases including nonexistent / not-dir / empty / Thumbs.db handling both modes / cascade / root-not-flagged / SYSTEM_JUNK_NAMES constant), TestFindBrokenSymlinks (4 cases, 2 skip without symlink permission), TestFindJunkFiles (10 cases including each major pattern type + custom override + size recording + subdir walk), TestApply (8 cases including all three kinds + safety-REFUSE-skips + missing-target-recorded + audit-logged-per-deletion + no-audit-when-None + failed-delete-recorded). 7 CLI integration at 	ests/integration/test_cli_cleanup.py: empty-dirs plan-only / empty-dirs --apply / empty-dirs --strict / junk plan / junk --apply --no-trash / junk safety-REFUSE-preserved / cleanup --help lists subcommands; 2 broken-symlinks tests skip without symlink permission. Total: **518 passing, 7 correctly-skipped (5 new symlink + 2 prior), 32.5s.** Opt-in suite: **526 total passing.** **F6 (Cleanup) is now feature-complete.** Stretch goals: dedup-aware cleanup (find files with the same content hash and offer to keep just one), scheduled cleanup (cron-style runs via the watcher infrastructure), more sophisticated junk detection (XDG-compliant cache cleanup, browser cache directories). Currently CleanupService is filesystem-only and doesnt update Curators index -- a deleted junk file thats in the index will appear as a phantom until next curator scan. v0.26+ may sync.
- **2026-05-06 v0.26** -- **Phase Gamma Milestone Gamma-5: Documents via PDF/DOCX metadata SHIPPED. F4 closed.** Third type-specific organization pipeline (after F2 music + F3 photos). New src/curator/services/document.py (~340 lines): DOCUMENT_EXTENSIONS frozenset (24 extensions covering PDF/DOC/DOCX/ODT/RTF/TXT/MD/RST text formats, EPUB/MOBI/AZW e-books, XLS/XLSX/ODS/CSV/TSV spreadsheets, PPT/PPTX/ODP/KEY presentations, PAGES/NUMBERS Apple iWork). Five-pattern filename date matcher in _FILENAME_DATE_PATTERNS (regex tuple ordered by specificity): ISO with separators (2024-03-15, 2024_03_15, 2024.03.15), compact YYYYMMDD (with word boundaries to avoid matching version numbers), year-month (2024-03), year-only (bounded (?:19|20)\d{2} so version numbers like 1234567890 dont accidentally match a 4-digit subset). _parse_filename_date returns (datetime, granularity) tuple where granularity is ymd/ym/y; defaults month=1 day=1 for coarser granularities; rejects out-of-range months/days; year-bounds 1900-2099 reject pre-photography and far-future false positives. _parse_pdf_datetime handles PDF spec format D:YYYYMMDDHHmmSSOHH'mm' with optional D: prefix and timezone suffix; clamps invalid hour/minute/second components rather than rejecting (preserves date portion). DocumentMetadata dataclass (created_at, created_at_source, title, author, raw exif dict; year_str + year_month_str properties for template substitution). DocumentService.read_metadata runs four-step fallback chain in priority order: (1) format-specific embedded metadata -- PDF via lazy-imported pypdf reading /CreationDate then /ModDate, OOXML (DOCX/XLSX/PPTX) via stdlib zipfile + xml.etree reading docProps/core.xml for Dublin Core dcterms:created then dcterms:modified (NO python-docx dep -- just core.xml extraction); (2) filename pattern; (3) mtime; (4) None placeholder. **Filename pattern beats mtime intentionally** -- a downloaded PDFs mtime is when you downloaded it, not when it was authored, so 
otes_2022-06-15.txt with a 2025 mtime should organize as 2022. propose_destination uses default template {year}/{year_month}/{filename} (year + year-month sub-folder + original filename preserved); year-month is coarser than photos year-month-day because documents typically aggregate by month not day. Filename preserved unchanged (no retitling) since document filenames usually encode meaningful identifying info (Invoice numbers, project names, dates). Reuses MusicService.sanitize_path_component for filesystem-safe components. OrganizeService.__init__ extended with optional document: DocumentService | None = None parameter (default-constructs if not supplied). OrganizeService.plan extended: when organize_type == "document" and file is document + SAFE, calls DocumentService and proposes destination (no has_useful_* gate -- mtime fallback always produces a usable destination, like photos). CLI: --type validation extended from {music, photo} to {music, photo, document} with updated error message; help text mentions all three pipelines and their respective destination shapes. Runtime wires DocumentService into CuratorRuntime alongside MusicService + PhotoService. Real-world end-to-end demo with 6 mixed documents covering ALL FOUR date sources: Invoice 4823.pdf (PDF /CreationDate D:20240315143000 -> 2024/2024-03/Invoice 4823.pdf), Tax Return 2023.pdf (PDF /CreationDate -> 2024/2024-04/Tax Return 2023.pdf), Q1 Report.docx (DOCX dcterms:created 2024-01-31T17:00:00Z -> 2024/2024-01/Q1 Report.docx), Meeting Minutes.docx (DOCX dcterms:created -> 2023/2023-09/), 
otes_2022-06-15_brainstorm.txt (filename pattern beat its 2025 mtime to land in 2022/2022-06/ -- proves priority order), random_thoughts.md (no embedded metadata, no filename date, mtime fallback -> 2020/2020-12/). 6 audit entries (organize.apply.move). Tests: 58 unit at 	ests/unit/test_document.py covering _parse_filename_date (12 cases including ISO/compact/year-month/year-only + year-bounds + invalid month/day handling + version-number rejection + first-match-wins), _parse_pdf_datetime (8 cases including canonical / D-prefix / partial dates / timezone handling / invalid clamping / out-of-range / None / garbage), DocumentMetadata (3 cases), is_document_file (parametrized over 13 cases), propose_destination (5 cases including unknown-date / illegal-chars-sanitized), 
ead_metadata (12 cases including non-document / nonexistent / PDF-creation-date / PDF-mod-date-fallback / PDF-no-metadata-falls-to-filename / DOCX-dcterms-created / DOCX-modified-fallback / XLSX-uses-same-OOXML-path / text-file-filename-date-beats-mtime / text-file-no-date-uses-mtime / corrupt-PDF-graceful / corrupt-DOCX-graceful / pypdf-unavailable-falls-back). Real PDF fixtures built via pypdf in-process; real DOCX fixtures built via stdlib zipfile + crafted core.xml -- no bundled binary blobs in repo. 5 CLI integration at 	ests/integration/test_organize_document.py: --help mentions document / unknown-type lists all three / plan with PDF + DOCX + filename-dated text / full apply round trip / text-file-mtime-fallback in CLI flow. Default suite: **581 passing, 7 correctly-skipped, 37.4s.** Opt-in suite: **589 total passing.** pypdf added to [organize] extras (>=4.0). **F4 (Documents) is now feature-complete for the local case.** Stretch goals: EPUB/MOBI metadata via separate libraries, RTF/ODT date extraction, classification-aware sub-folders (Invoices/Reports/Letters/) using ClassificationService.
- **2026-05-07 v0.28** -- **Phase Gamma Milestone Gamma-6 (F7 dedup-aware cleanup) SHIPPED.** Fourth pipeline added to existing CleanupService, leveraging Curators xxhash3_128 hash index from Phase Alpha. No filesystem walk, no rehashing -- pure SQL query against the existing iles table plus a Python-side groupby. Source changes in src/curator/services/cleanup.py: module docstring updated to describe four pipelines instead of three; new imports for FileEntity from curator.models.file, FileQuery from curator.storage.queries, FileRepository from curator.storage.repositories.file_repo; new CleanupKind.DUPLICATE_FILE = "duplicate_file" enum value; new module constant KEEP_STRATEGIES = ("shortest_path", "longest_path", "oldest", "newest") exported in __all__; CleanupService.__init__ gains optional ile_repo: FileRepository | None = None parameter. New method ind_duplicates(*, source_id=None, root_prefix=None, keep_strategy="shortest_path", keep_under=None) -> CleanupReport raises RuntimeError if file_repo not provided + ValueError on invalid strategy; queries via FileQuery(has_xxhash=True, source_ids=[source_id] if source_id else None, source_path_starts_with=root_prefix, deleted=False, order_by="source_path ASC"); groups by xxhash3_128; for each group with len > 1 picks keeper via _pick_keeper helper; emits one CleanupFinding per non-keeper with details={kept_path, kept_reason, dupset_id, hash, mtime, source_id}. Static _pick_keeper(group, *, keep_strategy, keep_under) returns (keeper, reason): when keep_under is set + at least one file matches the prefix, candidates narrow to those + reason becomes "<strategy> + keep_under"; otherwise the strategy applies to the whole group with reason just the strategy name. Strategy implementations use min/max with composite keys: shortest_path uses (len, source_path), oldest uses (mtime, source_path), etc. -- second key acts as deterministic tiebreaker for equal-length / same-mtime cases. _delete_one extended: a single elif branch matches both JUNK_FILE and DUPLICATE_FILE to share the send2trash-with-permadelete-fallback code path. Comment notes that trash mode is especially valuable for duplicates because if dedup picks the wrong keeper user can recover from Recycle Bin. Runtime wiring in src/curator/cli/runtime.py: cleanup = CleanupService(safety, audit=audit_repo, file_repo=file_repo) -- single-line addition makes dedup work in CLI without test plumbing. CLI in src/curator/cli/main.py: new _render_duplicate_report(rt, report, fmt_size) helper groups findings by dupset_id, prints "Set N: kept <path> (<reason>)" headers followed by indented duplicate lines with sizes, caps at 20 visible groups with overflow message; _render_cleanup_report dispatches DUPLICATE_FILE to it before the normal flat rendering. New @cleanup_app.command("duplicates") Typer subcommand: --source SOURCE (e.g. "local"), --root PREFIX for path prefix narrowing, --keep-strategy STRATEGY (default shortest_path), --keep-under PATH for prefix-preferred keepers, --apply (default plan-only), --no-trash (with help text marking it NOT RECOMMENDED for duplicates since trash is the safety net); reuses the shared _run_cleanup driver. Real-world end-to-end demo with 10-file tree (after the curator.db file): 3 copies of song.mp3 (200 B each, identical content) across Library/Music/, Downloads/, Backup/Old/2023/Music/; 2 copies of beach.jpg (450 B each) across Library/Photos/, Downloads/beach copy.jpg; 2 unique files (unique.mp3, thing.txt). Plan output: 2 duplicate groups, 3 files freeable, 850 B; Set 1 kept Downloads/song.mp3 (18 chars beats Library/Music/song.mp3 at 22 and Backup path at 30); Set 2 had a same-length tie between Downloads/beach copy.jpg (24 chars) and Library/Photos/beach.jpg (24 chars) -- alphabetic source_path tiebreaker picked Downloads first, which is correct deterministic behavior. After --apply --no-trash: deleted=3 skipped=0 failed=0 in 0.08s; 4 files preserved (2 keepers + 2 unique files); 3 audit entries with action=cleanup.duplicate_file.delete and details including kept_path + dupset_id. Tests: 18 unit at 	ests/unit/test_cleanup_duplicates.py -- 4 guard rails (no-file_repo-raises, invalid-strategy-raises, KEEP_STRATEGIES constant lists all four, empty-index-returns-empty), 4 keep-strategy variants on a two-file group (shortest_path default beats deeper, longest_path flips, oldest picks earlier mtime, newest picks later), 3 keep_under composability tests (pure-prefix-wins beats shortest_path, no-match-falls-through to strategy, prefix+strategy-tiebreaker correctly narrows to prefix-matching files then applies oldest), 1 three-file group test (two duplicates flagged, one keeper, all share dupset_id), 2 filter narrowing tests (root_prefix narrows to one duplicate set, source_id narrows by source), 3 apply tests (apply-deletes-duplicates-keeps-keeper, apply-audit-records-kind=duplicate_file with kept_path + dupset_id in details, safety-REFUSE-skips-duplicate so file is preserved). 6 CLI integration at 	ests/integration/test_cli_cleanup_duplicates.py -- subcommand-listed-in-cleanup-help, duplicates-help-lists-strategies, plan-finds-duplicates-after-real-scan (real curator scan + cleanup duplicates plan finds 3-file group as 2 findings), apply-removes-only-duplicates-keeps-keeper (real round-trip), keep-under-flips-keeper (without flag Downloads wins by shortest_path; with --keep-under under Library, the deeper Library file wins), no-duplicates-returns-clean-plan (3 unique files, count=0). Mid-build catches: (1) tests initially imported Database from curator.storage.database -- the real module is CuratorDB from curator.storage; (2) SourceRepository has insert(SourceConfig) not upsert(**kwargs) -- needed to construct SourceConfig dataclass and call insert; (3) one test assertion miscounted path lengths (had asserted Downloads/song.mp3 wins over Music/song.mp3, but Music/song.mp3 is 18 chars vs Downloads/song.mp3 21 -- shorter wins by definition of the strategy); (4) Filesystem MCP timed out mid-edit but the edit had succeeded server-side, verified via Select-String. Default suite: **642 passing, 7 correctly-skipped, 41.3s.** Opt-in suite: **650 total passing.** **F7 (Dedup-aware cleanup) is now feature-complete.** Stretch goals: fuzzy-hash duplicate detection (LSH-based near-duplicates with a confidence threshold for re-encoded images / re-compressed audio that arent bit-identical); index-sync for cleanup (close v0.25 phantom-file gap by marking deleted files in file_repo when cleanup removes them); --interactive mode that prompts per-set Y/N before deletion.
- **2026-05-07 v0.29** -- **Phase Gamma F8 (index sync for cleanup) SHIPPED.** Closes the phantom-file gap left open in v0.25 (cleanup) and v0.28 (dedup). Source change: new private method _mark_index_deleted(finding) on CleanupService invoked from pply() after every successful deletion (out.results.append(... DELETED)) and BEFORE the audit log call. Best-effort throughout -- guarded by null checks and try/except so it NEVER raises and NEVER fails the parent apply pass. Skip conditions documented in the docstring: (a) self.file_repo is None (legacy callers); (b) inding.kind == CleanupKind.EMPTY_DIR (directories arent FileEntity rows); (c) ind_by_path raises (DB unreachable etc.); (d) ind_by_path returns None (file wasnt indexed -- common for Thumbs.db etc. that classification filters out); (e) mark_deleted raises (DB locked etc.). Source resolution: for DUPLICATE_FILE findings the source_id is taken from inding.details["source_id"] (set by ind_duplicates in v0.28); for other kinds (junk, broken_symlink) default to "local" since thats the only source whose source_path is an absolute filesystem path. The pply() method itself only added a single 4-line block (# v0.29: comment + the self._mark_index_deleted(finding) call) so the change is minimally invasive to the existing apply flow. **Real-world end-to-end demo with 2-file dup pair**: curator scan local <dir> indexed both files (count=6 active including DB internals); first cleanup duplicates plan returned count=1; --apply --no-trash removed the duplicate from disk AND soft-deleted its FileEntity (active count dropped to 5; total count incl deleted stayed at 6; deleted_at timestamp set on the removed file); second cleanup duplicates plan returned **count=0** (in v0.28 it would have returned count=1 because the FileEntity for the just-deleted file would have been a phantom in the index). Tests: 8 unit at 	ests/unit/test_cleanup_index_sync.py: (1) 	est_apply_marks_duplicate_deleted_keeps_keeper -- duplicate FileEntity gets deleted_at set, keeper FileEntity unchanged; (2) 	est_subsequent_find_duplicates_does_not_re_flag -- the headline regression test that proves the gap is closed; (3) 	est_junk_apply_marks_indexed_file_deleted -- when an indexed Thumbs.db is junk-cleaned, its FileEntity is soft-deleted; (4) 	est_junk_apply_unindexed_file_no_error -- the common case where the junk file was never scanned; cleanup succeeds and index sync silently skips; (5) 	est_empty_dir_apply_does_not_query_index -- uses MagicMock(spec=FileRepository) and asserts ind_by_path.call_count == 0 AND mark_deleted.call_count == 0 after empty-dir apply (directories are explicitly short-circuited); (6) 	est_apply_works_with_no_file_repo -- null-safety for legacy callers; (7) 	est_mark_deleted_exception_does_not_fail_apply -- when mark_deleted raises RuntimeError, apply still reports DELETED and the file is gone from disk (best-effort confirmation; verified that mark_deleted.call_count == 1 proving the exception path was actually exercised); (8) 	est_find_by_path_exception_does_not_fail_apply -- same idea but failure at lookup time. **All four cleanup pipelines (junk + empty_dir + broken_symlink + duplicate_file) now keep the Curator index consistent automatically; no manual curator scan re-run needed after cleanup.** Default suite: **650 passing, 7 correctly-skipped, 35.5s.** Opt-in suite: **658 total passing.** **F8 (index sync) is now feature-complete.** Stretch: extend index sync to OrganizeService.apply (when files are MOVED their source_path should update in the index; currently a moved file would appear as a phantom at its old location until next scan).
- **2026-05-07 v0.30** -- **Phase Gamma F9 (fuzzy near-duplicate detection via LSH) SHIPPED.** Adds a match_kind="fuzzy" mode to CleanupService.find_duplicates that wires the existing Phase Beta MinHash-LSH service (FuzzyIndex) into the cleanup pipeline. Catches near-duplicates that have different bytes but similar content -- re-encoded JPEGs at different quality, re-compressed MP3s at different bitrates, re-OCR'd PDFs, edited text files. Source changes in src/curator/services/cleanup.py: new imports for FuzzyIndex and FuzzyIndexUnavailableError from curator.services.fuzzy_index; new module constant MATCH_KINDS = ("exact", "fuzzy"); new module constant DEFAULT_FUZZY_SIMILARITY_THRESHOLD = 0.85 (intentionally stricter than the lineage default of 0.5 because cleanup is destructive). ind_duplicates signature gains two new keyword-only params: match_kind: str = "exact" and similarity_threshold: float = DEFAULT_FUZZY_SIMILARITY_THRESHOLD. Validation raises ValueError on unknown match_kind (with the valid list in the message); FuzzyIndexUnavailableError propagates through (rather than being caught and silently returning empty) because the user explicitly asked for fuzzy mode and getting silent emptiness would mask the missing-dependency problem. New private method _find_fuzzy_duplicates does the work in five steps: (1) FileQuery(has_fuzzy_hash=True, source_ids=[source_id] if source_id else None, source_path_starts_with=root_prefix, deleted=False) pulls candidates from the index; (2) FuzzyIndex(threshold=similarity_threshold) builds the LSH index; (3) every candidate is index.add(curator_id, fuzzy_hash) -- malformed hashes are caught and recorded in report.errors but the run continues; (4) for each file, query the index for LSH neighbors -> build adjacency map (filtering self-matches); (5) walk **connected components** via iterative BFS so transitive groups (A~B and B~C imply {A,B,C} are one set even if A~C aren't direct LSH neighbors) collapse into one duplicate group. Each component with > 1 file gets the same _pick_keeper treatment (shortest_path / longest_path / oldest / newest, with optional keep_under prefix taking precedence) used by exact mode. Component IDs are synthetic strings "fuzzy:0", "fuzzy:1", ... so the renderer can group by dupset_id the same way it does for exact mode. Each fuzzy finding's details records match_kind="fuzzy" (so the renderer can distinguish modes), dupset_id, uzzy_hash (the duplicates own hash), kept_fuzzy_hash (so users can compare visually), mtime, source_id, similarity_threshold (so users know exactly what threshold fired). Existing exact-mode code path got one new field too -- match_kind="exact" in details -- so EVERY duplicate finding is now self-describing. CLI in src/curator/cli/main.py: cleanup duplicates subcommand gains --match-kind exact|fuzzy (default exact) and --similarity-threshold FLOAT (default 0.85) flags; help text warns about fuzzy false-positive risk and recommends reviewing the plan output before --apply. Custom renderer _render_duplicate_report reads indings[0].details["match_kind"] and prints "Cleanup (duplicate file, fuzzy / near-duplicate)" vs "... exact" in the header so the mode is impossible to mistake at a glance. **Real-world end-to-end demo with 3 text files** built from string.ascii_lowercase + ' ' pseudo-random prose: file_1 = base text (4000 chars), file_2 = base + " [edit: small change at end]" (near-duplicate of file_1 at jaccard ~0.85), file_3 = totally unrelated (4000 chars from a different seed). After curator scan local: all three got both xxhash3_128 AND fuzzy_hash populated. EXACT mode (--match-kind exact) returned **count=0** because the bytes differ; FUZZY mode at threshold 0.5 returned **1 group, 1 freeable file (3.9 KB)** correctly identifying file_2 as a duplicate of file_1 with the keeper being the shorter path (Library/document.txt); FUZZY mode at strict threshold 0.99 returned **count=0** because even the near-identical pair fell below 0.99 jaccard. unrelated.txt was correctly never flagged in any mode. Tests: 10 unit at 	ests/unit/test_cleanup_fuzzy_duplicates.py: 3 validation (MATCH_KINDS exposes both / invalid match_kind raises ValueError / DEFAULT_FUZZY_SIMILARITY_THRESHOLD is 0.85), 2 empty (no fuzzy hashes returns empty / isolated unrelated files not flagged), 3 detection (two-file group with all metadata correctly recorded incl match_kind+dupset_id+fuzzy_hash+kept_fuzzy_hash+similarity_threshold / threshold tightening at 0.99 suppresses what 0.5 catches / three-file transitive group via union-find produces 2 findings + 1 keeper sharing one dupset_id), 2 strategy interactions (oldest works in fuzzy mode just like exact / keep_under flips keeper). **Mid-build catch worth documenting**: initial test fixtures used repeated phrases like "the quick brown fox jumps over the lazy dog " * 100 -- this produced degenerate fuzzy hashes (96:YYYYYYYY...) whose MinHash n-gram signatures collapsed because every 3-gram of "YYY" is just another "YYY". Result: jaccard ~0.20 between near-dupes, well below the 0.5 threshold, and all 4 detection tests failed with count=0. **Fix**: built a _varied_text(seed) helper that uses 
andom.Random(seed).choices(string.ascii_lowercase + " ", k=4000) for prose-like content with realistic n-gram distribution. After the fix: jaccard 0.81-0.85 between near-dupes (from a 3-char edit at the end), 0.0 between unrelated. This is exactly the dynamic range needed for a real cleanup workflow, and it lands meaningfully above the 0.5 default threshold. All apply-path machinery (send2trash via _delete_one, audit logging, safety REFUSE checks, **v0.29 index sync**) works identically for fuzzy duplicates because CleanupKind.DUPLICATE_FILE is shared -- only the discovery method changed. Default suite: **660 passing, 7 correctly-skipped, 38.4s.** Opt-in suite: **668 total passing.** **F9 (Fuzzy dedup) is now feature-complete.** Stretch goals: render per-pair similarity scores (needs computing jaccard pairwise during the BFS instead of just bucket-membership); fuzzy + exact combined mode (run both, dedup the union); user-configurable --max-component-size N to refuse huge components that suggest a too-loose threshold.
- **2026-05-07 v0.31** -- **Phase Gamma F5 (code project organization) SHIPPED.** Fourth type-specific organize pipeline. New src/curator/services/code_project.py (~360 lines) with CodeProjectService (stateless) and CodeProject dataclass (root_path, vcs_type, project_name, primary_language, file_count, total_size, last_modified, language_breakdown). **Discovery layer**: is_project_root(path) returns the VCS type (git/hg/svn/zr/darcs) if a marker dir is present, else None; rejects file paths and submodule pointer files (.git as a file, not a dir). ind_projects(root) walks the tree with explicit prune-on-discovery semantics: when a project root is found, that subtree is NOT descended (so nested submodules absorb into the parent project rather than fragmenting at every git boundary). Skips SKIP_DIR_NAMES during descent (
ode_modules, .venv, __pycache__, 	arget, uild, dist, .idea, .vscode, endor, ower_components, _build, .gradle, .m2, etc. plus all VCS markers themselves). Sorts results by root_path for deterministic output. **Analysis layer**: nalyze_project(root) walks the project body counting files by extension into a Counter; uses LANGUAGE_BY_EXTENSION mapping (29 languages: Python/JS/TS/Java/Kotlin/Scala/Clojure/Ruby/Go/Rust/Swift/ObjC/Csharp/Fsharp/VB/PHP/Perl/C/Cpp/Haskell/OCaml/Elm/Elixir/Erlang/Lisp/Scheme/Lua/R/Julia/Nim/Zig/Vlang/Dart/Crystal + Shell/PowerShell/Batch/Web). **Language inference is by file count, not size** -- a 50 MB binary blob cant outvote 200 .py files. Markdown / Documentation files are registered but get weight 0 in dominance voting (so a doc-heavy project is still classified by its real code). IGNORED_EXTENSIONS filters .pyc / .lock / images / archives / binaries / docs (.pdf/.docx) so they dont count toward language inference. Tiebreaker is alphabetic on language name for determinism. _iter_project_files skips SKIP_DIR_NAMES during analysis too -- critical for correctness, because a JS project with a node_modules full of vendored .py installer scripts must still be classified as JavaScript. **Destination layer**: propose_destination(project, file_path, target_root) returns {target}/{language}/{project_name}/{rel_subpath} -- preserving the projects internal layout (project/src/main.py lands at Python/project/src/main.py, NOT flattened). Returns None if the file is outside the project root. ind_project_containing(file_path, projects) returns the most-specific project (longest matching root_path) whose root contains the file. **OrganizeService integration**: __init__ gains optional code: CodeProjectService | None = None parameter (default-constructed if None, matching the music/photo/document pattern). plan discovers projects ONCE up front (so a 1000-file scan with 50 projects walks the tree exactly once, not 1000 times) by calling ind_projects(root_prefix) if root_prefix is set, else falling back to the drive anchor of the first file. New lif organize_type == "code" and code_projects: branch in the proposal logic uses ind_project_containing to map each file to its project then propose_destination to compute the target path. Files outside any discovered project (loose files at the library root) get no proposal, by design. **Critical detail discovered during integration**: project files inherently land in the CAUTION bucket because the .git/ marker triggers SafetyServices project_file concern -- by design, so users dont accidentally reorganize active git repos. For music/photo/document modes, only SAFE files get proposals (dont auto-suggest reorganizing app-data music). For code mode, that guard is loosened: CAUTION files DO get proposals because the user passing --type code IS the explicit consent to organize project files. The bucketing stays the same; only the proposal computation widens. The two-fold fix: (1) should_propose = report.level == SafetyLevel.SAFE or (organize_type == "code" and report.level == SafetyLevel.CAUTION) in plan(), (2) integration tests look in CAUTION bucket for code-mode proposals. **CLI integration**: --type validation extended from {music, photo, document} to {music, photo, document, code}; help text now mentions "or 'code' (detects VCS-marked projects and proposes Language/ProjectName/relpath layout)". **Real-world end-to-end demo with 22 files across 4 projects + 2 loose files**: scrapbook (Python, git, 6 files including app/models.py + tests/test_main.py + README.md), 
usty-tool (Rust, git, 3 files including Cargo.toml + src/main.rs), rontend-app (TypeScript, git, 9 files including 5 adversarial .py files inside node_modules -- **language correctly inferred as TypeScript not Python because node_modules was skipped during language inference**), legacy-thing (Go, **.hg** marker not just .git, 2 .go files). After scan: 22 files indexed. Plan output: 20 CAUTION (all correctly flagged with project_file concern from SafetyService), 2 SAFE (the loose files at the library root). Each projects internal subpath was preserved in the proposal: <target>/Python/scrapbook/app/models.py, <target>/Rust/rusty-tool/src/main.rs, <target>/TypeScript/frontend-app/src/App.tsx, <target>/Go/legacy-thing/main.go. The loose files (scratch_idea.txt, 	odo.md) were correctly NOT proposed because ind_project_containing returned None for them. Tests: **36 unit** at 	ests/unit/test_code_project.py: 7 is_project_root (.git/.hg/.svn detection + non-VCS rejection + nonexistent + file-path + .git-as-file-not-dir submodule pointer guard), 8 find_projects (empty / single / 3 siblings sorted / pruning-at-boundary so nested .git absorbed / different depths / SKIP_DIR_NAMES skipping during descent / search-root-IS-a-project / nonexistent-root-empty), 11 analyze_project (counts files+size / Python inference / TypeScript inference / **node_modules adversarial test where 4 .py files inside dont make a JS project look like Python** / __pycache__ skipping / Unknown for empty / Unknown for only-ignored-extensions / alphabetic tiebreaker / Markdown-doesnt-outvote-real-code with 20 .md vs 2 .py / records last_modified / records vcs_type), 3 propose_destination (basic + subpath-preservation + outside-project-returns-None), 2 find_project_containing (correct-project + None-for-unrelated), 5 module-level constant tests (VCS_MARKERS includes git / SKIP_DIR_NAMES includes node_modules / SKIP_DIR_NAMES includes ALL VCS markers / Python ext mapped / .pyc in IGNORED). **5 CLI integration** at 	ests/integration/test_cli_organize_code.py: help-lists-code-type / invalid-type-still-rejected / code-type-requires-target / plan-proposes-Python-and-Rust-destinations-for-real-projects (real curator scan round-trip then organize --type code with --show-files, asserts both Python/myapp and Rust/rusty_thing proposals appear) / loose-file-outside-project-not-proposed. **Mid-build catch worth remembering**: integration tests initially looked for proposals in payload["safe"]["files"] (mirroring how music/photo/document tests work). They actually land in payload["caution"]["files"] because of the project-root safety concern. A diagnostic probe (writing the tree, scanning it, calling OrganizeService.plan directly) revealed safe=0 caution=2 for a 2-file Python project, which led to the should_propose loosening. Took two iterations of the integration test to land. Default suite: **701 passing, 7 correctly-skipped, 37.3s.** Opt-in suite: **709 total passing.** **F5 (Code project organization) is now feature-complete.** Stretch goals: (a) MULTI-PROJECT discovery in the apply pipeline (currently OrganizeService.stage moves files but the per-project staging hasnt been validated end-to-end); (b) detect non-VCS projects via heuristics (presence of pyproject.toml / package.json / Cargo.toml / go.mod); (c) submodule recognition (when a nested .git is in a submodules/ dir, report it as a sub-project rather than absorbing).
- **2026-05-07 v0.33** -- **Phase Gamma organize index sync SHIPPED.** Closes the second of the two phantom-file gaps (v0.29 closed the cleanup-deletes gap; v0.33 closes the organize-moves gap). When OrganizeService.stage() or apply() physically relocates a file via shutil.move, the corresponding FileEntity.source_path is now updated to the new on-disk location so subsequent find_by_path(original) calls return None (no phantom) and find_by_path(new_location) returns the entity. Without this, the index would have been silently wrong: find_by_path(original) would have kept returning a stale entity pointing at a path that no longer existed, and the next scan would have treated the old location as a deletion AND the new location as a brand-new file -- the same file recorded twice. New private method `OrganizeService._sync_index_after_move(curator_id_str, new_path)` handles the update via file_repo.get(cid) -> mutate entity.source_path -> file_repo.update(entity). Wired into all three move flows: stage moves call it with the staged path (after appending the MOVED outcome), apply moves go through stage internally so they get it for free, revert moves call it with the original path (after appending the RESTORED outcome). Best-effort throughout -- NEVER raises and NEVER fails the parent move. Skip conditions documented and tested: self.files is None (defensive null check), malformed curator_id (UUID parse fails), files.get(cid) raises (DB unreachable), get returns None (entity not in index -- shouldnt happen because plan() pulled it from the index, but guarded against the race), files.update(entity) raises (DB locked etc). Each failure mode is logged at debug level so a tail of the curator log shows exactly which path failed without polluting normal output. Only MOVED / RESTORED outcomes trigger the sync -- SKIPPED_NO_PROPOSAL (file was SAFE but had no proposal, e.g. a non-audio file in music mode), SKIPPED_COLLISION (destination already exists), and FAILED do NOT call the sync, because nothing actually moved. **Real-world end-to-end demo** with a single indexed untagged.mp3 file at library/untagged.mp3. Before apply: on disk original.exists()=True; in index entity.source_path = original, find_by_path(original) returns the entity. After org.apply(plan) (which moves the file to Music/Pink Floyd/The Wall/06 - Comfortably Numb.mp3 based on stubbed full mutagen tags): on disk original.exists()=False, new_path.exists()=True; in index entity.source_path = <new full path>, find_by_path(original) returns None (the headline gap-closure proof), find_by_path(new_path) returns the entity. **Phantom-file gap is provably closed** -- without v0.33 sync, find_by_path(original) would still have returned the entity. Tests: **9 unit** at tests/unit/test_organize_index_sync.py covering all paths: 4 stage/apply update tests (apply-updates-source_path-to-target which is the headline / find_by_path-works-for-NEW-path-after-apply / find_by_path-OLD-returns-None-after-apply which is the headline regression check that proves the gap is closed / stage-mode-also-updates-source_path so this isnt apply-only), 1 revert test (revert-resets-source_path-to-original; verifies the round-trip stage->revert leaves the index pointing at the original path again), 2 only-on-MOVED tests (skipped-no-proposal-does-NOT-update / skipped-collision-does-NOT-update where we pre-create the destination), 2 defensive tests (update-exception-does-not-fail-move using MagicMock(file_repo) with update.side_effect=RuntimeError to verify the on-disk move still succeeds AND that update WAS attempted via assert bad_repo.update.call_count == 1, proving the exception path was actually exercised; get-exception-does-not-fail-move using get.side_effect=RuntimeError to verify even an earlier failure mode is gracefully handled with update.call_count == 0 since the exception happened before the update path). Every test uses real services with real databases except the two defensive tests where MagicMock is needed to inject the failure modes. **All four cleanup pipelines (junk + empty_dir + broken_symlink + duplicate_file from v0.29) AND all three organize move flows (stage + apply + revert from v0.33) now keep the index consistent automatically.** No manual `curator scan` re-run is needed after any destructive operation -- the index always reflects the on-disk truth. Default suite: **728 passing, 7 correctly-skipped, 39.5s.** Opt-in suite: **736 total passing.** **Organize index sync is now feature-complete.** **Twelve-feature Phase Gamma block (v0.20 -> v0.33) is feature-complete** with all four type-specific organize pipelines (music + photo + document + code), all five cleanup detectors (junk + empty_dir + broken_symlink + exact dup + fuzzy dup), and full bidirectional index sync on every destructive operation. Excellent cut point for tagging.
- **2026-05-07 v0.33 TAGGED** -- **First stable release cut.** Bumped version from `0.1.0a1` (Phase Alpha placeholder) to `0.33.0` in three places: `pyproject.toml` (line 7), `src/curator/__init__.py` (`__version__`), and `src/curator/services/musicbrainz.py` (`_DEFAULT_USER_AGENT_VERSION` -- the hardcoded MB User-Agent version was still pinned to "0.27" from when the client was first written and never updated despite the feature shipping in v0.32). Confirmed `python -c "import curator; print(curator.__version__)"` reports `0.33.0`. Created `CHANGELOG.md` with a roll-up of every Phase Gamma version (v0.20 -> v0.33) including a per-version one-liner and the highlights of what makes 0.33 the first stable release: four organize pipelines feature-complete (music/photo/document/code), five cleanup detectors feature-complete (junk/empty_dirs/broken_symlinks/exact-dup/fuzzy-dup), bidirectional index sync on every destructive operation. Documents known gaps (gate-4 GUI / gate-5 gdrive / MB stretch / code stretch / Pillow 12). Final regression run at the new version: **728 default-suite passing, 7 correctly-skipped, 0 failures, 0 warnings, 61.1s** (cold cache after `rm -rf __pycache__`). Note: there is no `.git` dir at the project root, so the "tag" is documentation-only at this point -- the version bump + CHANGELOG.md + this BUILD_TRACKER entry collectively mark the cut point. To turn this into a real git tag the user would run `git init && git add -A && git commit -m "Release 0.33.0" && git tag -a v0.33.0 -m "First stable release"` from the project root. **Phase Gamma block officially complete.**
- **2026-05-07 v0.34** -- **Phase Beta gate 4 (GUI) read-only first ship SHIPPED.** Native PySide6 desktop GUI launched via `curator gui`. Three tabs (Browser / Bundles / Trash), each backed by a `QAbstractTableModel` adapter over the existing repositories. The GUI shares the EXACT same `CuratorRuntime` the CLI uses; everything below the runtime layer is unchanged, so any future CLI/GUI dispatch (Phase γ service mode) can swap them without touching the visual layer. New package `src/curator/gui/`: `__init__.py` (~25 lines, deliberately deferred-import so `import curator.gui` in a headless env doesnt crash), `models.py` (~330 lines with FileTableModel + BundleTableModel + TrashTableModel + size/datetime formatters), `main_window.py` (~150 lines with QMainWindow, tab widget, menu bar, status bar, all three views wired in), `launcher.py` (~50 lines with `run_gui(runtime) -> int` that boots QApplication + `is_pyside6_available()` cheap probe). New CLI subcommand `curator gui` at `src/curator/cli/main.py:gui_cmd` does the deferred-import dance: tries `from curator.gui.launcher import is_pyside6_available, run_gui`, falls back with an actionable `pip install curator[gui]` hint on ImportError, then constructs a MusicBrainzClient... wait no, that was v0.32. This one constructs nothing at the CLI layer -- it just hands the existing `rt: CuratorRuntime` to `run_gui` which boots `QApplication.instance() or QApplication(sys.argv)`, shows `CuratorMainWindow(rt)`, runs `app.exec()`, returns the exit code. **Architecture decisions worth documenting**: (1) **Models load all rows on construction + `refresh()` re-queries the DB.** Personal-scale Curator installs are low thousands of files; "load it all" is honest and simpler than lazy fetch. We can revisit when someone hits 50k rows. (2) **Models implement `sort()` Pythonically (no proxy model).** This means sort stability is via `list.sort` which is stable; tie-breaker is the natural sort order from the most recent prior sort. The QTableView header click triggers `model.sort(column, order)` because `setSortingEnabled(True)` is enabled. (3) **The status bar shows the DB path + per-tab row counts.** Updated on `refresh_all()` which is bound to File > Refresh / F5. (4) **Mutations are intentionally NOT wired in v0.34.** Selecting a row in Browser doesnt trash the file; selecting a bundle doesnt dissolve it. The visual layer ships first; HITL escalation paths land in v0.35. The about dialog mentions this explicitly. (5) **The GUI is opt-in via the new `[gui]` extra in pyproject.toml** (`PySide6>=6.6`). The `[dev]` extra also gained `pytest-qt>=4.2` for testing. The `[all]` extra now includes `gui`. (6) **Side-fix**: `cli/runtime.py:build_runtime` was missing the `code: CodeProjectService` collaborator that v0.31 added to OrganizeService. Fixed in this commit -- the runtime now constructs a `CodeProjectService()` and passes it to `OrganizeService(file_repo, safety, music=..., photo=..., document=..., code=code, audit=audit_repo)`. Without this, calling `curator organize --type code` from the CLI would have used the default-constructed CodeProjectService inside OrganizeService.__init__ rather than the explicit one (functionally equivalent, but the runtime should be exhaustive). **Testing**: 19 model tests + 1 launcher test in default suite (`tests/gui/test_gui_models.py`); 1 smoke test marked `slow` and gated behind `pytest -m slow` (constructs the actual QMainWindow via pytest-qt qtbot fixture, asserts title + tab count + refresh works without entering the modal exec loop). Default suite: 728 -> **748 passing, 7 correctly-skipped, 69.4s** (the regression suite is slower because PySide6 import adds ~30s on cold cache). Opt-in suite: 736 -> 749 (728 default + 1 GUI smoke + 8 prior opt-in + 12 GUI default deselected by `-m "not slow"`... wait no the 19 model tests are NOT slow-marked, only the smoke is, so opt-in adds the smoke for 749). **Real screenshot verified**: booted the GUI against a freshly-seeded DB with 9 files (mp3s, jpgs, .py, .pdf, .docx, .exe) and 2 bundles, captured a 1100x650 PNG screenshot via `window.grab().save(path, "PNG")`. The screenshot shows native Windows 11 dark theme, three tabs, sortable columns with formatted sizes ("5.0 MB", "234.4 KB", "540 B"), formatted modified dates ("2024-06-01"), and the status bar reading "DB: <path>" + "Files: 9   Bundles: 2   Trash: 0". GUI exits cleanly with code 0 when window is closed via `window.close()` or `app.quit()`. Saved at `docs/v034_gui_screenshot.png`. **What this v0.34 IS NOT**: there are no mutations, no double-click-to-trash, no bundle editing, no per-file inspect view, no lineage graph, no audit log view, no settings editor. DESIGN.md §15.2 lists 7 core views; we ship 3 of them. The remaining 4 (Inbox, Lineage Graph, Audit Log, Settings) and the mutation paths are deferred to v0.35+. **Version bumped to 0.34.0** in `pyproject.toml` and `src/curator/__init__.py`. The hardcoded MusicBrainz `_DEFAULT_USER_AGENT_VERSION` was already updated to "0.33" at the v0.33 cut and stays there until the next MB-touching change (no need to bump it for an unrelated GUI release). CHANGELOG.md will be updated in a follow-up to add the v0.34 entry.
- **2026-05-07 v0.35** -- **Phase Beta gate 4 GUI mutations SHIPPED.** Three destructive operations now wired through the PySide6 GUI from v0.34: (1) Browser tab right-click -> "Send to Trash..." -> confirm -> `TrashService.send_to_trash`; (2) Trash tab right-click -> "Restore..." -> confirm -> `TrashService.restore`; (3) Bundles tab right-click -> "Dissolve bundle..." -> confirm -> `BundleService.dissolve`. Each flow gates on a `QMessageBox.question` confirmation dialog (Yes / Cancel, defaults to Cancel for safety). All three actions also accessible via a new Edit menu (Ctrl+T trash / Ctrl+R restore / Ctrl+D dissolve) for keyboard users. **Source changes**: `src/curator/gui/main_window.py` grew from ~150 to ~330 lines. New private methods: `_show_browser_context_menu(pos)`, `_show_trash_context_menu(pos)`, `_show_bundles_context_menu(pos)` -- each calls QTableView.indexAt, builds a QMenu with one action, and dispatches to a `_slot_*_at_row(row)` method on the chosen action. The `_slot_*_at_row` methods pull the row's entity via `model.file_at(row) / .trash_at(row) / .bundle_at(row)`, show the confirmation, and on Yes call the `_perform_*` method. **Critical refactoring decision**: factored the actual mutation logic into three `_perform_*` methods (`_perform_trash`, `_perform_restore`, `_perform_dissolve`) that NEVER raise and return `(success: bool, message: str)`. The slots just show the message in `QMessageBox.information` (success) or `QMessageBox.warning` (failure). This makes the methods unit-testable without booting an event loop -- tests instantiate `CuratorMainWindow(rt)` and call `window._perform_trash(...)` directly. **`RestoreImpossibleError` handling**: on Windows, `send2trash` doesnt return the OS Recycle Bin path, so `TrashService.restore` raises `RestoreImpossibleError` for everything trashed via Curator. The `_perform_restore` method catches this by exception class name (avoids cyclic import) and returns a friendlier message: "Curator can't restore this file automatically (the OS trash location wasnt recorded at trash time). Please open your Recycle Bin and restore it manually." The trash record stays in place so the user can see the original path. **`Slot dispatch from Edit menu vs context menu**: both routes call the same `_slot_*_at_row(row)` method but the Edit menu routes through `_slot_*_selected()` first which checks for a current selection (showing an info dialog if none) and pulls `view.currentIndex().row()`. Context menus route directly because the user clicked on a specific row. **About dialog updated** to mention the v0.35 mutation capability. **Real-world end-to-end demo**: built a fresh CuratorRuntime against a temp DB, created a real `important_doc.txt` file on disk, indexed it, added it to a bundle. BEFORE: file on disk = True, FileEntity.deleted_at = None, bundle exists, no TrashRecord. Called `window._perform_trash(entity.curator_id, reason="demo trash")` directly. AFTER: file on disk = False (in OS Recycle Bin), FileEntity.deleted_at = 2026-05-07 16:49:33, TrashRecord exists. Then called `window._perform_dissolve(bundle.bundle_id)`. AFTER DISSOLVE: bundle row = None, FileEntity row preserved. Both operations returned success=True with friendly messages. **Tests**: **11 new** at `tests/gui/test_gui_mutations.py`: 3 `_perform_trash` (real file moved to recycle bin + entity marked deleted + trash record / nonexistent curator_id returns failure / file missing from disk returns failure with `send2trash` raising), 2 `_perform_restore` (RestoreImpossibleError handled gracefully on Windows / nonexistent returns failure), 2 `_perform_dissolve` (bundle removed + members preserved / nonexistent handled gracefully without raising), 2 slot cancellation paths (trash slot with QMessageBox.question patched to return Cancel does NOT mutate / dissolve slot Cancel path), 2 no-selection slots (slot called with no current index shows info dialog via patched QMessageBox.information). All 11 use real `build_runtime` against a temp DB; none uses pytest-qt qtbot because the dialog-free `_perform_*` methods + patched-dialog slot tests dont need event loop driving. Default suite: 748 -> **759 passing, 7 correctly-skipped, 43.9s** (back to fast because PySide6 import is now warmed up across the suite). Opt-in suite: 749 -> 760 (759 default + 1 GUI smoke). **GUI mutations are now feature-complete.** Phase Beta gate 4 progress: was ~30% at v0.34 (3 of 7 views, no mutations); now ~50% (3 of 7 views WITH all three relevant mutations wired). Remaining 50%: (a) 4 missing DESIGN.md §15.2 views (Inbox, Lineage Graph, Audit Log, Settings) (b) per-file inspect dialog on Browser double-click (c) bundle creation + membership editing UI. **Version bumped to 0.35.0.**
- **2026-05-07 v0.36** -- **Phase Beta gate 4 per-file inspect dialog SHIPPED.** Double-click any row in the Browser tab opens a modal `FileInspectDialog` showing everything Curator knows about that file in three tabs: Metadata (every fixed schema field including curator_id / source / paths / size both bytes-and-human / mtime / ctime / inode / xxhash3_128 / md5 / fuzzy_hash / file_type / extension / file_type_confidence / seen_at / last_scanned_at / deleted_at, plus every flex attribute prefixed `flex: <key>`), Lineage Edges (every edge where this file is on either side, with the OTHER file's path resolved from the file repo, direction arrow [`->`] for outbound or [`<-`] for inbound, kind, confidence, detected_by, notes), and Bundle Memberships (every bundle this file belongs to with role + confidence + added_at, bundle name + bundle_type resolved from the bundle repo). New module `src/curator/gui/dialogs.py` (~210 lines): `FileInspectDialog(file, runtime, parent=None)` QDialog subclass with `_build_ui()` setting up the header label + QTabWidget + QDialogButtonBox(Close). Three tab builders: `_build_metadata_tab` returns a 17-row plus-flex-attrs key/value QTableWidget via `_make_kv_table` helper, `_build_lineage_tab` queries `runtime.lineage_repo.get_edges_for(curator_id)` (returns edges in BOTH directions) and resolves the other file via `runtime.file_repo.get(other_id)`, `_build_bundles_tab` queries `runtime.bundle_repo.get_memberships_for_file(curator_id)` and resolves each bundle via `runtime.bundle_repo.get(bundle_id)`. Two factored helpers: `_make_kv_table(rows: list[tuple[str, str]])` and `_make_table(headers, rows)`. Header label uses HTML for visual hierarchy: bold path on first line, then size-bullet-modified-bullet-source line; if `deleted_at` is set, appends a red "DELETED (timestamp)" segment. **Wiring in main_window.py**: `_build_browser_tab` connects the QTableView's `doubleClicked` signal to a new `_slot_inspect_at_index(index)` method that pulls `self._files_model.file_at(index.row())` and calls `_open_inspect_dialog(file)`. The factored `_open_inspect_dialog` is the testable seam: tests patch this attribute on the window to capture the dialog construction without entering `dlg.exec()`. Browser context menu also gained an "Inspect..." action above the existing "Send to Trash..." (with a separator between), so users have three routes to the dialog: double-click, right-click + Inspect, or right-click on Browser then Edit menu (the Edit menu does NOT have Inspect bound -- it's the only mutation/action that's exclusively accessible from the Browser tab itself). **Real-world end-to-end demo**: built a runtime with a Pink Floyd "Comfortably Numb" file as subject, two related files (Mother.mp3 in same folder, comfortably_numb.mp3 in a backup dir with the same xxhash3), two lineage edges (DUPLICATE outbound to backup with confidence 1.00, NEAR_DUPLICATE inbound from Mother with confidence 0.78), and two bundle memberships (The Wall album as primary, Pink Floyd discography as member). Constructed `FileInspectDialog`, took a screenshot at 900x520. The dialog renders cleanly with all three tabs populated -- Lineage Edges tab pre-selected for the screenshot shows both edges with correct directions (`->` for outbound, `<-` for inbound), correct other-file paths, formatted confidences, detector names, and notes. Saved at `docs/v036_inspect_dialog.png`. **Tests**: **14 new** at `tests/gui/test_gui_inspect.py`: 3 construction (no-crash + window title contains filename / header includes path-size-source / deleted file header includes "DELETED" red marker), 4 metadata tab (curator_id row populated correctly / Size (human) row formatted as MB / flex attrs appear with `flex: <key>` prefix and the right value / xxhash row populated), 3 lineage tab (both edges shown with correct kind+direction+other-file path triples / confidence formatted to 2 decimals like "1.00" and "0.78" / empty when file has no edges using a separate isolated FileEntity), 2 bundles tab (both memberships shown with bundle names / role column shows "primary" for the Wall album row), 2 wiring (double-click slot opens inspect dialog with correct file via patched `_open_inspect_dialog` / invalid index is no-op via empty QModelIndex). All 14 use real `build_runtime` against a temp DB; none calls `dlg.exec()` or any other modal blocking call. Default suite: 759 -> **773 passing, 7 correctly-skipped, 42.9s.** Opt-in suite: 760 -> 774 (773 default + 1 GUI smoke). **Phase Beta gate 4 progress**: was ~50% at v0.35; now ~60% (3 of 7 views + all relevant mutations + per-file inspect dialog). Remaining 40%: 4 missing DESIGN.md §15.2 views (Inbox, Lineage Graph, Audit Log, Settings), bundle creation + membership editing UI. **Version bumped to 0.36.0.**
- **2026-05-07 v0.37** -- **Phase Beta gate 4 Audit Log view SHIPPED.** Fourth tab in the GUI main window: tabular read-only view over the `audit_log` table. New class `AuditLogTableModel` in `src/curator/gui/models.py` (~155 lines appended): subclass of QAbstractTableModel over AuditRepository, takes optional `limit` parameter (defaults to `DEFAULT_LIMIT = 1000` matching the repository default), `refresh()` calls `audit_repo.query(limit=self._limit)` which returns newest-first, `entry_at(row) -> AuditEntry | None`. Five columns: When (formatted "YYYY-MM-DD HH:MM" via existing `_format_dt`), Actor (e.g. "user.cli", "user.gui", "curator.organize.apply"), Action (e.g. "scan", "trash", "organize.apply.move"), Entity (compact "type:short_id" with UUID-shaped IDs truncated to 8 chars + "..."; empty when both entity_type and entity_id are None), Details (JSON-stringified dict, truncated at `DETAILS_PREVIEW_LEN = 80` chars with "..." suffix; full JSON shown via Qt.ToolTipRole on the cell hover). Two static helpers added: `_format_entity(entry)` handles UUID truncation + the "type:id" or "id" or "" cases; `_format_details(details, *, truncate)` does `json.dumps(default=str, sort_keys=True)` and conditional truncation. `sort()` implemented for all 5 columns (timestamp / actor / action / entity / details). Imports added: `from curator.models.audit import AuditEntry`, `from curator.storage.repositories.audit_repo import AuditRepository`. `__all__` extended to include `"AuditLogTableModel"`. **Wiring in main_window.py**: import added (`AuditLogTableModel` alongside the existing three model imports), new tab added in `_build_ui`: `tabs.addTab(self._build_audit_tab(), "Audit Log")` (now 4 tabs in this order: Browser / Bundles / Trash / Audit Log). New method `_build_audit_tab(self) -> QWidget` constructs `self._audit_model = AuditLogTableModel(self.runtime.audit_repo)`, the corresponding QTableView via the existing `_make_table_view` helper, and wraps it. **No context menu on the audit tab** -- the audit log is intentionally append-only at the storage layer (AuditRepository has no `delete` or `update` method by design, see DESIGN.md §17), so there's no GUI mutation path because there's no model-level mutation path. `refresh_all()` extended to call `self._audit_model.refresh()` after the trash refresh. `_refresh_status_bar()` extended: counts label now reads `Files: N    Bundles: N    Trash: N    Audit: N` (added the audit count as the 4th metric). **Real-world end-to-end demo with screenshot**: built a runtime, seeded 9 varied audit entries spanning typical Curator operations (user.cli scans, curator.classify file classification, curator.lineage edge detection, user.gui trash + bundle.dissolve, curator.organize.apply file moves, user.cli config.reload with no entity, curator.cleanup junk detection), launched the main window, set tab index to 3 (Audit Log), captured a 1100x650 screenshot via `window.grab()`. The screenshot at `docs/v037_audit_log.png` shows: 4 tabs with Audit Log active, 5 columns rendered cleanly with Actor names like "user.gui" / "curator.organize..." (the long ones truncated), Action names like "config.reload" / "bundle.dissolve" / "trash" (longer ones like "cleanup.junk.de..." truncated to fit), Entity column showing "scan_job:3309c..." / "file:6567ac97..." / "edge:08403430..." patterns plus an empty cell on the config.reload row, Details column with compact JSON like `{"files_seen": 2103, "root": "C:/Users/jmlee/Photos"}` and `{"members_preserved": 12, "name": "Old playlist"}`, and the status bar reading the new four-metric label `DB: ... | Files: 0    Bundles: 0    Trash: 0    Audit: 9`. Newest-first ordering verified visually: row 0 is the latest scan (+56 minutes from base time), row 8 is the initial scan. **Tests**: **23 new** at `tests/gui/test_gui_audit.py`: 3 construction (empty audit log -> 0 rows / columns match COLUMNS constant labels / loads 5 seeded entries), 1 newest-first ordering (timestamps descend, action of row 0 is the latest seed entry), 8 cell content (When column "2024-06-01 09:..." formatted / Actor column "user.cli" / Action column "config.reload" / Entity column with UUID truncates to "file:8charprefix..." / Entity column with no entity_type+entity_id returns empty / Details column truncates long JSON with "..." suffix / Details column empty for empty dict / Details ToolTipRole returns full untruncated JSON), 1 limit parameter (limit=3 caps a 5-row repo to 3 rows + still returns the newest 3), 7 helpers (_format_entity truncates UUID-shaped to "type:8chars..." / _format_entity keeps short non-UUID IDs intact / _format_entity handles missing entity_type / _format_details empty dict -> "" / _format_details short detail not truncated / _format_details long detail truncated with ... / _format_details no_truncate=False keeps full content), 3 wiring (4 tabs total with title "Audit Log" at index 3 / status bar includes "Audit: 5" / refresh_all also refreshes audit model and status bar updates to "Audit: 6" after a new log entry). All 23 use real `build_runtime` against a temp DB; none requires pytest-qt event loop driving. Default suite: 773 -> **796 passing, 7 correctly-skipped, 43.9s.** Opt-in suite: 774 -> 797 (796 default + 1 GUI smoke). **Phase Beta gate 4 progress**: was ~60% at v0.36; now ~70% (4 of 7 views shipped + all relevant mutations + per-file inspect dialog). Remaining 30%: 3 missing DESIGN.md §15.2 views (Inbox, Lineage Graph, Settings), bundle creation + membership editing UI. **Version bumped to 0.37.0** in pyproject.toml + curator/__init__.py.
- **2026-05-07 v0.38** -- **Phase Beta gate 4 Settings view SHIPPED.** Fifth tab in the GUI main window: read-only display of the active `curator.toml` configuration with a Reload-from-disk button for verifying TOML edits. New class `ConfigTableModel` in `src/curator/gui/models.py` (~135 lines appended): subclass of QAbstractTableModel over a flattened Config dict. Two columns: Setting (dotted-path key like `hash.prefix_bytes`, `lineage.fuzzy_threshold`, `trash.purge_older_than_days`) and Value (str-formatted primitive, "true"/"false" for booleans, "(null)" for None, JSON-formatted for lists). `_flatten(data, prefix="")` is a generator that recurses into nested dicts yielding (dotted_key, value_str) pairs in alphabetical key order; lists are leaf-formatted (no recursion into list items). `_format_value(value)` handles the type-specific conversion (None -> "(null)", bool -> "true"/"false", list/tuple -> json.dumps, dict -> json.dumps with sort_keys (defensive), other -> str()). Public method `set_config(config)` re-points the model at a different Config and refreshes -- this is the seam the Settings view's Reload button uses to refresh the *display* without affecting the live runtime. Tooltip on the Value column shows the full untruncated value (cells may elide). Sort works for both columns. Imports added: TYPE_CHECKING-only `from curator.config import Config` (avoid hard-importing in headless tests) plus the existing AuditEntry / AuditRepository imports already there from v0.37. `__all__` extended to include `"ConfigTableModel"`. **Wiring in main_window.py**: import added (`ConfigTableModel`), `QHBoxLayout` and `QPushButton` added to the PySide6.QtWidgets imports. New tab added in `_build_ui`: `tabs.addTab(self._build_settings_tab(), "Settings")` -- now 5 tabs total in this order: Browser / Bundles / Trash / Audit Log / Settings. **The Settings tab is the first GUI view that's NOT just a wrapped table** -- it composes a header label (`self._settings_header` showing source TOML path or "(using built-in defaults)"), the QTableView, a QHBoxLayout with the Reload button, and a help label below. `_build_settings_tab()` constructs all of this and returns a wrapped QWidget with QVBoxLayout. **Critical UX decision documented in the docstring**: the Reload button re-parses the source TOML file and updates the *display* only; the live runtime's services keep using the original config until Curator is restarted. This avoids a half-state mess where some services reflect new settings and others don't, while still letting users verify their TOML edits parsed correctly without restarting. The button's tooltip explains this; the help label below the button reinforces "Edit the file directly with your text editor. Changes apply on next Curator restart." **Mutation logic factored into `_perform_settings_reload()`** following the v0.35 pattern: returns `(success: bool, message: str, fresh_config: Config | None)`, never raises, takes no arguments. The slot `_slot_settings_reload()` calls it, shows a QMessageBox.warning on failure with the error message, otherwise calls `self._settings_model.set_config(fresh)` + `self._update_settings_header(fresh, reloaded=True)` + `self.statusBar().showMessage(message, 4000)` for a transient confirmation in the status bar. The header label gets a green "(reloaded from disk)" suffix to make the just-reloaded state visually obvious. **`_update_settings_header(config, *, reloaded)`** factored out so the initial-construction case (reloaded=False) and the post-reload case (reloaded=True) share the source-path-resolution logic. **Refresh integration**: `refresh_all()` (F5) refreshes Files / Bundles / Trash / Audit but NOT Settings -- the comment in the code explains "Settings model points at the runtime config; nothing to re-query there -- the explicit Reload button is the path for that." **Tests**: **26 new** at `tests/gui/test_gui_settings.py`: 3 construction (default Config has rows / columns match constant labels / Config from real TOML loads), 5 flattening (dotted-path keys / alphabetical order / overridden int value displayed correctly as "8192" / overridden string value as "DEBUG" / list value JSON-formatted with `[".py", ".md"]`), 8 helpers (_format_value None -> "(null)" / True -> "true" / False -> "false" / int -> str / str passes through / list JSON-formatted / _flatten simple dict yields sorted (k,v) tuples / _flatten nested dict yields dotted-path keys / _flatten with list value treats list as leaf no recursion), 1 set_config (re-points at new config and refreshes; new value shows up; old value gone), 3 wiring (5th tab "Settings" at index 4 / header text contains "Loaded from:" + source path / header text contains "built-in defaults" when no source TOML), 4 _perform_settings_reload (succeeds when TOML unchanged returns fresh config / picks up disk changes (KEY INVARIANT: rt.config.get(curator.log_level) stays "DEBUG" while fresh.get returns "WARNING") / returns failure on invalid TOML with success=False fresh=None / succeeds with no source TOML using full Config.load resolution), 2 _slot_settings_reload (slot integrates model refresh + header "reloaded from disk" suffix / patched QMessageBox.warning called on failure). All 26 use real `build_runtime` against a temp DB; none requires pytest-qt event loop driving. Also fixed v0.37 test `test_audit_tab_exists_with_title` (was asserting `count() == 4`, now `count() >= 4` since Settings tab makes it 5). Default suite: 796 -> **822 passing, 7 correctly-skipped, 45.1s.** Opt-in suite: 797 -> 823 (822 default + 1 GUI smoke). **Real-world end-to-end demo with screenshot**: built a runtime against a realistic curator.toml (override hash.prefix_bytes=8192, hash.suffix_bytes=8192, hash.fuzzy_for to a custom 8-extension list, trash.purge_older_than_days=90, lineage.fuzzy_threshold=75 + auto_confirm + escalate thresholds, curator.log_level=DEBUG), launched the main window, set tab index to 4 (Settings), captured a 1100x650 screenshot. The screenshot at `docs/v038_settings.png` shows: 5 tabs with Settings active, Edit menu visible, header label "Loaded from: C:\Users\jmlee\AppData\Local\Temp\curator_v038_screenshot\curator.toml", 18 config rows in the table (sorted ASC by Setting on initial render but user clicked header so visible state shows DESC: trash.* / plugins.* / lineage.* / hash.* / group.* / detection.*), one row highlighted green (selected) showing `hash.suffix_bytes = 8192`, JSON-formatted lists for `hash.fuzzy_for` and `detection.temp_files.patterns`, "Reload from disk" button at bottom, italic help text "Edit the file directly with your text editor. Changes apply on next Curator restart.", status bar with all 4 entity counts (Files: 0   Bundles: 0   Trash: 0   Audit: 0). **Phase Beta gate 4 progress**: was ~70% at v0.37; now ~80% (5 of 7 views shipped + all relevant mutations + per-file inspect dialog). Remaining 20%: 2 missing DESIGN.md §15.2 views (Inbox, Lineage Graph), bundle creation + membership editing UI. **Version bumped to 0.38.0** in pyproject.toml + curator/__init__.py.
- **2026-05-07 v0.39** -- **Phase Beta gate 4 Inbox view SHIPPED.** Sixth GUI tab landing the third of the four post-v0.34 DESIGN.md §15.2 core views, AND the canonical landing tab per the spec ordering. Three stacked dashboard sections in one tab: (a) **Recent scans** -- last 10 from `job_repo.list_recent`; (b) **Pending review** -- lineage edges with confidence in the `[escalate_threshold, auto_confirm_threshold)` ambiguous middle band (DESIGN.md §8.2 confidence thresholds); (c) **Recent trash** -- last 10 from the trash registry. Each section is a QGroupBox with row count in the title (e.g. "Recent scans — 4", "Pending review (confidence in [0.70, 0.95)) — 3"), small QTableView (height capped at 220px), and an italic empty-state hint label below when the section has 0 rows. **Inbox is now the FIRST tab** (index 0) per DESIGN.md §15.2 ordering: Inbox / Browser / Bundles / Trash / Audit Log / Settings. This shifts every other tab by +1; the audit and settings wiring tests were updated accordingly (`test_audit_tab_exists_with_title`: count >= 5, tabText(4) == "Audit Log"; `test_settings_tab_exists_at_index_4`: count == 6, tabText(5) == "Settings"). **Two new models added to `src/curator/gui/models.py`** (~210 lines appended): `ScanJobTableModel(job_repo, *, limit=10)` over `ScanJobRepository.list_recent` with 6 columns (Status / Source / Root / Files / Started / Completed) where the Files column shows "hashed/seen" for completed jobs and just "seen" for running ones; `PendingReviewTableModel(lineage_repo, file_repo, *, escalate_threshold=0.7, auto_confirm_threshold=0.95, limit=50)` with 5 columns (Kind / From / To / Confidence / Detected by) that resolves the from/to file paths via `file_repo.get(curator_id)` with a per-instance cache (`_path_cache`), falling back to `(<uuid>)` when the file row doesn't exist (defensive code path for hard-deleted files; foreign keys prevent the row state but the helper is tested directly). **`TrashTableModel` extended** to accept an optional `limit` kwarg, used by the Inbox to cap to 10 most-recent. **New public method on `LineageRepository`**: `query_by_confidence(*, min_confidence, max_confidence, limit)` returns edges in `[min, max)` ordered by `confidence DESC, detected_at DESC`. This replaced an earlier draft that called `lineage_repo._row_to_edge` directly from the model -- crossing the public/private boundary was a code smell, fixed by adding the proper public API. The new method is tested independently (4 tests at `TestLineageRepoQueryByConfidence` covering range filtering / empty range / max-is-exclusive semantics / limit). **Wiring in main_window.py**: imports updated, `tabs.addTab(self._build_inbox_tab(), "Inbox")` added as the FIRST `addTab` call. New methods `_build_inbox_tab()` (~60 lines composing the 3 sections) and `_make_inbox_section(title, view, model, *, empty_hint)` (returns a QGroupBox with title-with-count + view + optional empty-hint label when rowCount==0). The `_build_inbox_tab()` reads `lineage.escalate_threshold` and `lineage.auto_confirm_threshold` from `self.runtime.config` so the band is configurable per-deployment via curator.toml; this is reflected in the section title which shows the active thresholds (e.g. "Pending review (confidence in [0.70, 0.95))"). `refresh_all()` (F5) extended to also refresh the three Inbox models. **Mid-session repair**: a botched edit accidentally deleted the `_build_bundles_tab(self)` method header, leaving its body as orphaned dead code under `_build_browser_tab`. Caught immediately via the next compile check; fixed in one combined edit that restored `_build_bundles_tab` AND applied the imports + tab order changes that had been atomically rolled back from the original 3-edit batch (a third edit had a non-existent oldText anchor, so `Filesystem:edit_file` correctly rejected the whole batch). Lesson reinforced: when an `edit_file` call fails, ALL edits in that call roll back. **Real-world end-to-end demo with screenshot**: built a runtime with 4 scan jobs (1 running with 420/850 progress, 3 completed with full counts), 4 lineage edges spanning confidence bands (0.96 ABOVE auto_confirm so excluded; 0.87 / 0.82 / 0.74 IN the [0.70, 0.95) middle band), 7 indexed files for path resolution, 2 trashed files with reasons "junk: old installer" and "duplicate of C:/...". Set tab index to 0 (Inbox), captured a 1100x650 screenshot. The screenshot at `docs/v039_inbox.png` shows the 6-tab layout with Inbox active, three QGroupBox sections with row counts in their titles, the Pending review section header showing the active threshold band `[0.70, 0.95)`, all three tables populated with realistic data, status bar reading `Files: 7   Bundles: 0   Trash: 2   Audit: 0`. **Tests**: **25 new** at `tests/gui/test_gui_inbox.py`: 6 ScanJobTableModel (empty repo / loads seeded jobs / columns match constant / status column / Files column "hashed/seen" format / default limit 10 / explicit limit override), 6 PendingReviewTableModel (filters to ambiguous band excluding 0.99 / lower escalate still 2 because 0.99 still excluded by auto_confirm / raise auto_confirm to 1.0 includes all 3 / resolves file paths to readable strings / handles missing file gracefully via direct _resolve_path call (NOT via inserting a ghost edge -- foreign keys would silently reject that) / kind column shows enum.value as string), 4 LineageRepository.query_by_confidence (returns edges in range / empty range returns empty / max-is-exclusive semantics / limit caps results), 2 TrashTableModel limit param (no limit returns all / limit=1 caps to 1), 7 wiring (Inbox tab at index 0 / Browser shifted to index 1 / inbox models constructed with correct row counts / window builds cleanly with empty runtime (zero data) / refresh_all refreshes inbox scans model / inbox threshold values come from runtime config not hardcoded defaults). All 25 use real `build_runtime` against a temp DB; none requires pytest-qt event loop. Default suite: 822 -> **847 passing, 7 correctly-skipped, 49.0s.** Opt-in suite: 823 -> 848 (847 default + 1 GUI smoke). **Phase Beta gate 4 progress**: was ~80% at v0.38; now ~90% (6 of 7 views shipped + all relevant mutations + per-file inspect dialog). Remaining 10%: the Lineage Graph view (the most ambitious of the original 7 views; needs Qt's QGraphicsView or embedded HTML/D3) and bundle creation + membership editing UI. **Version bumped to 0.39.0** in pyproject.toml + curator/__init__.py.
- **2026-05-08 v0.40** -- **Phase Beta gate 5 SHIPPED.** Source plugin contract extended with `curator_source_write` for create-new-file operations; both `local_source` and `gdrive_source` plugins implement it. This is the foundational primitive for cross-source migration (Phase Delta Feature M) and cloud sync (Phase Delta Feature S) -- without it, the source contract was read-only-plus-delete, which made migrating TO any source impossible. **New hookspec** in `src/curator/plugins/hookspecs.py`: `curator_source_write(source_id, parent_id, name, data: bytes, *, mtime, overwrite) -> FileInfo | None` (~28 lines). Whole-file in-memory bytes interface for v1; large files (>500 MB) deferred to a streaming variant in Phase Gamma. Returns FileInfo for the newly-created file. Raises FileExistsError when overwrite=False and target exists; raises OSError/RuntimeError for source-specific failures. **New SourcePluginInfo field** in `src/curator/models/types.py`: `supports_write: bool = False` (defaults False so plugins not yet implementing the hook keep working). Both core plugins now advertise `supports_write=True`. **LocalFSSource implementation** in `src/curator/plugins/core/local_source.py` (~70 lines added): atomic write via `tempfile.mkstemp` in the same directory as the target followed by `os.replace` (which is atomic on the same FS). Auto-creates parent directories if missing; refuses if parent_id points at an existing FILE. Optional mtime preservation via `os.utime` on the tempfile before replace. Cleanup of the tempfile in an `except` block if any operation fails partway through. **Google Drive implementation** in `src/curator/plugins/core/gdrive_source.py` (~90 lines added): PyDrive2 CreateFile with `{"title": name, "parents": [{"id": parent_id}]}`, content set via `new_file.content = io.BytesIO(data)`, then Upload + FetchMetadata. Pre-flight existence check via `client.ListFile({"q": "..."})` because Drive permits duplicate titles in one folder so we must check explicitly to honor overwrite=False semantics. When overwrite=True, trashes existing files with the same title before creating the new one (more conservative than letting Drive's duplicate-title behavior take over). Catches and re-raises generic exceptions as RuntimeError with context. Drive doesn't support setting mtime via the v2 API used by PyDrive2; mtime parameter is documented as advisory only. **Plugin docstring update**: gdrive_source.py header changed from "Status: scaffolding. Implements register / enumerate / stat / read_bytes / delete; move is Phase Gamma." to "Status: v0.40 implements register / enumerate / stat / read_bytes / write / delete; move is Phase Gamma." Phase β gate 5 status: COMPLETE for the core read+write+delete contract. Move and watch remain explicit Phase γ items. **Tests**: **24 new** at `tests/unit/test_source_write_hook.py`: 4 contract-level (SourcePluginInfo.supports_write defaults False / settable True / local advertises True / gdrive advertises True when PyDrive2 available; plus a hookspec-presence smoke check), 4 LocalWriteBasic (writes bytes / returns FileInfo with inode in extras / creates parent dir if missing / returns None for non-local source), 2 LocalWriteOverwrite (no overwrite raises FileExistsError preserving original / overwrite=True replaces atomically), 2 LocalWriteMtime (mtime parameter sets file's mtime to within 1s precision / mtime omitted uses now), 3 LocalWriteAtomicity (no temp file left on success / temp file cleaned up on os.replace exception / target unchanged when overwrite fails midway via patched os.replace), 1 LocalWriteParentValidation (parent_id pointing at file raises OSError "not a directory"), 3 GDriveWriteBasic (creates new file via mocked client / returns None for non-owned source / returns None when client construction fails via patched _build_drive_client), 2 GDriveWriteOverwrite (existing file no overwrite raises with no CreateFile call / existing with overwrite trashes old then creates new with 2 CreateFile calls), 1 GDriveWriteFailure (Upload exception wrapped as RuntimeError with "upload of" in message), 1 GDriveWriteContent (content captured as BytesIO containing the actual bytes pre-Upload). All 24 tests use real tmp_path for local + injected fake client for gdrive following the existing test_gdrive_source.py pattern. None requires PyDrive2 actually installed. **Mid-session repair**: a multi-edit batch on gdrive_source.py failed because the docstring header anchor used "Implements" (capital I) while my edit text used different casing -- the whole edits array rolled back atomically (correct behavior). Worked around by splitting into separate edits with smaller, surer ASCII-only anchors. Lesson reinforced: edit_file is atomic per call; when ANY edit in a batch fails, ALL edits roll back. **Default suite**: 847 -> **871 passing, 7 correctly-skipped, 49.4s.** Opt-in suite: 848 -> 872 (871 default + 1 GUI smoke). **Phase Beta progress**: gate 4 still at 90% (Lineage Graph still pending); gate 5 now COMPLETE for core read+write+delete. Overall Phase Beta: ~95%. **What this unblocks**: Feature M (Migration tool) can now be designed against a complete source plugin contract -- the universal write primitive means migration TO local OR TO gdrive works through the same code path. Feature S (Cloud sync) v1 can wrap rclone for the actual sync engine while using `curator_source_write` for any non-rclone-handled cases. **Version bumped to 0.40.0** in pyproject.toml + curator/__init__.py.
- **2026-05-08 v0.41** -- **Phase Beta gate 4 COMPLETE: Lineage Graph view SHIPPED.** Seventh and final GUI tab from DESIGN.md §15.2. Renders the full lineage edge graph as a 2D node+edge visualization with type-colored edges (magenta=duplicate, orange=near_duplicate, blue=version_of, green=derived_from, yellow=renamed_from), confidence labels at edge midpoints, and a color-keyed legend bar at the top of the tab. Read-only for v0.41; focus-mode (centered on a selected file) is a v0.42 follow-up but the builder already supports it via `build_focus_graph(file_id, max_hops)`. **GitHub asset leveraged**: `networkx>=3.0` (https://github.com/networkx/networkx, MIT license, ~5MB pure Python) added to `[gui]` extras for graph algorithms (layout via spring/kamada_kawai/circular/shell layouts; default is spring with seed=42 for deterministic test results). This is exactly the kind of asset Jake explicitly endorsed leveraging in this turn -- writing layout algorithms from scratch would have been multiple days of work. **New module** `src/curator/gui/lineage_view.py` (~360 lines): `LineageGraphBuilder` class with `build_full_graph()` and `build_focus_graph(file_id, *, max_hops)` methods; pure-Python facade over file_repo + lineage_repo. Returns `GraphLayout` dataclass with `nodes: list[GraphNode]` and `edges: list[GraphEdge]`. Per-instance file resolution cache (`_file_cache`). `_basename` helper handles both `/` and `\\` path separators (cross-platform); `_ellipsize` truncates labels at 24 chars with single-char ellipsis (\u2026). Color mapping in `EDGE_KIND_COLORS` dict + `color_for_edge_kind(kind)` helper; unknown kinds get a neutral gray default (#586e75). The Qt rendering layer is in a deferred-imported `_make_lineage_graph_view(builder)` factory function that returns a `LineageGraphView(QGraphicsView)` -- defers the PySide6 import so headless tests stay fast. The view scales layout coords from networkx's [0,1] output to scene coordinates with margin; renders edges first then nodes-on-top so node circles overlay edge lines; tooltips on edges show "{kind} (confidence={X.XX})\ndetected by: {detector}", tooltips on nodes show full path. **Wiring in main_window.py**: 7th tab "Lineage Graph" added at index 6. New methods `_build_lineage_tab()` (constructs the builder + view + legend header in a QVBoxLayout) and `_build_lineage_legend_html()` (formats colored swatches inline using ` ` Unicode bullet + edge-kind color from EDGE_KIND_COLORS). `refresh_all()` extended with a hasattr-guarded call to `self._lineage_view.refresh()`. **Tests**: **26 new** at `tests/gui/test_gui_lineage.py`: 1 BuilderEmpty (empty repo yields empty graph), 6 BuilderFull (5 nodes/4 edges built / labels are basenames not paths / edge kinds correct / confidences preserved as floats / coordinates assigned / deterministic with same seed), 3 BuilderFocus (1-hop yields 3 nodes / 2-hop picks up indirect neighbors / isolated UUID returns empty), 7 Helpers (basename unix/windows/no-separator / ellipsize short-unchanged/long-truncated / color for known/unknown kinds / EDGE_KIND_COLORS has expected keys), 5 Wiring (7th tab at index 6 named correctly / builder+view attached to window / empty runtime renders empty-state hint with scene items >=1 / populated runtime renders >=5 scene items / refresh_all re-renders after data change), 4 GraphLayout dataclass (empty=is_empty=True / nodes-only-not-empty / edges-only-not-empty). All 26 tests pass on first run. **Test wiring updates**: `test_gui_inbox.py::test_inbox_tab_at_index_0` and `test_gui_settings.py::test_settings_tab_exists_at_index_4` updated for new tab count (6 -> 7). The Inbox stays at index 0, Settings stays at index 5; only the tab count changed because Lineage Graph appended at the end. **Mid-session repair**: another atomicity-of-edit_file teaching moment. Tried to insert `_build_lineage_tab` using a phantom-anchor (a placeholder method that didn't actually exist in the file). edit_file correctly rejected the whole batch. Recovered by using a real anchor (`_make_table_view`'s `@staticmethod` decorator). **Default suite**: 871 -> **897 passing, 7 correctly-skipped, 52.6s.** Opt-in suite: 872 -> 898 (897 default + 1 GUI smoke). **Real-world end-to-end demo with screenshot**: built a runtime with 10 files seeded across 4 conceptual clusters (3-file music cluster, 3-file code cluster, 2-file photo edit pair, 2-file report version chain) plus 1 cross-cluster duplicate edge between main.py and IMG_0001.jpg (confidence 0.99) demonstrating that the visualization reveals unexpected relationships. 9 lineage edges total spanning all major edge kinds. Set tab index to 6 (Lineage Graph), captured a 1100x650 screenshot. The screenshot at `docs/v041_lineage_graph.png` shows: 7-tab layout with Lineage Graph active, edge-kinds legend bar at top with colored swatches (\u25cf duplicate / near_duplicate / version_of / derived_from / renamed_from), 10 dark teal node circles with basename labels, 9 colored edges with confidence labels at midpoints, clear visual clusters that self-organize from spring layout, and the cross-cluster magenta duplicate edge crossing the canvas (main.py to IMG_0001.jpg) clearly visible as the longest edge. Status bar reads "Files: 10   Bundles: 0   Trash: 0   Audit: 0". **Phase Beta gate 4: 100% (all 7 DESIGN.md §15.2 views shipped)**. Combined with Phase Beta gate 5 also complete, **Phase Beta is now ~98%**. Remaining items are bundle creation/editing UI and `curator gdrive auth` helper, both classifiable as Phase γ polish. **Version bumped to 0.41.0** in pyproject.toml + curator/__init__.py.
- **2026-05-08 ECOSYSTEM-DESIGN MILESTONE (no version bump; design work only)** -- Received and integrated APEX architecture inventory response. Critical finding: the original Phase Delta framing assumed APEX's subAPEX2 (Vampire) was the file inventory subsystem; it isn't. Vampire is a PDF-to-KB content extractor. The actual file inventory subsystem is **Synergy (subAPEX12)** -- the canonical state-of-disk authority per APEX's Master Scroll v0.4 (built v0.2.2, shipped 2026-04-30). Synergy directly overlaps with Curator's role: both walk files, both hash, both do cross-drive dedup. This realigns the ecosystem question from "Curator vs subAPEX2" to "Curator vs Synergy" with four resolution options enumerated. **New deliverables on disk**: (1) `docs/APEX_INFO_RESPONSE.md` (~30KB, ~3000 words) -- the canonical APEX architecture inventory verbatim from the APEX session, with Standing Rule citations, `[NOT IN PROJECT KNOWLEDGE]` markers per APEX's Standing Rule 11, and complete subsystem roster (9 codenamed subsystems: Synergy / Succubus / Vampire / Opus / Locker / Inkblot / Id / Latent / Sketch). (2) `ECOSYSTEM_DESIGN.md` (~36KB, ~1000 lines) -- full integration design synthesizing the APEX response into 8 sections: Synergy/Curator overlap with 4 resolution options + recommendation (Option B: Synergy becomes Curator client), hard constraints from APEX Constitution translated to Curator-side requirements (MORTAL SIN rule -> required `curator_pre_trash` veto plugin; SHA256 reconciliation; hash-verify-before-move discipline; Compounding Learning), Suite Integration Protocol (SIP) v0.1 with 11 conventions (health check / audit log / SHA256 universal / config / plugin discovery / MCP convention / CLI / version / naming / Compounding Learning), per-product responsibility matrix (Curator + APEX + Umbrella + Nestegg), 3 first-integration milestone candidates (apex-safety plugin RECOMMENDED at 3-4h, Curator MCP server at 6-10h, SHA256 secondary hash at 3-5h), 13 enumerated open decisions DE-1 through DE-13, accumulating ideas log with 14 IDEA-NN items + 3 NOT-IDEA explicit anti-patterns. (3) Banner added to `DESIGN_PHASE_DELTA.md` pointing readers to `ECOSYSTEM_DESIGN.md`. **Hard constraints from APEX Constitution that translate to Curator-side requirements**: Standing Rule 9 / MORTAL SIN rule (NEVER delete assessment-derived artifacts -- Output, kb, scrolls, extractions, indexes, manifest.json, profile.json, verification_report); Self-sufficiency aim (APEX must work offline -- any Curator dependency must have graceful fallback); Citation chain terminates at source PDF page (Constitution Section 3 NON-NEGOTIABLE -- Curator data is enrichment never Scribe authority); Standing Rule 3 No new memory systems (formally rules out side-by-side Curator+Synergy as a long-term resolution); SHA256 universal hash (APEX uses SHA256 throughout; Curator uses xxh3 + MD5 -- recommend adding SHA256 as secondary); Compounding Learning (Constitution Section 4 NON-NEGOTIABLE -- every component produces artifact + learning trace); Hash-verify-before-move (per user memory 2026-05-01 -- triple-check source-absent + destination-present + hash-match SHA256). **First-integration milestone recommendation**: APEX safety plugin for Curator -- separately distributable curatorplug-apex-safety package registering curator_pre_trash veto hook that checks paths against APEX assessment-derivation patterns and blocks trash with reason citing Standing Rule 9. ~3-4 hours of work. Tiny scope, big symbolic value (validates Curator's plugin system works for external code, validates APEX governance can be enforced by Curator hooks, prevents real data loss currently only prevented by humans), no APEX-side changes needed. **Code state unchanged**: still v0.41.0 on disk; 897 tests passing; this entire turn was design synthesis with zero code modifications. **Next-session natural starts**: review the 13 DE-N decisions in ECOSYSTEM_DESIGN.md Section 7; ship Candidate 1 (apex safety plugin); or pivot to Curator-internal Phase Delta work (Migration tool unblocked since v0.40).
- **2026-05-08 CONCLAVE PROPOSAL + SYNERGY PHASED-RECOMMENDATION (no version bump; design only)** -- Two design deliverables in response to Jake's substantive 2026-05-08 input. (1) Synergy resolution updated from "Option B forever" to "phased B then A" -- Jake's framing that "Synergy may be an early alpha of Curator" changes the optimal path. New recommendation: Phase 1 (now to ~1 month) Curator MCP server ships + Synergy adds opt-in "use Curator if available" code path + native walk fallback for self-sufficiency; Phase 2 (1-3 months) APEX subsystems consume Curator MCP directly for non-Synergy queries (Inkblot RCS_01-20 location query, Sketch TAT version finder, Latent FAP version locator) and Synergy scope organically narrows to the six APEX-specific analyzer reports; Phase 3 (event-triggered, when nothing Synergy does is unique to it) those six reports become a thin curatorplug-apex-reports plugin and Synergy formally retires via Master Scroll edit, Curator becomes canonical state-of-disk authority. Trigger is event-driven not calendar-driven. Honors offline-first ethos via fallback path; honors APEX authority structure via explicit Constitutional moment in Phase 3 not accidental drift. (2) `docs/CONCLAVE_PROPOSAL.md` written (~26KB, ~3000 words, comparable depth to APEX_INFO_RESPONSE.md per Jake's explicit request). Conclave is the multi-Lens ensemble indexer Jake proposed: 5-9 independent extractors run on the same source PDF, produce candidate KB outputs, then collectively vote section-by-section on the best version. Mathematical premise: individual extractors cap at ~90-95% accuracy on complex psychological assessment manuals; ensemble voting with uncorrelated errors collapses error rates multiplicatively past 99% (matches APEX Constitution Section 1 accuracy aim). Proposed nine Lenses: Lens 1 PdfText (pdfplumber MIT) for born-digital text extraction; Lens 2 OcrFlow (pytesseract GPL-2 wrapping Apache-2 Tesseract) for scanned documents; Lens 3 OcrPaddle (PaddleOCR Apache-2) for multi-column scans + non-English; Lens 4 MarkerPdf (Marker GPL-3 with commercial exception, license watch item) for academic-quality PDF-to-Markdown; Lens 5 TableSurgeon (Camelot MIT + table-transformer MIT) specialist for scoring rubric tables and norm tables; Lens 6 VisionClaude (anthropic SDK MIT) frontier VLM for figures/diagrams/handwriting at $0.005-0.05 per page; Lens 7 VisionLocal (Qwen2-VL-7B Apache-2 or Llama-3.2-Vision) offline VLM fallback for self-sufficiency aim; Lens 8 StructuredHeuristic pure regex/pattern matcher for stereotyped psychometric structures (Likert scales, item lists, score-if-X rubrics); Lens 9 CitationGraph (anystyle MIT or GROBID Apache-2) for cross-assessment lineage via reference graphs. Configurable subset presets: cheap-and-fast (3 Lenses) / balanced (5) / full (9) / custom. Five-stage pipeline: source preparation -> parallel Lens execution -> alignment via SequenceMatcher and content fingerprints -> per-element voting (unanimous/majority/plurality with one-vote-margin always flagged) -> synthesis emitting APEX KB format. Argument-generation for flagged disagreements produces structured case files showing each Lens's output + self-confidence + argument. Full audit trail satisfies APEX Constitution Section 4 (Compounding Learning) by capturing both consensus artifact AND learning trace per vote. Logic gates and decision trees as the organizing primitive: each Lens's internal classification rules ARE explicit decision trees; ensemble combination is configurable AND/OR composition with confidence weights; matches the logic-gate extraction workflow Jake has been building for other projects. Position in ecosystem: standalone constellation product (not Curator subsystem, not APEX subAPEX) integrating bidirectionally with Curator (queries MCP server for source files, writes lineage edges back) and APEX (emits APEX KB format -- profile.json + manifest.json + verification_report.md + extractions JSONL + kb Markdown -- so APEX consumes through existing Vampire/Succubus channel with zero APEX-side changes initially). Honors all APEX hard constraints from ECOSYSTEM_DESIGN.md Section 2: never deletes assessment artifacts, SHA256 chained audit logs, citation chain preserved (every claim traces to source page). Phased rollout proposal: Phase 1 single-Lens proof of concept ~10h; Phase 2 voting infrastructure with 4 Lenses ~25h; Phase 3 vision Lens integration ~30h; Phase 4 ML extraction Lenses ~25h; Phase 5 citation graph + GUI ~30h. Total to v1.0: ~120h, best built parallel with Curator Phase Delta work not before. Eight open questions (OQ-1 through OQ-8) for Jake including codename choice (Conclave preferred metaphor: voting body), build location, Lens count for v1, vision Lens budget cap, first test assessment for proof of concept, build sequencing relative to Curator 1.0. Ten Conclave-specific ideas log items including confidence-weighted voting, Lens performance learning over time, disagreement explorer GUI, cross-assessment validation, Lens-as-plugin extensibility, diff mode for re-indexing regressions. Three explicit anti-patterns (build-all-9-before-shipping-1, build-without-Curator-MCP-server-first, scope-creep-into-LLM-orchestration-framework). ECOSYSTEM_DESIGN.md updated: Synergy recommendation rewritten to phased B-to-A; per-product responsibility matrix gains Conclave row as fifth constellation product (proposed); ideas log gains [IDEA-00] Conclave reference. **Code state unchanged**: still v0.41.0 on disk; 897 tests passing; pure design synthesis turn.
- **2026-05-08 ATRIUM GOVERNANCE SUITE + CONCLAVE LENSES v2 (no version bump; design only)** -- Major design milestone in response to Jake's request for (a) a complete project documentation collection that enshrines all goals, requirements, standards, and protocols anyone working on the project must follow, and (b) updated Lens research expanding beyond the original 9. **New Atrium directory** at C:\Users\jmlee\Desktop\AL\Atrium\ (peer to Curator, Apex, future Conclave/Umbrella/Nestegg) containing the constellation governance suite. **Constellation working name**: Atrium (central hall connecting separate buildings; architectural metaphor for the constellation pattern). Five Atrium documents created: (1) CONSTITUTION.md (~3000 words, ~13KB) -- the binding governance with Six Aims (Accuracy / Reversibility / Self-sufficiency / Auditability / Composability / Portability), Five Non-Negotiable Principles (MORTAL SIN inherited from APEX Standing Rule 9, Hash-Verify-Before-Move triple-check, Citation Chain Preservation, No Silent Failures, Atomic Operations Where Atomicity Is Claimed), six Articles. Amendment codeword TBD (proposed Keystone). Awaiting Jake's ratification. (2) CHARTER.md (~2000 words, ~9KB) -- operational elaboration of Constitution Articles III + IV. Defines constellation pattern (why standalone-first, how products are added/retired, how products respect domains, how conflicts surface, how Atrium relates to APEX as peer). (3) CONTRIBUTOR_PROTOCOL.md (~2500 words, ~14KB) -- how anyone (human or Claude session) works on Atrium products. Codifies all 11 operating rules from this conversation (numbered options, c=continue, no corner-cutting, build location locked, folder numbers persist, edit_file atomicity warning with specific failure mode documentation including em-dash / casing / phantom-anchor patterns, end every turn with Continue cue), per-product conventions (file ops / test discipline / version bumping / naming), documentation discipline (BUILD_TRACKER vs CHANGELOG vs design docs), seven mid-session repair patterns documented from this conversation, things to NEVER do, standard restart prompt for new sessions. (4) GLOSSARY.md (~1500 words, ~12KB) -- alphabetical terminology reference. ~50 terms defined including all APEX-borrowed terms with [APEX] attribution markers, all Atrium-specific terms, all Conclave terms (Lens, Verification Lens, ColPali, late interaction, etc.). Cross-references between terms. (5) ONBOARDING.md (~2000 words, ~7KB) -- for fresh Claude sessions or new human contributors. Defines reading order (Constitution -> Charter -> Onboarding -> Contributor Protocol -> Glossary), current state snapshot per product, operating rules in 60 seconds, common tasks (ship feature / fix bug / update design doc / add Conclave Lens / add new Atrium product / fresh Claude session), things to NEVER do short list, what to do when something goes wrong (test regression / tool fail / Constitutional crisis suspected / merge conflict), success criteria after 2h and after 1 day. **Conclave Lens roster v2** at Curator/docs/CONCLAVE_LENSES_v2.md (~16KB, ~3000 words). Expanded from 9 to 12 Lenses based on 2024-2025 state of art with explicit Lens distinctness criterion (distinct method + distinct failure mode + distinct cost profile, all required for inclusion). Original 9 retained with refinements; four genuine additions (GotOcrUnified -- Stepfun GOT-OCR 2.0 transformer-unified OCR with different architecture from CRNN-based Tesseract / Paddle; NougatScience -- Meta scientific paper specialist with equation awareness distinct from Marker; MinerU -- Shanghai AI Lab comprehensive pipeline with different orchestration philosophy than Marker AGPL-3 license watch item; ColPaliVerify -- late-interaction retrieval verifier in different role of verification not extraction). Updated configurable subset presets: Triage 3 / Cheap-and-fast 5 / Balanced 7 / Full 12 / Custom with wallclock estimates per assessment. Lens evaluation methodology added (7 steps for adding Lenses including pairwise failure correlation must be less than 0.8 for at least one existing Lens AND marginal accuracy contribution must exceed 0.5 percent). Honest exclusions documented (Mistral OCR API duplicates VisionClaude role; Reducto/Aryn duplicates roles; Surya alternative to PaddleOCR; Donut superseded by GOT-OCR 2.0; LayoutLMv3 superseded; etc.). Re-validation calendar: at Conclave build start full pass; quarterly during development; annually after ship; on demand at major model releases. Four new open questions OQ-9 through OQ-12 added (Marker vs MinerU as primary, ColPaliVerify weight semantics, Lens version pinning policy, ground-truth assessment for benchmarking). Honest knowledge cutoff disclosure: Jan 2026 training data; fast-moving categories need re-validation when build begins. **Code state unchanged**: still v0.41.0 on disk; 897 tests passing; entire turn was governance and design synthesis with zero code modifications. **Total new content this turn**: 5 Atrium governance docs (~10000 words) + Conclave Lenses v2 (~3000 words) = ~13000 words / ~71KB across 6 new files. **Next-session natural starts**: Jake ratifies Atrium Constitution + chooses amendment codeword; Jake decides on Atrium working name (Atrium recommended); Jake triages OQ-9 through OQ-12; or pivot to actual code (apex-safety plugin / Migration tool / Conclave Phase 1).
- **2026-05-08 v0.42** -- **Phase Beta gate 5 polish: ``curator gdrive`` CLI subcommands SHIPPED.** Closes the gdrive UX gap from v0.40. Three new subcommands at ``curator gdrive {paths|status|auth}``. Per-alias filesystem convention: ``~/.curator/gdrive/<alias>/{client_secrets.json,credentials.json}``; ``CURATOR_HOME`` env var overrides the base for tests + non-default deployments. **New service module** ``src/curator/services/gdrive_auth.py`` (~280 lines) keeps OAuth side-effects isolated to ONE function (``run_interactive_auth``) so everything else is unit-testable without network or PyDrive2: ``default_gdrive_dir()`` returns ``~/.curator/gdrive`` (or ``$CURATOR_HOME/gdrive``); ``paths_for_alias(alias, *, base=None) -> AuthPaths`` validates alias (rejects empty / path-separators / "."/".."); ``auth_status(alias) -> AuthStatus`` returns one of three states (``no_client_secrets`` / ``no_credentials`` / ``credentials_present``) with human-readable detail; ``ensure_alias_dir`` mkdir-p; ``run_interactive_auth(paths, *, auth_method)`` does PyDrive2's CommandLineAuth or LocalWebserverAuth then SaveCredentialsFile; ``source_config_for_alias(alias)`` returns a SourceConfig.config dict for downstream ``sources add`` integration. Two new exception types: ``PyDrive2NotInstalled`` (RuntimeError) and ``ClientSecretsMissing`` (FileNotFoundError). **CLI sub-app** wired in main.py: new ``gdrive_app`` Typer with ``no_args_is_help=True``. Three commands: ``gdrive paths [alias]`` shows where files would live (no network) with ``--json`` support; ``gdrive status [alias]`` reports current state (no network) color-coded red/yellow/green by state with ``--json`` support; ``gdrive auth [alias] [--method command_line|local_webserver] [--force]`` runs the interactive OAuth flow with skip-when-credentials-present-without-force semantics. All three commands honor the global ``--json`` flag for piping. Default alias is ``"default"`` so ``curator gdrive auth`` (no args) Just Works for the common single-account case. Exit codes: 0 success / 0 skipped (already authed) / 1 client_secrets missing / 1 PyDrive2 auth failed / 2 PyDrive2 not installed. **Tests**: **40 new** (29 unit at ``tests/unit/test_gdrive_auth.py`` + 11 CLI integration at ``tests/integration/test_cli_gdrive_auth.py``). Unit tests cover: default_gdrive_dir env override / paths_for_alias layout + 5 validation cases (empty / forward-slash / backslash / "." / ".." rejected) / auth_status across all three states / to_dict shape / ensure_alias_dir idempotency / source_config_for_alias shape / 7 run_interactive_auth tests via ``patch.dict(sys.modules, {"pydrive2": MagicMock(), "pydrive2.auth": MagicMock(GoogleAuth=mock_class)})`` covering PyDrive2NotInstalled / ClientSecretsMissing / command_line success path / local_webserver success path / unknown method / auth failure wrapped as RuntimeError / save failure wrapped as RuntimeError. CLI tests cover: ``gdrive paths`` default + named + ``--json`` + invalid-alias-rejected; ``gdrive status`` all 3 states + ``--json`` for 2 states; ``gdrive auth`` skips when credentials present + skip with ``--json`` shape + missing client_secrets exit 1 + completes with mocked PyDrive2 + completes with ``--json`` shape + ``--force`` re-runs even with existing creds + ``--method local_webserver`` calls correct PyDrive2 method. **Mid-build catch worth documenting**: initial test ``test_missing_client_secrets_exits_1`` asserted ``"client_secrets.json not found" in result.stdout`` but the err_console writes to stderr, not stdout, and Typer's CliRunner separates them. Fixed by checking ``exit_code == 1 AND mock_gauth.CommandLineAuth.assert_not_called()`` -- proves we exited at the file-check before any PyDrive2 call, which is the actual contract the test should verify. **Default suite**: 897 -> **937 passing, 8 correctly-skipped, 0 failures, 0 xfailed, 55.6s.** (Skip count gained 1: a conditional ``pytest.skip`` when PyDrive2 is genuinely not installed in the environment, since that test path is covered by the import-blocking variant.) Opt-in suite: 906 -> 946 (937 default + 1 GUI smoke + 8 prior opt-in). **Phase Beta gate 5: now includes the auth UX polish.** **Phase Beta overall: ~99%** (only bundle creation/editing UI remains for Phase γ polish). **Version bumped to 0.42.0** in pyproject.toml + curator/__init__.py.
- **2026-05-08 v0.43** -- **Phase Beta gate 4 polish: bundle creation + editing UI SHIPPED. Phase Beta closes at 100%.** Final remaining DESIGN.md Section 15.2 surface (per the v0.42 BUILD_TRACKER summary) now lands. New `BundleEditorDialog` class in `src/curator/gui/dialogs.py` (~410 lines) -- modal dialog used for both Create and Edit modes, distinguished by the optional `existing_bundle` parameter. Layout: Name + Description text fields at top; horizontal QSplitter with three panes (Available files left | Add->/<-Remove/Set as star Primary buttons middle | In bundle right). Both lists are QListWidgets with QLineEdit search filters above; double-click moves an item between lists. The primary member is marked with a star prefix; defaults to first member if user does not explicitly choose one. Validation rejects empty name + zero-member bundles via QMessageBox.warning before the dialog accepts, so invalid state never persists. `BundleEditorResult` dataclass exposes name + description + member_ids + primary_id + existing_bundle_id + initial_member_ids; `added_member_ids` and `removed_member_ids` properties compute the set diff for edit-mode dispatchers. Dialog itself does NOT call BundleService -- it is purely a UI surface; on accept the caller pulls `get_result()` and dispatches to the appropriate `_perform_*` method on the main window. This keeps the dialog testable without DB writes AND keeps mutation logic in the testable seams. Three new GUI flow paths added in `src/curator/gui/main_window.py` (~170 lines added): (1) `_slot_bundle_new` opens editor in Create mode, dispatches to `_perform_bundle_create` on accept which calls `BundleService.create_manual(name, member_ids, description, primary_id)` and returns (success, message); (2) `_slot_bundle_edit_at_row(row)` opens editor pre-populated with the bundle's current state, dispatches to `_perform_bundle_apply_edits` on accept which renames if changed via `BundleRepository.update`, computes added/removed sets, applies via `BundleService.add_member` / `BundleService.remove_member`, then re-applies primary roles via `BundleRepository.add_membership` (INSERT OR REPLACE); (3) `_open_bundle_editor` is the testable seam -- tests patch it on the window instance to inject a synthetic `BundleEditorResult` or None (cancel) without booting Qt event loop. Two new Edit menu actions: "&New bundle... Ctrl+N" + "&Edit selected bundle... Ctrl+E" alongside existing Trash/Restore/Dissolve. Bundles tab context menu reworked to ALWAYS offer "New bundle..." (even when right-clicking on empty space with no row selected); when a row IS selected, also offers "Edit bundle..." and "Dissolve bundle...". About dialog mentions v0.43 capability. **Mid-build catch worth documenting**: initial code called `file_repo.list_active(limit=10000)` which doesn't exist on FileRepository -- fixed by using the real API `file_repo.query(FileQuery(deleted=False, limit=10000))` matching the FileTableModel pattern. Caught at compile-check time before tests ran. **Second mid-build catch**: a CHANGELOG edit_file batch failed because the second edit referenced text the first edit had already replaced (phantom-anchor failure mode -- exactly the failure documented in `Atrium/META/CLAUDE_LOGIC_GATES.md` GATE-EH-004). The whole batch correctly rolled back atomically. Recovered by combining into a single edit. Tests: **32 new** at `tests/gui/test_gui_bundle_editor.py`: 3 BundleEditorResult dataclass tests (added_member_ids set diff / removed_member_ids set diff / no-existing-bundle-id-is-create-mode), 4 _perform_bundle_create (creates with right name+members AND verifies in DB / primary_id set correctly with file 2 as primary not file 0 / empty-member-list-returns-failure since service raises ValueError / no-description-works), 6 _perform_bundle_apply_edits (rename-only-no-membership-change shows Added:0 Removed:0 / add-one-member from 2 to 3 / remove-one-member from 2 to 1 / promote-member-to-primary changes role of file 1 / nonexistent-bundle-returns-failure with "no longer exists" / description-change-persists), 4 slot wiring with patched _open_bundle_editor (slot-new-with-accepted-result actually creates / slot-new-with-cancel-is-noop nothing created / slot-edit-at-row-with-accepted-result renames AND removes member / slot-edit-no-selection-shows-info dialog), 12 real dialog tests with no exec() (create-mode-blank-dialog title contains "Create new bundle" with all 3 files in Available 0 in bundle / edit-mode-pre-populates with name+description+correct member counts / move-to-bundle-via-helper / validate-empty-name-blocks-accept with QMessageBox.warning patched / validate-empty-members-blocks-accept / accept-produces-correct-result with default primary = first member / set-primary-changes-marker with star prefix appearing in item text / remove-primary-clears-primary state / filter-available-hides-non-matching reduces 3 visible to 1 / format_file_label-basename-with-parent shows ".../c/d" / format_file_label-no-separator returns input unchanged / basename_for_sort-lowercases on Unix and Windows paths), 3 context menu wiring (bundles-context-offers-new-when-empty via attribute presence / edit-menu-has-new-bundle-action with text and Ctrl+N shortcut / edit-menu-has-edit-bundle-action with text and Ctrl+E shortcut). Mid-test fix: initial test fixture omitted required `mtime` field on FileEntity construction; added `mtime=datetime(2024, 6, 1, 12, 0, 0)` to seed helper. **Real-world end-to-end demo with screenshot**: built a runtime against a temp DB seeded with 12 audio files across 3 albums (4 Pink Floyd Wall tracks, 3 Pink Floyd Dark Side tracks, 3 Radiohead OK Computer tracks, 2 Beatles Abbey Road tracks), pre-created one bundle "Pink Floyd - The Wall" with the 4 Wall tracks (file 0 as primary). Opened BundleEditorDialog in Create mode, pre-filled name "Radiohead - OK Computer" and description "Studio album, 1997", selected the 3 Radiohead files in Available and clicked Add (which moved them to In bundle), found "Paranoid Android" in In bundle and clicked Set as Primary (which changed dlg._primary_id to that UUID and added star prefix), filtered the Available list to "Pink Floyd" so the screenshot shows the 7 Pink Floyd files visible in Available with the 3 Radiohead tracks showing in the In bundle pane with Paranoid Android starred. Captured 1000x600 screenshot via `dlg.grab().save(path, "PNG")` at `docs/v043_bundle_editor.png` (42039 bytes). **Default suite: 937 -> 969 passing, 8 correctly-skipped, 0 failures, 60.2s.** Opt-in: 946 -> 978. **Phase Beta gate 4 (GUI): 100% complete -- all 7 DESIGN.md Section 15.2 views shipped (Inbox / Browser / Bundles / Trash / Audit Log / Settings / Lineage Graph) PLUS all relevant mutations (Trash / Restore / Dissolve / Bundle create / Bundle edit) PLUS per-file inspect dialog PLUS bundle editor.** **Phase Beta gate 5 (gdrive): 100% since v0.42.** **Phase Beta: 100%. Curator is eligible for a v1.0-rc1 tag at user discretion.** Curator transitions to Phase Gamma polish + Phase Delta substantive features (Migration tool already unblocked by v0.40 source write hook). **Version bumped to 0.43.0** in pyproject.toml + curator/__init__.py.
- **2026-05-08 v1.0.0rc1 TAGGED** -- **First release candidate cut.** Curator's first version that considers itself feature-stable for the use cases the project was designed to serve. Phase Alpha + Phase Beta are 100% complete (Phase Beta closed by v0.43 bundle creation + editing UI ship; Phase Beta gate 5 closed by v0.42 gdrive auth UX; Phase Beta gate 4 closed by v0.43 with all 7 DESIGN.md Section 15.2 views shipped + 5 mutations + per-file inspect dialog + bundle editor). Bumped version from `0.43.0` to `1.0.0rc1` (PEP 440 release-candidate syntax) in two places: `pyproject.toml` (line 7) and `src/curator/__init__.py` (`__version__`). Confirmed `python -c "import curator; print(curator.__version__)"` reports `1.0.0rc1`. Added comprehensive CHANGELOG entry under [1.0.0rc1] heading covering: what v1.0 IS (file indexing across local + gdrive sources via pluggable source-plugin contract; xxh3_128 + ssdeep fuzzy + MD5 hashing in single-pass file reads; lineage detection for exact + near + version + renamed; bundles with manual creation + editing GUI + plugin proposers; cross-platform trash + restore via vendored send2trash with Windows Recycle Bin v1+v2 metadata parsing + macOS AppleScript + Linux freedesktop.org spec; long-running file watcher with debounced events + incremental scans; safety primitives with four concern types and three-level verdicts; four organize pipelines for music + photos + documents + code projects with plan/stage/apply/revert flows and bidirectional manifest reverts; five cleanup detectors with full index-sync on every destructive operation closing the v0.25 phantom-file gap; native PySide6 GUI with 7 tabs and 5 mutations and inspect dialog; full Typer CLI with --json mode for all subcommands; append-only SHA256-chained JSONL audit log per SIP v0.1; pluggy plugin system with hookspecs for source/classifier/lineage/bundle-proposer/pre-trash plugins). Documented what v1.0 IS NOT (Migration tool Feature M deferred ~6-10h; Sync Feature S deferred; Update protocol Feature U deferred; MCP server deferred ~6-10h that unblocks Synergy Phase 1 + Conclave Phase 1; APEX safety plugin curatorplug-atrium-safety deferred ~3-4h; CLI bundle creation parity deferred to v1.x polish; OneDrive + Dropbox source plugins deferred to v1.x). **Final regression run at the new version: 969 passing, 8 correctly-skipped, 0 failures, 0 xfailed, 60.98s** (cold cache after `rm -rf __pycache__`). Opt-in suite: 978 total passing. **Documentation-only release marker per the v0.33 precedent**: there is still no `.git` dir at the project root, so the "tag" at v1.0.0rc1 is documentation-only -- the version bump + CHANGELOG.md entry + this BUILD_TRACKER entry collectively mark the cut point. To turn this into a real git tag the user would run `git init && git add -A && git commit -m "Release 1.0.0rc1" && git tag -a v1.0.0rc1 -m "First release candidate"` from the project root after deciding on .gitignore, squash strategy, and remote. **The long-deferred git_init decision is now the highest-priority outstanding item across the entire portfolio** per Atrium GATE-PM-013 (surface git/backup risk). Every milestone Curator has reached so far is unrecoverable if the project's hard drive fails. **Phase status**: Phase Alpha + Phase Beta = 100% complete. Curator transitions to v1.x development cycle. Next-substantive-feature candidates (in priority order): Migration tool (Feature M; fully unblocked by v0.40 write hook); APEX safety plugin (smallest scope at ~3-4h, biggest symbolic value); Curator MCP server (~6-10h; unblocks Synergy Phase 1 + Conclave Phase 1 simultaneously); Sync (Feature S); Update protocol (Feature U); CLI bundle creation parity; OneDrive + Dropbox source plugins. **Cumulative session shipping summary**: in the conversation that produced v1.0.0rc1, Curator went from v0.41.0 (Phase β at 98%, 897 tests) -> v0.42.0 (gdrive auth UX, +40 tests) -> v0.43.0 (bundle creation + editing UI, +32 tests) -> v1.0.0rc1 (release-cut). Net: three meaningful Phase β closures + a release tag, all in one session, while also producing the Atrium constellation governance suite + Conclave proposal + multi-Lens roster expansion + comprehensive logic-gate inventory + reflection document on the working pattern. Honest note for future sessions: this is exactly the pattern of shipping discipline GATE-PM-001 said I should be pushing harder for, and we successfully did it -- the design momentum from earlier in the session naturally landed in shipping momentum once the user explicitly pivoted with `c` on option 1.
- **2026-05-08 git_init ceremony COMPLETED** -- The v1.0.0rc1 tag is no longer documentation-only. Ran `git init -b main`, staged 180 files (2.2 MB total), verified no `.db`/`.log`/credentials files snuck through, committed as `9e9a32a "Release 1.0.0rc1 - First release candidate"` with detailed body covering Phase Alpha + Phase Beta capabilities and explicit list of v1.x deferrals, then created annotated tag `v1.0.0rc1` with release message. Tagger identity from existing `git config --global` (KULawHawk@users.noreply.github.com -- GitHub no-reply alias). The pre-existing `.gitignore` (826 bytes) was already comprehensive: covers build artifacts + venvs + IDE files + test caches + type-checker caches + `*.db`/`*.log`/`*.db-journal` + `Github/[0-9]*/` for the procurement repos while explicitly preserving `Github/PROCUREMENT_INDEX.md` and `Github/CURATOR_RESEARCH_NOTES.md`. Top-level files committed: 6 .md docs (README + DESIGN + DESIGN_PHASE_DELTA + ECOSYSTEM_DESIGN + CHANGELOG + BUILD_TRACKER) + .gitignore + pyproject.toml; 81 src files; 74 test files; 14 docs files (mostly v0.34-v0.43 screenshots); 2 Github/ index docs; 1 example. Repo size: 1.0 MB across 233 git objects. Working tree clean after commit. **Remote NOT yet configured** -- the GitHub username `KULawHawk` is known from the git config but no `git remote add origin` was run; pushing to GitHub (or any remote) is a separate explicit decision the user can make later via `git remote add origin <URL>` followed by `git push -u origin main --tags`. The highest-priority risk per Atrium GATE-PM-013 (surface git/backup risk) is now mitigated: every Curator milestone is captured in a real git history with a real annotated tag at v1.0.0rc1. Backup status: still single-disk (no remote, no second copy) -- adding a remote remains the natural next risk-reduction step. **Future sessions should know**: there is now a `.git` directory at the project root; commits and tags are real; the v1.0.0rc1 CHANGELOG entry's "documentation-only release marker" caveat is OBSOLETE for the v1.0.0rc1 tag specifically (it correctly described the state at write-time but doesn't describe the current state).