# v1.7.170 — Round 3 Tier 3 ship 16: cli/main.py `scan-pii`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 16 of 20-22 (trimmed ceremony)
**Scope:** `scan-pii` command

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 73.60% | **78.30%** (+4.70%) |

## What landed

`tests/unit/test_cli_scan_pii_coverage.py` (NEW, 13 tests): target not found, single file (no matches / with matches / show-matches with metadata / error report), directory (recursive flag + --ext), --high-only filter, --head-bytes rebuild, JSON, CSV (per-file / per-match / no-header / tsv).

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_scan_pii_coverage.py` (+~340, new, 13 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.171** — `export-clean` (~190 lines).
