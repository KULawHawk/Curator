# v1.7.131 — Tier 3 ship 5: `storage/repositories/trash_repo.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 3 sub-ship 5 of 11 (trimmed ceremony)
**Scope:** `storage/repositories/trash_repo.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `storage/repositories/trash_repo.py` | 82.22% | **100.00%** (+17.78%) |

39 statements, 6 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_storage_trash_repo_coverage.py` (NEW, 4 tests) using shared `repos`/`local_source` fixtures to satisfy the FK chain:
- `delete(curator_id)` removes the record
- `list(since=...)` excludes older records (line 91-92)
- `list(limit=None)` returns all matching, covering the False arm of the LIMIT-append branch (97->100)
- `count()` returns row total (lines 104-105)

No source changes.

## Notable iteration

First-try test attempted to construct TrashRecord directly against a fresh CuratorDB and hit a FK violation (trash_registry → files). Fixed by adopting the shared `repos`/`local_source` fixtures from `tests/conftest.py`, then a model-field lookup error (`FileEntity.file_hash` vs the actual `xxhash3_128`).

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_storage_trash_repo_coverage.py` (+~95, new, 4 tests)
- `docs/STORAGE_SWEEP_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.132** — `storage/repositories/migration_job_repo.py` (6 lines + 5 partial branches).
