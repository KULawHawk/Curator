# v1.7.186 — Round 4 Tier 2 ship 2: `gui/lineage_view.py` Part 1

**Date:** 2026-05-13
**Type:** Round 4 Tier 2 sub-ship 2 of 6 — trimmed ceremony
**Scope:** Cover the pure-Python surface of `lineage_view.py` (dataclasses + helpers + builder); Qt view in Part 2

## Coverage delta

| Module | Before | After |
|---|---|---|
| `gui/lineage_view.py` | 0.00% (246 stmts, 54 br, 0 covered) | **65.67%** (155 stmts covered, **all 54 branches covered**) |

Missing lines 365-501 = `_make_lineage_graph_view` + `LineageGraphView` class — the entire Qt view layer, scoped for Part 2.

## What landed

`tests/unit/test_gui_lineage_view_part1_coverage.py` (NEW, 56 tests). Test classes:

| Class | Tests | Surface covered |
|---|---:|---|
| `TestGraphDataclasses` | 5 | `GraphNode`, `GraphEdge`, `GraphLayout` + `.is_empty` |
| `TestColorMapping` | 6 | All 5 known edge kinds + unknown-kind default |
| `TestBuilderStaticHelpers` | 13 | `_basename`, `_ellipsize`, `_coerce_datetime` exhaustive cases |
| `TestBuilderInit` | 5 | Default + valid + invalid layout; custom seed; empty cache |
| `TestResolveFile` | 2 | Cache hit/miss + exception-caches-None |
| `TestGetTimeRange` | 4 | Populated; both NULL; row=None; exception |
| `TestFetchAllEdges` | 3 | No filter; with filter (SQL + param verified); exception |
| `TestBuildFullGraph` | 3 | networkx unavailable; normal flow; time filter |
| `TestBuildFocusGraph` | 6 | networkx unavail; 1-hop; multi-hop; empty frontier early-break; exception swallowed; cycle visited-set |
| `TestBuildFromEdges` | 8 | Empty; **all 4 layouts via parametrize**; layout exception → grid fallback; edge_kind with/without `.value`; file unresolvable → placeholder |

## Coverage measurement

Per Lesson #96 + DEFERRED_DECISIONS #1, used `tests/unit/`:

```bash
QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -m pytest \
    tests/unit/test_gui_lineage_view_part1_coverage.py \
    --cov=curator.gui.lineage_view --cov-branch --cov-report=term-missing
# 56 passed in 4.64s; 246 stmts, 91 miss, 54 branches, 0 partial → 65.67%
```

## Test infrastructure notes

- **`MagicMock` for repo collaborators** — `file_repo.get`, `lineage_repo.get_edges_for`, `lineage_repo._row_to_edge`. The context-manager `lineage_repo.db.conn()` modeled with explicit `__enter__`/`__exit__`/`execute()` mocks (see `_make_lineage_repo_for_fetch` factory).
- **Real `networkx`** drives `_build_from_edges` — the 4 layout algorithms actually run, not stubbed. This catches algorithm-side regressions if networkx updates.
- **Custom helpers** (`_FakeEdge`, `_FakeFile`) for edge/file shape compatibility without instantiating the full pydantic models.

Per Lesson #84 (stub vocabulary reuse): the helpers here are scoped to this test file because the lineage_view test surface is small. If Part 2 or `gui/models.py` tests need the same helpers, promote to a shared module.

## Files

- `tests/unit/test_gui_lineage_view_part1_coverage.py` (NEW, 56 tests, +~330 lines)

No source changes. No new lesson — successful application of Lessons #98, #84, #93 in concert.

## Next

**v1.7.187** — `gui/lineage_view.py` Part 2 + pragma audit close. Cover the Qt view layer using pytest-qt's `qtbot` fixture; close at 100% line + branch.
