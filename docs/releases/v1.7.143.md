# v1.7.143 — Tier 4 ship 6: `services/organize.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 4 sub-ship 6 of 8 (trimmed ceremony)
**Scope:** `services/organize.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/organize.py` | 80.29% | **100.00%** (+19.71%) |

336 statements, 80 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_services_organize_coverage.py` (NEW, 26 tests) covering all duration_seconds None branches, plan() code/music/photo/document mode branches incl. MB enrich, stage() proposal-not-under-target failure, _write_manifest (existing list / non-list / unreadable / write failure), revert_stage (unreadable / malformed / move failure), _sync_index_after_move (bad UUID / entity None).

No source changes.

## Notable iteration

Two false-start fixes: OrganizePlan needs `root_prefix` (not just source_id); StageReport doesn't have target_root/mode fields. Line 251 was RevertReport (not OrganizePlan) — three duration_seconds properties in this file.

After first measurement: 1 line + 7 partial branches missing. Each partial was an "Optional path returns None" — added one focused test per.

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_services_organize_coverage.py` (+~440, new, 26 tests)
- `docs/MID_SIZE_SERVICES_SWEEP_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.144** — `services/hash_pipeline.py` (52 lines + 12 partial branches).
