# v1.7.102 — Coverage Sweep 8/12: `services/musicbrainz.py` to 100%

**Date:** 2026-05-13
**Type:** Coverage Sweep sub-ship 8 of 12 (trimmed ceremony)
**Scope:** `services/musicbrainz.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/musicbrainz.py` | 88.34% | **100.00%** (+11.66%) |

123 statements, 40 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_musicbrainz_coverage.py` (NEW, 10 tests) covering all uncovered regions:

- `_musicbrainzngs_available` ImportError path (sys.modules sentinel)
- `_configure_user_agent` defensive (MagicMock side_effect)
- `lookup_recording` defensives: non-numeric score, tracks without position, tracks with non-numeric position, mediums exhausted
- `_extract_artist` credit-entry skip arms (dict without name, non-dict non-str)
- `_parse_year` defensives: falsy input, non-numeric prefix, out-of-range year

Reused the fake-`musicbrainzngs`-module-via-sys.modules pattern from `test_music_enrichment.py`. `musicbrainzngs` isn't installed in this environment, so the fake injection is the canonical approach.

No source changes.

## Lesson captured

No new lesson. The `sys.modules[name] = fake_module` injection pattern is now used by **three sub-ships** (v1.7.96 fuzzy_index, v1.7.100 music, v1.7.102 musicbrainz) plus the existing tests. That's enough repetition to count as a settled pattern. Honest logging.

## Files

- `tests/unit/test_musicbrainz_coverage.py` (+~210, new, 10 tests)
- `docs/COVERAGE_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.103** — `services/classification.py`. Handoff: ~30 min, 12 lines.
