# v1.7.164 — Round 3 Tier 3 ship 10: cli/main.py `cleanup_app`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 10 of 20-22 (trimmed ceremony)
**Scope:** `cleanup empty-dirs`, `cleanup broken-symlinks`, `cleanup junk`, `cleanup duplicates`, + 4 rendering/conversion helpers

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 49.09% | **54.86%** (+5.77%) |

**Crossed 50% threshold on cli/main.py.**

## What landed

`tests/unit/test_cli_cleanup_coverage.py` (NEW, 19 tests):
- empty-dirs: empty / with findings (system_junk detail) / --strict / --apply / JSON × 2
- broken-symlinks: target detail rendering
- junk: matched_pattern detail / --apply --no-trash
- _render_cleanup_report: 25-finding cap "and 5 more" / errors section
- _render_cleanup_apply: deleted + skipped + failed details
- duplicates: no-dupes / exact-mode / fuzzy-mode label / 25-group cap / errors / JSON / all flags pass-through

Test infrastructure: `_build_report`, `_apply_report`, `_dup_findings` factories. Monkeypatch CleanupService methods.

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_cleanup_coverage.py` (+~485, new, 19 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.165** — `gui` + `gdrive_app` (~145 lines).
