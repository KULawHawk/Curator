# v1.7.166 — Round 3 Tier 3 ship 12: cli/main.py `migrate` Part 1

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 12 of 20-22 (trimmed ceremony) — first half of v1.7.166/167 split
**Scope:** `_parse_job_id`, `_migrate_list`, `_migrate_status`, `_migrate_abort`, `_migrate_resume` (lookup), `_render_migration_plan`, plus migrate_cmd validation + lifecycle dispatch + plan-only mode

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 57.82% | **64.70%** (+6.88%) |

**Biggest single-ship gain of Tier 3.** 130 lines covered.

## What landed

`tests/unit/test_cli_migrate_part1_coverage.py` (NEW, 24 tests):
- --list: empty / status-filter / 7 status colors / JSON
- --status: invalid UUID / not found / full + minimal / JSON
- --abort: human + JSON + invalid UUID
- --resume lookup: invalid UUID / not found / repo exception
- Validation: missing args / cross-source unwritable / plan ValueError
- Plan-only mode: human (3 buckets + hints) / 20+ moves cap / empty-SAFE / JSON / --ext / --include / --exclude / --path-prefix

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_migrate_part1_coverage.py` (+~410, new, 24 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.167** — `migrate` Part 2 (~230 lines).
