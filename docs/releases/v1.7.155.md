# v1.7.155 — Round 3 Tier 3 ship 1: cli/main.py top-level setup + helpers + `inspect`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 1 of 20-22 (trimmed ceremony)
**Scope:** Top-level setup + helpers + `inspect` command

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 10.73% (1881 stmts, 1627 uncovered) | **14.05%** (1863 stmts, 1552 uncovered) |

+~75 lines covered. Statement count dropped 1881→1863 because the dead `_resolve_file` duplicate (lines 187-216) is now `# pragma: no cover` and excluded from counts.

## What landed

`tests/unit/test_cli_setup_inspect_coverage.py` (NEW, 9 tests):
- `--version` flag (lines 119-120)
- `_check_csv_dialect` invalid + valid (lines 251-255)
- `_err_exit` + `_emit_json` incidental coverage (lines 220, 225-226)
- `inspect` full surface (lines 263-338): no-match error, UUID + JSON, path + human, deleted_at + flex attrs, lineage edges (both directions), bundle memberships

## Bug found and surfaced

Two `_resolve_file` functions exist in `cli/main.py` (lines 187, 3711). The second shadows the first; the first's substring-match feature is dead code. `inspect`'s docstring advertises substring path matching but it never works.

**Conservative action:** `# pragma: no cover` on the dead first definition with documented justification + TODO surfaced for Jake. No behavior change.

**Decision deferred:**
- (a) Delete the dead first definition (cleanup)
- (b) Merge substring-match into the live second definition (restore feature)
- (c) Update `inspect` help text to remove the substring claim (truthful doc)

## Lesson captured

No new lesson. Lesson #90 (read code before writing tests) caught the bug; Lesson #91 (pragma with documented justification) is the textbook tool.

## Files

- `tests/unit/test_cli_setup_inspect_coverage.py` (+~245, new, 9 tests)
- `src/curator/cli/main.py` (+~14 lines pragma + justification on dead `_resolve_file`)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.156** — `scan` + `group` + `lineage` commands (~170 lines predicted).
