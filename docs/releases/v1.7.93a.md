# v1.7.93a — Migration Phase Gamma sub-ship 5a/6: Progress sisters + cross-source-transfer body

**Date:** 2026-05-13
**Type:** Migration Phase Gamma sub-ship 5a of 6 (split from v1.7.93 per Lesson #88)
**Scope:** `services/migration.py` — `_emit_progress_audit_conflict`, `_resolve_collision_for_progress`, `_cross_source_overwrite_with_backup_for_progress`, `_cross_source_rename_with_suffix_for_progress`, `_cross_source_transfer` body, `_can_write_to_source`, `_hook_first_result`, `_read_bytes_via_hook`, `_invoke_post_write_hook`

Group A of the v1.7.93 split. The persistent-path *_for_progress family of methods (sisters of the apply-time methods covered in v1.7.89-91) plus the cross-source bytes-transfer infrastructure that all persistent-path cross-source code depends on. v1.7.93b (Group B, landmark) closes the persistent-job lifecycle + worker pool.

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/migration.py` | 80.49% | **90.86%** (+10.37%) |

Right in the predicted 88-91% band. Pattern dividends per Lesson #87: the v1.7.91 cross-source test patterns mapped directly onto the *_for_progress sisters; the v1.7.90 collision-resolution patterns mapped onto `_resolve_collision_for_progress`. No new stub classes needed — the 5-stub vocabulary from v1.7.89 + the pluggy mock from v1.7.91 covered the entire surface.

## What landed

`tests/unit/test_migration_persistent_progress.py` (NEW, ~860 lines, 51 tests, 9 test classes):

| Class | Tests | Targets |
|---|---|---|
| TestEmitProgressAuditConflict | 4 | audit=None / base details / details_extra merge / log raises (1672-1695) |
| TestResolveCollisionForProgress | 9 | skip / fail / overwrite-with-backup success / OSError / rename-with-suffix success / 9999 exhaustion / unknown mode / audit=None nested / nested log raises (2022-2105) |
| TestCrossSourceOverwriteBackupForProgress | 5 | existing_file_id=None degrade / rename fails degrade / retry MOVED / retry HASH_MISMATCH / retry SKIPPED_COLLISION (1713-1786) |
| TestCrossSourceRenameWithSuffixForProgress | 4 | first suffix wins / third wins after collisions / HASH_MISMATCH surfaces / 9999 exhaustion (1805-1857) |
| TestCrossSourceTransfer | 8 | happy path / src bytes None / write FileExistsError / write None / dst unreadable / hash mismatch / verify=False / src_xxhash cached (2293-2408) |
| TestInvokePostWriteHook | 2 | pm=None / missing hookspec (2438-2449) |
| TestCanWriteToSource | 8 | pm=None / register exception / infos-not-list / None in list / exact match / prefix match / supports_write=False / no match (2210-2233) |
| TestHookFirstResult | 7 | pm=None / missing hook / FileExistsError propagates / other exception / single value / list first non-None / list all None (2235-2261) |
| TestReadBytesViaHook | 4 | first chunk None / short read / empty=EOF / None mid-loop (2263-2290) |

No source code changes.

## Branches closed

All targets in v1.7.93 Group A:

* `_emit_progress_audit_conflict` (1672-1692) — audit=None early return, base details, details_extra merge, log-raises swallow
* `_resolve_collision_for_progress` (2022-2105) — all 4 modes (skip, fail, overwrite-with-backup, rename-with-suffix) + the OSError fallback for overwrite-with-backup + the RuntimeError exhaustion for rename-with-suffix + the defensive unknown-mode fall-through + the nested `_emit_audit`'s audit=None and log-raises branches
* `_cross_source_overwrite_with_backup_for_progress` (1713-1786) — both degraded-to-skip paths + retry-MOVED success + retry-HASH_MISMATCH propagation + retry-SKIPPED_COLLISION race
* `_cross_source_rename_with_suffix_for_progress` (1805-1857) — first-suffix-wins + third-suffix-wins-via-`continue` + HASH_MISMATCH on retry + 9999 exhaustion
* `_cross_source_transfer` body (2293-2408) — read → write → verify happy path + all 4 error/edge paths + 3 verify-branch variations (cached src_xxhash, verify_hash=False, mismatch with dst-delete)
* `_can_write_to_source` (2210-2233) — pm=None / register exception / non-list wrap / None entries / source_type exact match / `source_type:` prefix match / supports_write=False / no match
* `_hook_first_result` (2235-2261) — pm=None / missing hook attr / FileExistsError re-raise / other exception swallow / non-list return / list first-non-None / list all-None
* `_read_bytes_via_hook` (2263-2290) — first chunk None at offset=0, short read termination, empty-bytes EOF mid-loop, None mid-loop
* `_invoke_post_write_hook` (2438-2449) — pm=None silent no-op + missing hookspec silent no-op

## Notable iteration

One iteration pass on the `_setup_pm_for_transfer` test helper. Initial design used a single FIFO queue across both src and dst read-back phases; for short data (single chunk), the short-read break exited the read loop with the trailing `b""` EOF terminator still queued, which then got consumed by the dst read-back yielding an empty buffer and a spurious HASH_MISMATCH in the happy-path test. Fix was trivial: drop `b""` terminators in tests where data fits in one chunk; the short-read break already exits the loop.

This is Lesson #90 (data-flow tracing) extended to test helpers — the helper's iterator-consumption pattern must match how the production code drives it. Not a new lesson; the existing rule applies.

## Lesson captured

**No new lesson this ship.** This was doctrine-in-action paying dividends:

- **Lesson #84 (stub patterns compound):** 51 tests written with zero new stub classes. The 5-stub vocabulary established in v1.7.89 + the pluggy mock from v1.7.91 covered the entire surface.
- **Lesson #87 (pattern dividends):** the *_for_progress family of methods are structurally sister-functions of methods covered in v1.7.89-91. Each `TestX...ForProgress` class directly mirrored the corresponding v1.7.89-91 test class with `MigrationProgress` swapped in for `MigrationMove`.
- **Lesson #88 (split if scope grows beyond 1.5x):** the v1.7.93 consolidated scope from the v1.7.92 calibration was identified as >2x typical at the start of v1.7.93 work. Split at the natural API seam between progress-side helpers (this ship) and the persistent-job machinery (v1.7.93b). The seam was clean; no work was duplicated.

When a sub-ship lands without surfacing a fresh lesson, that's a signal the doctrine is working — the patterns are mature enough that the work is predictable. We log "no new lesson" honestly rather than fabricate a thin one.

## Files

- `tests/unit/test_migration_persistent_progress.py` (+860, new, 51 tests)
- `docs/MIGRATION_PHASE_GAMMA_SCOPE.md` (+15, tracker + split decision documentation)
- `CLAUDE.md` (+1 line, status header)

No source changes.

## Next

**v1.7.93b — sub-ship 6 of 6, ARC CLOSURE (LANDMARK):** persistent job lifecycle + worker pool. Target: 90.86% → **100.00%**.

Remaining ~81 missing lines:
- `run_job` body 2614-2820 (options resolution from job.options, threading worker spawn, status finalize) — including small defensive bits 2618 / 2635-2636 / 2658-2659
- `_worker_loop` 2820-2905 (per-row outcome dispatch, MigrationConflictError handler, on_progress callback)
- `_execute_one_persistent` 2935-2952 (dispatch)
- `_execute_one_persistent_same_source` 2980-3108 (~130 lines)
- `_execute_one_persistent_cross_source` 3110-3300 (~190 lines)
- `create_job` body (2859-2892)
- `_build_report_from_persisted` 3302-end (including defensive 3323-3324)

Requires NEW `StubMigrationJobRepository` modeled on `StubAuditRepository` — 9+ methods. Threading test design: monkeypatch `ThreadPoolExecutor` or run with `workers=1` for determinism. Full landmark ceremony: arc closure summary, lessons rollup #79-93+, "Seven Phase Gamma modules at 100%" status, doctrine close-out.
