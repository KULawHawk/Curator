# v1.7.63 — macOS SafetyService fix: `/private` was over-broad

**Date:** 2026-05-12
**Type:** macOS-specific bug fix in production code
**Scope:** `src/curator/services/safety.py` (`_macos_os_managed_paths`)

## TL;DR

v1.7.62 fixed Rich-help tests; CI went from 0/9 → 6/9 green. The 3 macOS cells still failed with a different error: SafetyService misclassifies pytest's tmp_path as OS_MANAGED (REFUSE) instead of CAUTION. Root cause: `Path("/private")` in `_macos_os_managed_paths()` is over-broad — `/private/var/folders/...` is macOS's user TMPDIR. Fix: list specific `/private/...` subdirs.

## Why this is a real bug

macOS path conventions:
- `/tmp` → symlink to `/private/tmp`
- `/var` → symlink to `/private/var`
- `TMPDIR` defaults to `/private/var/folders/<hash>/T/`

All three are USER-WRITABLE temp dirs. Python's `tempfile` and pytest's `tmp_path` use them. Blocking `/private` wholesale prevented Curator from organizing ANY file in user temp directories on macOS — a real user workflow.

The original `Path("/private")` was a shortcut intended to block `/private/etc`, `/private/var/db`, `/private/var/log` etc. (which is correct), but swept too much.

## The fix

```python
def _macos_os_managed_paths() -> list[Path]:
    return [
        Path("/System"),
        # v1.7.63: replaced Path("/private") with specific subdirs.
        # The bare "/private" was over-broad: macOS uses /private/var/folders
        # as the user TMPDIR (where pytest's tmp_path lives), and /private/tmp
        # is the symlink target of /tmp. Both are user-writable and must NOT
        # be OS-managed.
        Path("/private/etc"),
        Path("/private/var/db"),
        Path("/private/var/log"),
        Path("/private/var/run"),
        Path("/private/var/spool"),
        Path("/Volumes"),
        Path("/usr"),
        Path("/sbin"),
        Path("/bin"),
        Path("/dev"),
    ]
```

Now blocked (system-managed): `/private/etc`, `/private/var/db`, `/private/var/log`, `/private/var/run`, `/private/var/spool`.

Now allowed (user-writable): `/private/var/folders/...`, `/private/tmp`, `/private/var/tmp`.

## Files changed

| File | Change |
|---|---|
| `src/curator/services/safety.py` | +10 / -1 |
| `CHANGELOG.md` | v1.7.63 entry |
| `docs/releases/v1.7.63.md` | this file |

## Verification

- Local test_safety.py: 37 passed, 1 skipped, 2 deselected in 0.97s ✅
- macOS code path not exercised on Windows; no regression possible locally
- Expected CI on macOS: 3 SafetyService failures resolved → **all 9 cells GREEN**

## What this does NOT do

- Doesn't refactor "OS_MANAGED short-circuits other checks" architecture
- Doesn't audit Linux's `/var`-blocking equivalent (also broad but tests don't hit it)
- Doesn't fix Windows live recycle-bin test (still deferred)
- Doesn't address Node.js 20 deprecation warning
- Doesn't add CI status badge (5th ship noting)

## Catches

- **v1.7.62 unmasked this bug.** Layered CI debugging: 0/9 → 6/9 → 9/9 (expected). Each green wave reveals the next.
- **`/private` wholesale = known macOS gotcha.** TMPDIR + `/tmp` symlink convention since OS X 10.0 (2001). Listing parent dirs containing user-writable subtrees is the recurring mistake.
- **macOS CI matrix earned its keep.** Developer is on Windows; without macOS CI, this bug ships to users.
- **3 alternatives considered, chose minimal subdir list:**
  - User-writable exemption list (invasive)
  - Reorder checks (production risk)
  - **Specific `/private/...` subdirs** ← chosen

## Lessons reinforced

- #66 ("green local ≠ green CI") — sixth ship in sub-arc
- #67 ("diagnose with logs") — second use this turn
- Subordinate: **enumerate specific OS-managed subdirs, never parent dirs with user-writable subtrees**

## Arc state

- **63 ships**, all tagged
- Local Windows pytest: 1807/10/0 (unchanged)
- Coverage: 66.96% (unchanged)
- CI: v1.7.62 = 6/9 green. v1.7.63 expected = **9/9 green**, finally closing the 19-ship CI-red arc.
- Lessons: #46-#67
- Detacher-pattern ships: 19

## What comes after CI green (finally)

1. **CI status badge in README** — overdue
2. **`scripts/ci_diag.ps1`** — one-command log download
3. **Pre-push CI verification** — would have caught v1.7.42's silent failure
4. **Hoist inline ANSI-strip to conftest.py fixture**
5. **Bump actions/* to v5** (Node.js 24 readiness, June 2026)
6. **Linux `/var` audit** for the same pattern
7. **Next-weakest module coverage scan**
8. **Phase Gamma features** (CLI + GUI integrations)

The arc that began at v1.7.40 (Tier 1 backlog) closes at v1.7.63 with **every category resolved AND CI verified green on Windows, Ubuntu, and macOS across Python 3.11, 3.12, 3.13**.
