# v1.7.179 — Round 3 Tier 4 ship 4 (FINAL): `gui/cleanup_signals.py` to 100%

**Date:** 2026-05-13
**Type:** Round 3 Tier 4 sub-ship 4 of 4 (FINAL) — trimmed ceremony, but with Round 3 close-out summary appended
**Scope:** Two bridges (GroupProgressBridge + CleanupProgressBridge) + four QThread workers (GroupFindWorker, GroupApplyWorker, CleanupFindWorker, CleanupApplyWorker) in `curator.gui.cleanup_signals`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `gui/cleanup_signals.py` | 0.00% | **100.00%** (94 stmts, 8 branches) |

## What landed

`tests/unit/test_gui_cleanup_signals_coverage.py` (NEW, 15 tests):

- **GroupProgressBridge** — instantiate; all 6 signals (find_started/completed/failed + apply_started/completed/failed) attribute-checked AND emit + deliver verified
- **CleanupProgressBridge** — attribute existence (signals exercised through worker tests below)
- **GroupFindWorker**: `run()` success (find_duplicates returns report → started + completed) + failure (raise → started + failed)
- **GroupApplyWorker**: `run()` success (apply returns → started with findings-count + completed) + failure
- **CleanupFindWorker**:
  - `__init__` rejects unknown mode (`ValueError("unknown mode")`)
  - `run()` mode dispatch: `junk` → find_junk_files(root, patterns=...); `empty_dirs` → find_empty_dirs(root, ignore_system_junk=...); `broken_symlinks` → find_broken_symlinks(root)
  - Defensive unreachable branch (re-assign `_mode` to bogus value after construction; `run()` raises inside the dispatch, caught by broad except, emitted as find_failed)
  - Failure path: helper raises → started + failed
- **CleanupApplyWorker**: `run()` success + failure
- `__all__` exports all 6 classes

Test infrastructure: same module-scoped `qapp` fixture carried from v1.7.177/178. Each worker's `run()` called directly (synchronous) — no QThread event loop required.

No source changes. No new lesson.

## Files

- `tests/unit/test_gui_cleanup_signals_coverage.py` (+~340, new, 15 tests)

---

## 🎯 Round 3 close-out

This ship closes Round 3 in full. **34 ships across four tiers** (v1.7.146 → v1.7.179).

### Tier summary

| Tier | Scope | Ships | Coverage outcome |
|---|---|---|---|
| 1 | Stabilization (migration measurement → 100%, README badge, Dependabot pip, constellation, mutation report, v2.0 draft) | 6 | `services/migration.py` 99.71% → 100% |
| 2 | CLI Coverage Arc kickoff (scope plan + `cli/mcp_keys.py` + `cli/mcp_orphans.py`) | 3 | Both modules → 100% |
| 3 | `cli/main.py` decomposition by Click command group | 21 | 10.73% → 99.43% |
| 4 | GUI signals (launcher + 3 progress bridges) | 4 | 4 × 0% → 100% |

### Coverage gains vs Round 2 close (v1.7.145)

| Module | Round 2 close | v1.7.179 |
|---|---|---|
| `services/migration.py` | 99.71% | **100.00%** |
| `cli/main.py` | 10.73% | **99.43%** |
| `cli/mcp_keys.py` | 78.11% | **100.00%** |
| `cli/mcp_orphans.py` | 64.84% | **100.00%** |
| `gui/launcher.py` | 0% | **100.00%** |
| `gui/migrate_signals.py` | 0% | **100.00%** |
| `gui/scan_signals.py` | 0% | **100.00%** |
| `gui/cleanup_signals.py` | 0% | **100.00%** |

### Doctrine state

- **Zero new numbered lessons captured.** Round 3 ran entirely on Lessons #79–95. The doctrine is at saturation for the test-design questions encountered.
- **10 `# pragma: no cover` annotations** added in `cli/main.py` per Lesson #91, each with documented justification (defensive boundaries, dead code surfaced by the arc).
- **1 real bug surfaced** per partnership directive: duplicate `_resolve_file` definitions at `cli/main.py` lines 187 (dead) and 3711 (live). Deferred to Jake: 3 options (delete / merge feature / fix docstring) — see scope plan.

### What remains <100% in the tree

- `cli/main.py` (99.43%) — pragma-bound on documented defensive boundaries and dead code; deferred to Jake's decisions
- `gui/*` widgets — not in Round 3 scope. Round 4 candidate if Jake routes there
- A handful of view-model and runtime glue files in `gui/` — separate strategy required (Lesson #92 note from Round 2 still applies)

### Where to go next

- v2.0 release notes draft already in `docs/RELEASE_NOTES_v2.0_DRAFT.md` (v1.7.151 work)
- Mutation testing report in `docs/MUTATION_TESTING_REPORT.md` (v1.7.150) — recommends Round 4 includes a dedicated mutation-testing arc on WSL
- Dead `_resolve_file` decision is the smallest near-term item Jake can resolve

## Next

Round 3 is complete. Awaiting Jake's direction on Round 4 / v2.0 cut.
