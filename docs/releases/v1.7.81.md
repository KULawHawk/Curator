# v1.7.81 — Phase Gamma starter: tier.py unit tests (53% → 99%)

**Date:** 2026-05-12
**Type:** Test coverage (Phase Gamma start)
**Scope:** `tests/unit/test_tier_service.py`

## TL;DR

First ship after the v1.7.80 capstone. 27 focused unit tests lift `services/tier.py` coverage from **52.78% → 98.61%**.

## Trimmed ceremony

Per the self-critique offered alongside this ship: focused test-only ships don't earn the full "Catches / Lessons / Limitations / Arc State" release-notes ritual. That format is reserved for landmark ships (like v1.7.80). This entry is intentionally short.

## What's covered

`TestTierRecipeFromString` (3) — valid recipes, case-insensitive parsing, helpful error message on unknown input

`TestTierCriteriaCutoff` (2) — explicit `now` injection + default-`utcnow` path

`TestTierReportProperties` (8) — `candidate_count`, `total_size` (sum + skip-zero), `duration_seconds` (set + unset), `by_source` (grouped + empty)

`TestTierServiceScanCold` (4) — stale-provisional pick, source_id filter, root_prefix filter, oldest-first sort

`TestTierServiceScanExpired` (2) — past-expiry pick, root_prefix filter

`TestTierServiceScanArchive` (2) — stale-vital pick, skip on missing `last_scanned_at`

`TestMatchesRootPrefix` (4) — None matches everything, exact, case-insensitive, non-matching

`TestScanReportMetadata` (2) — timestamps populated, scanned_count = candidate_count

## Implementation notes

- `FakeFile` dataclass instead of real `FileEntity` (pydantic model with many fields the service doesn't read)
- `StubFileRepository` implementing only the 2 methods the service calls (`query_by_status`, `find_expiring_before`)
- Fixed `NOW = datetime(2026, 5, 12, 12, 0, 0)` for deterministic age math
- Uses `curator._compat.datetime.utcnow_naive` to match codebase style and avoid the `datetime.utcnow()` deprecation warning

## Coverage delta

| Module | Before | After |
|---|---|---|
| `src/curator/services/tier.py` | 52.78% | 98.61% |

The one remaining missed line (254) is `assert exp is not None` after a method that guarantees non-None. Unreachable from a correct caller; coverage-by-design at 99%.

## Files

- `tests/unit/test_tier_service.py` (+330)

## Arc state

- 81 ships, all tagged
- pytest local: 1866 / 10 / 0 (+27)
- Coverage local: 66.96% → ~67.3%
- CI v1.7.79: ✅ confirmed GREEN (8 verified post-arc runs)
- Doctrine: still v1.0, no amendments needed

## Next

- `services/bundle.py` (53% → 90%+ target) — similar size, similar shape
- Or `services/lineage.py` (54%, larger module)
- Or take a break — v1.7.80 was a natural pause point and v1.7.81 was a clean opener for Phase Gamma
