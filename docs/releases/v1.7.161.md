# v1.7.161 — Round 3 Tier 3 ship 7: cli/main.py `watch`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 7 of 20-22 (trimmed ceremony)
**Scope:** `watch` command

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 36.78% | **39.59%** (+2.81%) |

## What landed

`tests/unit/test_cli_watch_coverage.py` (NEW, 13 tests):
- Import-error / WatchUnavailableError (exit 2) / NoLocalSourcesError (exit 1)
- Human output (3 event kinds) / JSON-lines output
- --apply scan_paths + counters / scan_paths exception / errors in report
- KeyboardInterrupt human + JSON mode branches
- --apply suffix branches (deleted+edges / JSON mode skip / empty suffix skip)

## Notable iteration

KeyboardInterrupt test initially exit-130'd because a stub with no `yield` isn't a generator — the raise fired at construction (outside the inner try). Yielding one event first fixed it.

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_watch_coverage.py` (+~270, new, 13 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.162** — `doctor` + `safety_app` (~145 lines).
