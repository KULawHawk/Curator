# v1.7.176 — Round 3 Tier 4 ship 1: `gui/launcher.py` to 100%

**Date:** 2026-05-13
**Type:** Round 3 Tier 4 (stretch) sub-ship 1 of 4 (trimmed ceremony)
**Scope:** `gui/launcher.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `gui/launcher.py` | 35.29% | **100.00%** (+64.71%) |

## What landed

`tests/unit/test_gui_launcher_coverage.py` (NEW, 5 tests):
- `is_pyside6_available()` True/False branches (sys.modules poison for the False case)
- `run_gui` returns app.exec() result with stubbed QApplication + CuratorMainWindow
- `run_gui` reuses existing QApplication instance
- `run_gui` propagates nonzero exit codes

Stubbing pattern: `monkeypatch.setattr(PySide6.QtWidgets, "QApplication", _Fake)` and `monkeypatch.setattr(curator.gui.main_window, "CuratorMainWindow", lambda rt: mock)` — no real window construction or event loop.

No source changes. No new lesson.

## Files

- `tests/unit/test_gui_launcher_coverage.py` (+~115, new, 5 tests)

## Next

**v1.7.177** — `gui/migrate_signals.py` (5 lines).
