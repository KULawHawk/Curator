# v1.7.118 — Tier 2 ship 2: `plugins/core/lineage_fuzzy_dup.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 2 sub-ship 2 of 9 (trimmed ceremony)
**Scope:** `plugins/core/lineage_fuzzy_dup.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `plugins/core/lineage_fuzzy_dup.py` | 48.94% | **100.00%** (+51.06%) |

35 statements, 12 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_lineage_fuzzy_dup_coverage.py` (NEW, 11 tests) — covers module-level ppdeep import-fallback chain (importlib.reload with sys.modules patched) and every branch of `curator_compute_lineage`.

No source changes.

## Lesson captured

No new lesson, but a carry-forward worth flagging: `importlib.reload` + `monkeypatch.setitem(sys.modules, ...)` is the clean idiom for testing module-level import-fallback chains. Builds on the sys.modules-sentinel pattern (v1.7.96 / 100 / 102) + adds reload for module-level state.

## Files

- `tests/unit/test_lineage_fuzzy_dup_coverage.py` (+~210, new, 11 tests)
- `docs/PLUGINS_MCP_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.119** — `mcp/middleware.py`. First MCP-server module.
