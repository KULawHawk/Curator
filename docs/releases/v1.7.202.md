# v1.7.202 — Round 5 Tier 1 ship 4: `GroupDialog`

**Date:** 2026-05-13
**Type:** Round 5 Tier 1 sub-ship 4 of 8 — trimmed ceremony
**Scope:** Cover the two-phase duplicate finder (Find → review → Apply)

## Coverage delta

| Module | Before | After |
|---|---|---|
| `gui/dialogs.py` | 49.02% | **60.77%** (+268 stmts) — crosses 60% milestone |

## What landed

`tests/unit/test_gui_dialogs_group_coverage.py` (NEW, 26 tests). Carries the `_SyncWorker` pattern from v1.7.199 (ScanDialog) to both Find and Apply phases.

- Construction (3) — basic / with sources / source list exception
- State helpers (5) — source selection / match kind toggle / indeterminate / clear results
- Find phase (7) — import failure / no dups / with dups / failure / fuzzy / non-dict details / short hash
- Apply phase (8) — no report / user cancel / import failure / all deleted / mixed outcomes / hard delete / fails / many errors truncated
- Slot no-ops (2)
- Close button (1)

## Test infrastructure

`_make_worker_stubs` factory returns 3 classes parametrized by `find_completes_with` / `find_fails_with` / `apply_completes_with` / `apply_fails_with`. Each Stub's `.start()` synchronously emits the appropriate signals.

`_MockOutcome` Enum stand-in for `ApplyOutcome`.

## Files

- `tests/unit/test_gui_dialogs_group_coverage.py` (NEW, 26 tests, +~470 lines)

No source changes. No new lesson.

## Next

**v1.7.203** — `CleanupDialog` (~330 stmts; three-mode cleanup picker).
