# v1.7.182 — Round 4 Tier 1 ship 3: `docs/MUTATION_TESTING_DEFERRED.md`

**Date:** 2026-05-13
**Type:** Round 4 Tier 1 sub-ship 3 of 5 — trimmed ceremony, doc-only
**Scope:** Formalize the mutation-testing deferral as a standalone document

## What landed

`docs/MUTATION_TESTING_DEFERRED.md` (NEW). Structure:

1. **Decision** — when (2026-05-13 formalization; originally attempted v1.7.150), who (Code's killed-run call + Jake's approval)
2. **Why deferred** — the math (1,092 mutants on migration.py = 8–50 hours per module = multi-week budget for comprehensive coverage), what v1.7.150 actually tried, the equivalent-mutations triage burden
3. **What it would take to open the arc** — scope plan + dedicated CPU + triage protocol + tool re-evaluation criteria
4. **When to revisit** — 4 signals: real bug through 100% coverage; CI capacity available; Curator at v3.0 / wider distribution; user-facing bug traced to weak test
5. **See also** — cross-references

Modeled on `docs/PLATFORM_SCOPE.md` (the Windows-only deferral). Per Lesson #100 / Doctrine #18, large enough deferrals warrant their own document; this is the second such document in the project.

## CLAUDE.md updated

Doctrine #15 (Lesson #97) now points to `docs/MUTATION_TESTING_DEFERRED.md` as the authoritative record. The doctrine text is unchanged otherwise — just a "see X" pointer added.

## Why this matters

Round 3 v1.7.150 captured the mutation-testing deferral correctly **in release notes**, but those are easy to lose track of as the project accumulates ships. Lesson #97 captured the rule in the doctrine. This document is the **complete deferred-state record** with all the context a future "should we open mutation testing yet?" conversation will need.

Without it, future sessions would have to re-discover the 1,092-mutants finding, re-run the math, and re-decide whether to defer. With it: one file, one read, full context.

## Files

- `docs/MUTATION_TESTING_DEFERRED.md` (NEW, +~145 lines)
- `CLAUDE.md` (1-line link added in Doctrine #15)

No source changes. No new lesson.

## Next

**v1.7.183** — `docs/GUI_TESTING_STRATEGY.md`: formalize Lesson #98 (Qt headless pattern, foundation for Round 4 Tier 2) + plan the pytest-qt extension for the big GUI modules.
