# v1.7.124 — Tier 2 ship 8: `plugins/core/local_source.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 2 sub-ship 8 of 9 (trimmed ceremony)
**Scope:** `plugins/core/local_source.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `plugins/core/local_source.py` | 62.18% | **100.00%** (+37.82%) |

143 statements, 50 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_local_source_coverage.py` (NEW, 26 tests) — covers every hook (`enumerate`/`_iter`, `read_bytes`, `stat`, `move`, `delete`, `write`), `_matches_ignore` branches, `_stat_to_file_stat`, and `_owns` DB-lookup arms.

No source changes.

## Notable iteration

Production code `from send2trash import send2trash` references the PyPI package (not the vendored copy at `curator._vendored.send2trash`). PyPI send2trash isn't installed → lines 302-304 unreachable without injecting a fake module via `sys.modules`. Small variant of the established `sys.modules[name] = None` pattern.

## Lesson captured

No new lesson. Honest logging.

## Files

- `tests/unit/test_local_source_coverage.py` (+~310, new, 26 tests)
- `docs/PLUGINS_MCP_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.125** — `plugins/core/gdrive_source.py`. Tier 2 final ship; may split.
