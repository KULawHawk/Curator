# v1.7.205 — Round 5 Tier 1 ship 7: `TierDialog` (last dialog)

**Date:** 2026-05-13
**Type:** Round 5 Tier 1 sub-ship 7 of 8 — trimmed ceremony
**Scope:** Cover the largest single dialog class (TierDialog, ~450 stmts)

## Coverage delta

| Module | Before | After |
|---|---|---|
| `gui/dialogs.py` | 84.96% | **96.92%** — crosses 95% |

39 lines + 35 partial branches remain for the v1.7.206 pragma-audit closer.

## What landed

`tests/unit/test_gui_dialogs_tier_coverage.py` (NEW, 44 tests + 1 skipped):

- Construction (2)
- Recipe change (3): min-age updates / expired disables age / unknown data
- Scan flow (6): no candidates / 4-status candidates / source filter / failure / bad recipe / audit-exception swallowed
- Row → FileEntity resolution (8): negative / out-of-range / no path_item / no curator_id / malformed UUID / repo raises / file not found / file found
- Event filter (4): Enter / Delete / other key / non-table obj
- Keyboard shortcuts (2): no-row paths
- Actions (8): inspect success+failure / set_status success+failure+audit-failure / send_to_trash confirm+cancel+failure
- Context menu (1+skipped): no-file early-return / resolved-file context menu skipped (QMenu.exec is a blocking C++ slot)
- Bulk migrate (8): no selection / no root / cancel target / cancel confirm / plan failure / no matching moves / apply failure / mixed outcomes / many failures
- Selection helpers (1)

## Notable: context menu construction skipped

`_on_table_context_menu` calls `menu.exec(...)` which is a blocking native C++ slot that can't be monkeypatched cleanly even under the offscreen Qt platform. The individual `_action_*` methods that the menu items dispatch to are all tested directly via `TestActions` and `TestBulkMigrate`. The pragma audit closer (v1.7.206) decides whether to add `# pragma: no cover` to the menu construction lines.

## Coverage measurement

```bash
QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -m pytest \
    tests/unit/test_gui_dialogs_*coverage.py \
    --cov=curator.gui.dialogs --cov-branch --cov-report=term
# 290 passed, 2 skipped in 44.41s
# 2234 stmts, 39 miss, 432 branches, 35 partial → 96.92%
```

## Files

- `tests/unit/test_gui_dialogs_tier_coverage.py` (NEW, 44 tests + 1 skipped, +~620 lines)

No source changes. No new lesson.

## Next

**v1.7.206** — pragma audit close. Final Tier 1 ship. 39 missing lines + 35 partials to review and either cover or pragma per Lesson #99 / Doctrine #17. Expecting 0-2 pragmas per Lesson #103 / Doctrine #21.
