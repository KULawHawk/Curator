# v1.7.125 — Tier 2 ship 9 (FINAL): `plugins/core/gdrive_source.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 2 sub-ship 9 of 9 (trimmed ceremony — ARC CLOSURE)
**Scope:** `plugins/core/gdrive_source.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `plugins/core/gdrive_source.py` | 77.69% | **100.00%** (+22.31%) |

307 statements, 92 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_gdrive_source_coverage.py` (NEW, 47 tests) — closes every uncovered region:

- `_pydrive2_available` ImportError branch (force via `sys.modules['pydrive2'] = None`)
- `_build_drive_client` full body — fake `pydrive2.auth` + `pydrive2.drive` modules injected via `sys.modules`. Covers missing config, no-credentials, refresh-token, and authorize branches.
- `_drive_file_to_file_info` / `_drive_file_to_file_stat` ValueError/TypeError/native-mime size-parse arms
- Every hook's `client is None` short-circuit (`enumerate`, `stat`, `read_bytes`, `delete`, `rename`, `write`)
- `_iter_folder` ListFile exception arm
- `stat` / `read_bytes` / `delete` broad-except arms via `_ExplodingDriveClient`
- `curator_source_rename` — all branches incl. the overwrite-trash skip-self loop guard (545->544)
- `curator_source_write` — existence-check failure, trash-existing failure, collider FileExistsError, Upload-failure RuntimeError wrap
- `_owns` DB-lookup arms: match / None / mismatch / DB-raises
- `_get_or_build_client` build-exception branches (ImportError / FileNotFoundError / RuntimeError)
- `_resolve_config` exception arms + bottom return-None when disk config lacks `client_secrets_path`

No source changes.

## Notable iteration

Predicted as the riskiest ship of Tier 2 (potential split into 125a + 125b for PyDrive2 mocking architecture per Lesson #88). The mocking architecture turned out to be small: PyDrive2 `GoogleDriveFile` is a `dict` subclass with method attachments, so a `dict` subclass or a `MagicMock` with `__getitem__.side_effect = real_dict.__getitem__` suffices. Only `_build_drive_client` itself needed the `sys.modules` shim (and even that was two `types.ModuleType` instances with two class attributes each). Sub-ship landed as a single ship.

47 tests passed on first run; coverage diff showed 2 missing branches (line 426 = delete non-owned source, 545->544 = rename overwrite-trash self-skip). Added 2 more tests to close, total 47.

## Lesson captured

No new lesson. Honest logging.

## Files

- `tests/unit/test_gdrive_source_coverage.py` (+~580, new, 47 tests)
- `docs/PLUGINS_MCP_SWEEP_SCOPE.md` (+2 lines, tracker close + ARC COMPLETE marker)

## Tier 2 close-out

All 9 sub-ships closed:

| Ship | Module | Final |
|---|---|---|
| v1.7.117 | `plugins/core/classify_filetype.py` | 100% |
| v1.7.118 | `plugins/core/lineage_fuzzy_dup.py` | 100% |
| v1.7.119 | `mcp/middleware.py` | 100% |
| v1.7.120 | `mcp/auth.py` | 100% |
| v1.7.121 | `mcp/tools.py` | 100% |
| v1.7.122 | `mcp/server.py` | 100% |
| v1.7.123 | `config/__init__.py` | 100% |
| v1.7.124 | `plugins/core/local_source.py` | 100% |
| v1.7.125 | `plugins/core/gdrive_source.py` | 100% |

Combined with Round 1 (19 services) + Round 2 Tier 1 (9 mixed) + Round 2 Tier 2 (9 plugins/MCP/config) = **37 modules at 100% line + branch**.

## Next

Tier 2 closed. Reporting in to Jake before opening Tier 3 (Storage Repositories Sweep, ~12 ships).
