# v1.7.172 — Round 3 Tier 3 ship 18: cli/main.py `tier`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 18 of 20-22 (trimmed ceremony) — single ship despite scope-plan "likely split" prediction
**Scope:** `tier` command (scan-only + --apply branches)

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 82.33% | **89.72%** (+7.39%) |

**Biggest single-ship gain of the entire CLI Coverage Arc.** 133 lines covered.

## What landed

`tests/unit/test_cli_tier_coverage.py` (NEW, 23 tests):
- Recipe validation (unknown -> exit 2)
- Scan: no-candidates / with-candidates / 3 recipes / expired skips Min age / show-files + limit / source+root+min-age pass-through / archive default 365 / JSON / CSV (header/no-header/tsv)
- --apply: missing --target/--root exits 2, no-candidates early return, makedirs OSError exits 2, filtered-moves-empty warning, --dry-run preview + 10-cap, --yes executes + tally, confirm declined exits 1, failures+skipped tally, --keep-source switches mode label

No source changes. No new lesson.

## Notable

Scope plan predicted Lesson #88 split. Source survey revealed scan/apply halves share scan-report infrastructure — single set of factories suffices. Landed as single ship.

## Files

- `tests/unit/test_cli_tier_coverage.py` (+~540, new, 23 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.173** — `audit-summary` (~200 lines).
