# v1.7.174 — Round 3 Tier 3 ship 20: cli/main.py `audit-export`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 20 of 20-22 (trimmed ceremony) — final command before arc close
**Scope:** `audit-export` command

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 94.90% | **97.93%** (+3.03%) |

## What landed

`tests/unit/test_cli_audit_export_coverage.py` (NEW, 16 tests):
- Validation: invalid --format / mutually-exclusive flags / negative --older-than / bad ISO / since-after-before / refuses .db extension
- Formats: JSONL / CSV / TSV / stdout
- Filters: --older-than / --actor / --action / --entity-type
- Singular count / JSON output / meta-audit event recorded

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_audit_export_coverage.py` (+~265, new, 16 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.175** — Final cleanup ship + ARC CLOSE (25 lines + 31 partial branches remaining).
