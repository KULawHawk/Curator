# v1.7.162 — Round 3 Tier 3 ship 8: cli/main.py `doctor` + `safety_app`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 8 of 20-22 (trimmed ceremony)
**Scope:** `doctor`, `safety check`, `safety paths`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 39.59% | **44.29%** (+4.70%) |

## What landed

`tests/unit/test_cli_doctor_safety_coverage.py` (NEW, 13 tests):
- doctor: clean run, sources section, missing send2trash → exit 1, PyPI ppdeep fallback, missing ppdeep is non-issue
- safety check: nonexistent path errors, SAFE/CAUTION outputs (stub), --handles + holders
- safety paths: populated + empty (via __init__ wrap) + JSON

## Notable iteration

- `from curator._vendored import send2trash` falls through to package-attribute lookup; needed both `sys.modules` poison AND `delattr` on the package module.
- `SafetyService.app_data` / `os_managed` are instance attrs not class properties; solved by wrapping `__init__`.

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_doctor_safety_coverage.py` (+~280, new, 13 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.163** — `organize` + `organize-revert` (~200 lines).
