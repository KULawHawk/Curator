# v1.7.189 — Round 4 Tier 2 ship 5: `gui/models.py` Part 2 (AuditLog + Config models)

**Date:** 2026-05-13
**Type:** Round 4 Tier 2 sub-ship 5 of 6 — trimmed ceremony
**Scope:** Cover `AuditLogTableModel` and `ConfigTableModel` with their helper functions

## Coverage delta

| Module | Before (after Part 1) | After Part 2 |
|---|---|---|
| `gui/models.py` | 39.77% (335 stmts) | **63.26%** (508 stmts, 0 partial branches) |

+173 new statements covered. The 266 missing lines are exactly the 4 remaining models for Part 3.

## What landed

`tests/unit/test_gui_models_part2_coverage.py` (NEW, 52 tests):

### AuditLogTableModel (26 tests)

- `TestAuditLogTableModelInit` (4) — default + custom limit; init exception fallback; `entry_at`
- `TestAuditLogTableModelFilter` (4) — `set_filter` with all 6 kwargs; empty strings omitted; None values omitted; partial filter; refresh applies kwargs
- `TestAuditLogTableModelProtocol` (10) — full Qt protocol, including row-out-of-range via `createIndex`
- `TestAuditLogFormatters` (8) — `_format_entity` exhaustive cases (both empty / no-type / long-UUID truncated / short-id kept); `_format_details` (empty / truncate / no-truncate / JSON-serialization-failure fallback)

### ConfigTableModel (26 tests)

- `TestConfigTableModelInit` (4) — basic init; config-exception fallback; `setting_at`; `set_config` refresh
- `TestConfigTableModelProtocol` (8) — full Qt protocol (counts, headerData, data + tooltip, unsupported role, invalid index, row out of range, column fallback, sort)
- `TestConfigFlatten` (14) — `_flatten` (simple dict / nested / list / tuple / scalar-at-root); `_format_value` (None / bool / int / string / list / tuple / dict / list-JSON-failure / dict-JSON-failure)

## Notable coverage paths

- **Empty filter strings** — `set_filter(actor="")` ≠ "set actor to empty string"; the impl skips empty values via the `if actor:` truthy guard. Tested explicitly.
- **JSON serialization fallback** — monkeypatched `json.dumps` to raise, verified `str(...)` fallback fires (covers the `except` branches in `_format_details` + `_format_value`)
- **`_flatten` scalar-at-root** — emits `("", scalar_str)`; covers the non-dict, non-list/tuple branch (the lowest priority fallback)

## Pattern reuse (Lesson #84)

Part 1's canonical pattern ported directly:
- Stub repo with `MagicMock`
- `createIndex(row, col)` to bypass index-bounds and hit the `if not (0 <= row < len(self._rows))` branch
- Sort each column + 99-column fallback + None-value handling
- `data()` exhaustive role × column matrix

`_StubConfig` introduced here as a minimal `.as_dict()` wrapper — used for ConfigTableModel tests.

## Coverage measurement

```bash
QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -m pytest \
    tests/unit/test_gui_models_part1_coverage.py \
    tests/unit/test_gui_models_part2_coverage.py \
    --cov=curator.gui.models --cov-branch --cov-report=term-missing
# 106 passed in 3.17s; 774 stmts, 266 miss, 380 branches, 0 partial → 63.26%
```

## Files

- `tests/unit/test_gui_models_part2_coverage.py` (NEW, 52 tests, +~430 lines)

No source changes. No new lesson.

## Next

**v1.7.190** — `gui/models.py` Part 3 + pragma audit close. The 4 remaining models (`ScanJobTableModel`, `PendingReviewTableModel`, `MigrationJobTableModel`, `MigrationProgressTableModel`) plus `_format_duration` helper. Target: 100% line + branch.
