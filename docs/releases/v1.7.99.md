# v1.7.99 — Coverage Sweep 5/12: `services/pii_scanner.py` to 100%

**Date:** 2026-05-13
**Type:** Coverage Sweep sub-ship 5 of 12 (trimmed ceremony)
**Scope:** `services/pii_scanner.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/pii_scanner.py` | 97.94% | **100.00%** (+2.06%) |

208 statements, 80 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_pii_scanner_coverage.py` (NEW, 2 tests):

1. **`test_parse_jwt_non_dict_payload_returns_none`** — covers line 471 (the `isinstance(..., dict)` defensive return in `_parse_jwt`). Builds a valid 3-part JWT where the payload base64-decodes to a JSON array `[1,2,3]` instead of a dict.

2. **`test_scan_file_read_oserror_returns_report_with_error`** — covers lines 752-758 (the `except Exception` around file read/stat). Patches `curator.services.pii_scanner.open` to raise OSError; the scanner returns a `PIIScanReport` with the error message instead of propagating.

**Small source refactor:** removed the `try/except` around `raw.decode("utf-8", errors="replace")` (formerly lines 760-767). Per Python's documented behavior, `bytes.decode("utf-8", errors="replace")` is total and cannot raise — the existing source comment already noted "should be impossible w/ errors=replace". Same refactor pattern as v1.7.95 (`forecast.py`).

## Lesson captured

No new lesson. Both patterns established. Honest logging.

## Files

- `tests/unit/test_pii_scanner_coverage.py` (+~75, new, 2 tests)
- `src/curator/services/pii_scanner.py` (+5 / -8, dead-guard removal)
- `docs/COVERAGE_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.100** — `services/music.py`. Handoff: ~20 min, 6 lines. **🎉 100-ship milestone.**
