# v1.7.196 — Round 4 Tier 4 ship 1: `docs/DIALOGS_DECOMPOSITION.md`

**Date:** 2026-05-13
**Type:** Round 4 Tier 4 sub-ship 1 of ~11 — trimmed ceremony, doc-only
**Scope:** Inventory every dialog class in `gui/dialogs.py` and plan the Tier 4 close

## What landed

`docs/DIALOGS_DECOMPOSITION.md` (NEW):

### Inventory

- **3 module-level helpers**, **3 result/data classes**, **10 dialog classes**
- Total: **2234 statements + 432 branches** across 4286 raw lines

### Dialog complexity tiers

| Tier | Count | Names |
|---|---:|---|
| Small | 3 | FileInspectDialog, VersionStackDialog, ForecastDialog |
| Medium | 3 | BundleEditorDialog, HealthCheckDialog, ScanDialog |
| Large | 4 | GroupDialog, CleanupDialog, SourceAddDialog, (TierDialog as largest) |

### Sub-ship plan: 11 ships v1.7.197 → v1.7.207

| Ship | Scope |
|---|---|
| v1.7.197 | Helpers + result classes (~85 stmts) |
| v1.7.198 | FileInspectDialog + ForecastDialog (~220 stmts) |
| v1.7.199 | VersionStackDialog + ScanDialog Part 1 |
| v1.7.200 | ScanDialog Part 2 |
| v1.7.201 | BundleEditorDialog |
| v1.7.202 | HealthCheckDialog |
| v1.7.203 | GroupDialog |
| v1.7.204 | CleanupDialog |
| v1.7.205 | SourceAddDialog |
| v1.7.206 | TierDialog (largest, possibly splits) |
| v1.7.207 | Pragma audit close |

## Pragma budget revision

Original Lesson #101 estimate for 2234 stmts: 10-15 pragmas. **Revised post-Tier-3 observation: 0-5 pragmas.** Curator's GUI pattern (4 modules in a row at 100% with 0 new pragmas) suggests this large dialog module will probably also close pragma-free.

## Test strategy carry-forwards

- **Lesson #98 / Doctrine #16**: Qt headless foundation (offscreen + qapp + qtbot.addWidget)
- **pytest-qt qtbot**: for modal interaction (mouse clicks, signal waits)
- **`_SyncWorker` pattern**: stubs background worker classes for dialogs that spawn QThreads (carries forward from main_window.py Tier 3)
- **`silence_qmessagebox` fixture**: monkeypatches QMessageBox interactive methods
- **`make_runtime_stub` factory**: from main_window.py Part 1; reusable across all dialog tests
- **`dialog.accept()/reject()` direct calls**: bypass `.exec()` blocking modal entry

## Risks

1. **Modal `.exec()` blocks** — must monkeypatch or use direct accept/reject
2. **Worker-thread spawning** — must stub worker classes
3. **Service method monkeypatching** — most dialogs call runtime services
4. **Sandbox-fragility** — DEFERRED_DECISIONS #1 carries through any trash-related dialog flow
5. **TierDialog split** — largest single class; may need to be split into 2 ships per Lesson #88

## Files

- `docs/DIALOGS_DECOMPOSITION.md` (NEW)

No source changes. No new lesson.

## Next

**v1.7.197** — Helpers + result classes (smallest sub-ship; validates the test infrastructure before tackling the bigger dialog classes).
