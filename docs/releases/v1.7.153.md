# v1.7.153 — Round 3 Tier 2 ship 2: `cli/mcp_keys.py` to 100%

**Date:** 2026-05-13
**Type:** Round 3 Tier 2 ship 2 of 3 (trimmed ceremony)
**Scope:** `cli/mcp_keys.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/mcp_keys.py` | 78.11% | **100.00%** (+21.89%) |

131 statements, 38 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_cli_mcp_keys_coverage.py` (NEW, 13 tests). Establishes the CliRunner + isolated_keys_dir + monkeypatch fixture pattern for the CLI arc. Closes:

- JSON-output error paths in `generate`, `revoke`, `show`
- `KeyFileError` handlers in all 4 commands (`add_key` or `load_keys` raises) — both human + JSON
- Defensive `removed=False` branch in `revoke` via monkeypatched `remove_key`

No source changes.

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_cli_mcp_keys_coverage.py` (+~205, new, 13 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.154** — `cli/mcp_orphans.py` to 100%. Final Tier 2 ship.
