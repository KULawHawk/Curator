# v1.7.183 — Round 4 Tier 1 ship 4: `docs/GUI_TESTING_STRATEGY.md`

**Date:** 2026-05-13
**Type:** Round 4 Tier 1 sub-ship 4 of 5 — trimmed ceremony, doc-only
**Scope:** Formalize the Qt headless pattern + plan the pytest-qt extension for big GUI modules

## What landed

`docs/GUI_TESTING_STRATEGY.md` (NEW):

1. **Baseline check** — re-measured GUI module sizes against the Round 4 handoff. Statement counts match exactly. Raw line counts are 1.6–2.1× higher (docstrings/blanks/comments). Codifies inline a Lesson #93 clarification: **use the same metric the original estimate used; raw `wc -l` ≠ statement count**.
2. **Foundation: the Qt headless pattern** — the 5-component pattern from Lesson #98 / Doctrine #16 with copy-pasteable PowerShell + bash invocations
3. **Extension: pytest-qt** — what `qtbot` brings to widget-interaction testing, and what the v1.7.184 smoke test must prove
4. **Module-by-module strategy** — concrete test approach for `lineage_view`, `models`, `main_window`, `dialogs` with predicted pragma budgets (Lesson #101 / Doctrine #19)
5. **Ship counts: handoff confirmed** — Tier 2 (4-7 ships), Tier 3 (3-5 ships), Tier 4 (8-12 ships, default deferral to Round 5)
6. **See also** cross-references

## Initial false alarm + correction

Drafted the strategy doc with the raw line counts (`wc -l` output) as the "actual" GUI module sizes. That made the handoff look 1.9× stale and triggered a ship-count revision upward. **Re-checked: the handoff used statement counts (what coverage measures), and those match exactly.**

The correction was made before this ship landed, and the Lesson #93 datapoint is recorded inline in the strategy doc. Net effect on Round 4: **no budget revision needed; handoff stands.**

This near-miss is itself worth recording — a Lesson #93 application that almost fired falsely. The clarification: re-measure with the original metric, not with whatever metric is easiest to obtain. Going into the strategy doc rather than CLAUDE.md because it's a refinement on #93, not a new rule.

## CLAUDE.md updated

Test discipline section now points to `docs/GUI_TESTING_STRATEGY.md`.

## Files

- `docs/GUI_TESTING_STRATEGY.md` (NEW, +~220 lines)
- `CLAUDE.md` (1-paragraph link added to test discipline section)

No source changes. No new numbered lesson — the metric-matching clarification is recorded inline in the strategy doc as a refinement on Lesson #93.

## Next

**v1.7.184** — add `pytest-qt` as dev dependency + smoke test. Final Tier 1 ship.
