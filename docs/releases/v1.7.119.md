# v1.7.119 — Tier 2 ship 3: `mcp/middleware.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 2 sub-ship 3 of 9 (trimmed ceremony)
**Scope:** `mcp/middleware.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `mcp/middleware.py` | 93.33% | **100.00%** (+6.67%) |

76 statements, 14 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_mcp_middleware_coverage.py` (NEW, 5 tests) — covers `_remote_addr` None branch, `update_last_used` exception swallow (via `asyncio.run` for the async dispatch), and audit-emitter exception swallow in both `_emit_failure` and `_emit_success`.

No source changes.

## Lesson captured

No new lesson, but a carry-forward: `asyncio.run(mw.dispatch(...))` works for single-call async tests without needing pytest-asyncio.

## Files

- `tests/unit/test_mcp_middleware_coverage.py` (+~120, new, 5 tests)
- `docs/PLUGINS_MCP_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.120** — `mcp/auth.py`.
