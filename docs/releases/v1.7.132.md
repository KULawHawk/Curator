# v1.7.132 — Tier 3 ship 6: `storage/repositories/migration_job_repo.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 3 sub-ship 6 of 11 (trimmed ceremony)
**Scope:** `storage/repositories/migration_job_repo.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `storage/repositories/migration_job_repo.py` | 91.54% | **100.00%** (+8.46%) |

106 statements, 24 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_storage_migration_job_repo_coverage.py` (NEW, 6 tests) closing 6 lines + 5 partial branches:
- `update_job_status(status="queued")` catchall else (line 103)
- `next_pending_progress` BEGIN IMMEDIATE exception swallow (lines 266-269) via a custom `_ExecuteRaisingConn` wrapper
- `update_progress(outcome=None)` branch 324->326
- `update_progress(src_xxhash="...")` line 331
- `update_progress(status="in_progress")` branch 333->336 (skip completed_at)
- `query_progress(limit=N)` lines 401-402

No source changes.

## Notable iteration

`sqlite3.Connection.execute` is read-only — couldn't monkeypatch the method directly. Wrapped the entire connection with a `_ExecuteRaisingConn` class that intercepts `BEGIN IMMEDIATE` and delegates everything else via `__getattr__`. Patched `db.conn` to return the wrapper.

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_storage_migration_job_repo_coverage.py` (+~165, new, 6 tests)
- `docs/STORAGE_SWEEP_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.133** — `storage/exceptions.py` (9 lines, currently 0%).
