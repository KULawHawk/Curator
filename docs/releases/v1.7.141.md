# v1.7.141 — Tier 4 ship 4: `services/photo.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 4 sub-ship 4 of 8 (trimmed ceremony)
**Scope:** `services/photo.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/photo.py` | 93.21% | **100.00%** (+6.79%) |

134 statements, 28 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_services_photo_coverage.py` (NEW, 5 tests):
- `_parse_exif_datetime` ValueError arm (datetime(2026,2,30) raises)
- `_pillow_available` ImportError via sys.modules sentinel
- `read_metadata` stat OSError in mtime fallback (call-counter monkeypatch on Path.stat)
- `_read_exif` unexpected-exception arm (RuntimeError from PIL.Image.open)
- `_read_exif` meta.raw build failure (fake EXIF with items() raising)

No source changes.

## Notable iteration

First-try naive Path.stat patch broke `p.exists()` (which also calls stat). Worked around with a call counter: first call passes through, second raises.

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_services_photo_coverage.py` (+~145, new, 5 tests)
- `docs/MID_SIZE_SERVICES_SWEEP_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.142** — `services/cleanup.py` (50 lines + 9 partial branches).
