# v1.7.106 — 🎯 Coverage Sweep ARC CLOSED: `services/document.py` to 100% + arc closure

**Date:** 2026-05-13
**Type:** Coverage Sweep sub-ship 12 of 12 (FINAL) + arc closure
**Scope:** `services/document.py` + arc-level wrap-up

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/document.py` | 89.66% | **100.00%** (+10.34%) |

199 statements, 62 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_document_coverage.py` (NEW, 15 tests) covering parse-failure defensives (`_parse_filename_date` int() failure, `_parse_pdf_datetime` invalid calendar dates), `_pypdf_available` ImportError, `read_metadata` mtime OSError fallback, all PDF read defensives, OOXML missing-core.xml + generic exception, and dcterms loop branches (no-dates fall-through, no-Z-suffix, out-of-range year, unparseable ISO).

No source changes.

## Notable iteration

`test_read_metadata_mtime_stat_oserror_swallowed` initially monkeypatched `Path.stat` globally — but `Path.exists()` in CPython 3.13 calls `stat()` internally. Fix: call-counter that lets the first stat call (from `.exists()`) succeed and raises on the second (the mtime fallback).

Same "stat-patch blast radius" pattern as v1.7.105 (`Path.stat` affecting `Path.is_file()`). Both are flavors of Lesson #90's corollary about low-level filesystem primitives.

## Lesson captured

No new lesson. The stat-patch corollary is now established (v1.7.105 + v1.7.106). Honest logging.

---

# 🎯 Coverage Sweep arc closure

**Opened:** v1.7.94 (2026-05-13)
**Closed:** v1.7.106 (2026-05-13)
**Duration:** Single session

## Arc-level results

- **All 12 sweep targets at 100% line + branch** ✅
- **19 services modules at 100% total** (7 from Migration Phase Gamma + 12 from Coverage Sweep)
- **108 ships** total in the Curator repo
- **3 source refactors** for provably-unreachable defensive code (v1.7.95, v1.7.99, v1.7.104)
- **1 `# pragma: no branch`** annotation (v1.7.104)
- **0 new lessons captured** during the arc — pure doctrine-in-action across all 12 ships

## Trajectory

| Ship | Module | Before → After | Tests |
|---|---|---|---|
| v1.7.95 | forecast.py | 98.45 → 100.00 | 1 |
| v1.7.96 | fuzzy_index.py | 98.02 → 100.00 | 1 |
| v1.7.97 | watch.py | 97.77 → 100.00 | 2 |
| v1.7.98 | audit.py | 89.66 → 100.00 | 1 |
| v1.7.99 | pii_scanner.py | 97.94 → 100.00 | 2 |
| v1.7.100 | music.py 🎉 | 94.55 → 100.00 | 6 |
| v1.7.101 | metadata_stripper.py | 94.17 → 100.00 | 6 |
| v1.7.102 | musicbrainz.py | 88.34 → 100.00 | 10 |
| v1.7.103 | classification.py | 91.89 → 100.00 | 4 |
| v1.7.104 | migration_retry.py | 77.78 → 100.00 | 9 |
| v1.7.105 | code_project.py | 89.45 → 100.00 | 11 |
| v1.7.106 | document.py | 89.66 → 100.00 | 15 |
| **TOTAL** | **12 modules** | | **68 tests** |

## Six "settled patterns" emerged during the arc

Each used in 2-4 ships, automatic by the third use:

1. **`sys.modules[name] = None`** for optional-dependency ImportError paths
2. **Lazy-import patch-at-origin** (e.g. `PIL.Image.open`, `pypdf.PdfReader`) when production code lazy-imports inside method bodies
3. **Refactor unreachable defensive code** when it's provably dead
4. **`# pragma: no branch`** for language-produced unreachable branches
5. **Module-level data-structure monkeypatch as test seam** (e.g. `_FILENAME_MUSIC_PATTERNS`) for defensive arms unreachable through production patterns
6. **Selective `Path.X` monkeypatch with call-counter** to avoid blast radius on higher-level wrappers

## What this arc unlocks

- Future Coverage Sweep arcs for `plugins/core/*` and `mcp/*` are now low-cost
- `cli/main.py` arc (10.73% baseline) is the next major coverage milestone
- GUI coverage strategy is the last remaining frontier

## Session totals

This session: v1.7.91 → v1.7.106 = **15 ships in one session, 19 modules at 100%, 2 multi-ship arcs closed**.
