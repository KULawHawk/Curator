# v1.7.105 — Coverage Sweep 11/12: `services/code_project.py` to 100%

**Date:** 2026-05-13
**Type:** Coverage Sweep sub-ship 11 of 12 (trimmed ceremony)
**Scope:** `services/code_project.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/code_project.py` | 89.45% | **100.00%** (+10.55%) |

151 statements, 48 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_code_project_coverage.py` (NEW, 11 tests) — covers the `except OSError: continue` defensive boundaries around `Path.iterdir`, `is_dir`, `is_file`, and `stat`, plus the unrecognized-extension skip and the all-zero-counts "Unknown" return.

No source changes.

## Notable iteration

`test_analyze_project_stat_oserror_skips_file` initially monkeypatched `Path.stat` globally — but `Path.is_file()` calls stat internally, so the test file was filtered out of `_iter_project_files` before reaching the `analyze_project` per-file loop where line 296-297 lives.

**Fix:** monkeypatch `svc._iter_project_files` directly to yield the target path, then monkeypatch `Path.stat` to raise selectively. Lesson #90 corollary — when patching low-level filesystem primitives, trace what depends on them; patch at the higher seam instead.

## Lesson captured

No new lesson. Lesson #90 (data-flow tracing) covers the corollary. Honest logging.

## Files

- `tests/unit/test_code_project_coverage.py` (+~250, new, 11 tests)
- `docs/COVERAGE_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.106** — `services/document.py`. **Final sub-ship of the Coverage Sweep arc.**
