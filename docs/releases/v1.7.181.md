# v1.7.181 — Round 4 Tier 1 ship 2: `docs/DEFERRED_DECISIONS.md` index

**Date:** 2026-05-13
**Type:** Round 4 Tier 1 sub-ship 2 of 5 — trimmed ceremony, doc-only
**Scope:** Create the canonical index of deferred decisions per Lesson #100 / Doctrine #18

## What landed

`docs/DEFERRED_DECISIONS.md` (NEW):

### Structure
1. **How to add a new deferred decision** — template + reference to Lesson #100
2. **Pending decisions** — 2 entries
3. **Resolved decisions** — 1 entry (v1.7.180's `_resolve_file` resolution)
4. **See also** — cross-references to doctrine items and prior deferral docs

### Pending entries seeded

**#1 Sandbox-fragile trash-flow tests (medium cost-of-waiting)**
The recycle-bin enumerator hang in `_vendored/send2trash/win/recycle_bin.py` that forced v1.7.180's coverage measurement to fall back to `tests/unit/`. Recommends both `@pytest.mark.integration` markers and a `CURATOR_SANDBOX` env-var fixture for sandbox-friendly full-suite runs. Awaiting Jake's call.

**#2 `dialogs.py` decomposition strategy (no cost-of-waiting)**
Forward-looking strategy decision for the largest GUI module. Recommends deferring the strategy itself until Tier 3 closes — pre-committing now would violate Lesson #88. The Round 4 handoff explicitly says Tier 4 may move to Round 5; this entry tracks that gate.

### Resolved entry seeded

**R1 Duplicate `_resolve_file`** — moved here with full outcome record citing v1.7.180 (option b, merge prefix-match) and Lesson #102 (shadowed definitions become silent regressions).

## Why this matters

Round 3's `_resolve_file` deferral worked end-to-end (it produced the right answer in v1.7.180), but the **provenance trail was scattered**: a comment in source, release notes for v1.7.155, the pragma annotation, the v1.7.175 sweep, the v1.7.179 close-out. To know what was pending, you had to grep release notes and CHANGELOG simultaneously.

This index fixes that. Future Code sessions inheriting the lesson library will have **one file** to scan for "what's pending right now?" The discipline scales as the lesson library grows.

## Files

- `docs/DEFERRED_DECISIONS.md` (NEW, +~115 lines)

No source changes. No new lesson — this ship is the application of Lesson #100, not a new discovery.

## Next

**v1.7.182** — `docs/MUTATION_TESTING_DEFERRED.md`: formalize Lesson #97's mutation-testing deferral as its own standalone doc (same pattern as `docs/PLATFORM_SCOPE.md` for the Windows-only decision).
