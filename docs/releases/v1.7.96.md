# v1.7.96 — Coverage Sweep 2/12: `services/fuzzy_index.py` to 100%

**Date:** 2026-05-13
**Type:** Coverage Sweep sub-ship 2 of 12 (trimmed ceremony)
**Scope:** `services/fuzzy_index.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/fuzzy_index.py` | 98.02% | **100.00%** (+1.98%) |

81 statements, 20 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_fuzzy_index_coverage.py` (NEW, 1 test) — uses `monkeypatch.setitem(sys.modules, "datasketch", None)` to simulate the missing-dependency case. Python's import machinery treats `sys.modules[name] = None` as "do not import this module," causing `from datasketch import MinHashLSH` to raise ImportError, which is translated to `FuzzyIndexUnavailableError`.

No source changes.

## Lesson captured

No new lesson this ship. The `sys.modules[name] = None` pattern is a standard Python testing technique for optional-dependency import failure paths. Worth noting as carry-forward for similar tests in `mcp/*` and any future optional-deps in `services/`. Honest logging.

## Files

- `tests/unit/test_fuzzy_index_coverage.py` (+33, new)
- `docs/COVERAGE_SWEEP_SCOPE.md` (+1 line, tracker update)

## Next

**v1.7.97** — `services/watch.py`. Handoff predicts ~10 min, 3 lines.
