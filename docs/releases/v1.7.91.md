# v1.7.91 — Migration Phase Gamma sub-ship 3/5: Cross-source execution + overwrite-with-backup

**Date:** 2026-05-12
**Type:** Migration Phase Gamma sub-ship 3 of 5 (biggest by line count)
**Scope:** `services/migration.py` — `_execute_one_cross_source`, `_cross_source_overwrite_with_backup`, `_find_existing_dst_file_id_for_overwrite`, `_attempt_cross_source_backup_rename`, `_find_available_suffix`

Third execution ship of the Migration Phase Gamma arc. Closes Clusters 4 (cross-source orchestration) and 5 (overwrite-with-backup retry flow), plus two cross-source helpers that the dispatch flow depends on.

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/migration.py` | 70.05% | **77.47%** (+7.42%) |

**Below the revised v1.7.91 target band of ~80-84% (expecting +10-14%).** The gap is the `_cross_source_rename_with_suffix` body at lines 1713-1786 (~70 stmts), correctly deferred to v1.7.92 per the scope plan. The actual delta delivered (+7.42%) matches what Clusters 4+5 alone should yield; the +10-14% estimate would have required absorbing some of Cluster 6 too. Honest miss; scope discipline preserved.

## What landed

`tests/unit/test_migration_cross_source.py` (NEW, ~810 lines, 38 tests, 6 classes + helpers)

**New infrastructure (Lesson #84 extension):** `StubMigrationPluginManager` + `StubMigrationHooks` — a configurable pluggy hook namespace where tests set per-hook callables. The stub supports both standard hook registration (`set_hook`) AND hookspec-not-registered cases (`remove_hook` → AttributeError on getattr). This is the first sub-ship to need real pluggy mock infrastructure; future sub-ships covering Cluster 7 persistent-path code will reuse it.

## Branches closed

`_execute_one_cross_source` (Cluster 4):
- Transfer-success happy path → FileEntity update, plugin-mediated trash, audit_move
- Transfer-exception boundary (line 1115-1118): outcome=FAILED with type info
- HASH_MISMATCH propagation
- All 4 on-conflict mode dispatch branches (lines 1140-1178): skip/fail/overwrite-with-backup/rename-with-suffix + the defensive `else` for unknown mode
- 2 degraded-return paths (helper returns None → outcome already set by helper)
- keep_source=True → outcome=COPIED, no index update, no trash
- FileEntity vanished (line 1198): `files.get` returns None → FAILED with "vanished"
- FileEntity update exception (lines 1204-1207): caught with "index update failed"
- Trash plugin returns False (lines 1217-1221): error appended, outcome stays MOVED
- Trash defensive boundary (lines 1222-1230): see Lesson #91 below

`_cross_source_overwrite_with_backup` (Cluster 5):
- Happy path: find→rename→retry-transfer succeeds → returns (MOVED, file_id, hash)
- existing_file_id=None → degrade to skip
- Rename hook fails → degrade to skip
- Retry transfer raises → FAILED with "backup preserved per DM-5" message
- Retry HASH_MISMATCH → outcome=HASH_MISMATCH with backup-preserved message
- Retry unexpected SKIPPED_COLLISION (race) → outcome=SKIPPED_COLLISION

`_find_existing_dst_file_id_for_overwrite`:
- Strategy 1 (stat returns non-None) → returns dst_path
- Strategy 2 (stat None, enumerate match by basename) → returns matched file_id
- Both strategies None → returns None
- Enumerate hook raises during call → defensive None
- Iterator raises mid-loop → defensive None
- Stat raises (defensive boundary) → falls through to enumerate
- **Defensive boundaries 1581-1582 and 1595-1596**: tested via `_hook_first_result` monkeypatch (see Lesson #91)
- Iterator with non-matching items only → falls through to final `return None` (closes branch 1605→1600 + line 1613)

`_attempt_cross_source_backup_rename`:
- pm=None → (False, "no plugin manager available")
- hook missing (AttributeError on getattr) → (False, "...hookspec not registered")
- FileExistsError → (False, "backup name collision: ...")
- Other exception → (False, "TypeName: msg")
- All None results → (False, "plugin does not implement...")
- First non-None result → (True, None)
- Non-list result (single value) → wrapped, processed correctly

`_find_available_suffix` exhaustion (line 1530):
- 9999 candidate suffixes all exist (via Path.exists monkeypatch) → RuntimeError raised with "[1, 9999]" message

No source code changes.

## Lesson captured

**Lesson #91 — Defensive boundaries can be effectively unreachable via the path they nominally protect against.**

The migration code has `except Exception` clauses around calls to `_hook_first_result` at three sites: lines 1222-1230 (trash-via-hook), 1581-1582 (stat-via-helper), 1595-1596 (enumerate-via-helper). But `_hook_first_result` itself catches all non-FileExistsError exceptions internally (line 2255) and returns None. **A plugin hook that raises `RuntimeError` will NEVER trigger the caller's `except Exception`** — the helper swallows it first.

So the caller's `except Exception` is defensive code against future refactors (or unforeseen edge cases in the helper itself), not against plugin behavior. To test it, monkeypatch `_hook_first_result` directly:

```python
def boom_helper(hook_name, **kw):
    if hook_name == "curator_source_delete":
        raise RuntimeError("hook helper propagated")
    return None

monkeypatch.setattr(svc, "_hook_first_result", boom_helper)
```

**General rule:** before writing a test that thinks it's testing a defensive `except`, trace whether the exception can actually reach that boundary through normal paths. If the layer between you and the exception swallows it, you're testing nothing. Either patch deeper, or annotate `# pragma: no cover` with a justification per apex-accuracy doctrine.

This pattern shows up 3 times across the cross-source code for the same defensive purpose. If `_hook_first_result`'s exception-swallowing behavior ever changes, these tests will start exercising real paths and may need adjustment — worth keeping in mind for future refactors.

## Files

- `tests/unit/test_migration_cross_source.py` (+810, new)
- `docs/MIGRATION_PHASE_GAMMA_SCOPE.md` (+1, tracker update)

No source changes.

## Arc state

- **91 ships**, all tagged
- Six Phase Gamma modules at 100% (unchanged)
- Migration arc: **sub-ship 3 of 5 closed**, running coverage 77.47%
- 1 new lesson (#91)
- 1 new stub class (`StubMigrationPluginManager`)
- Test count: 2051 → 2089 (+38)

## Next

**v1.7.92 — sub-ship 4 of 5:** `_cross_source_rename_with_suffix` body (~70 stmts, lines 1713-1786) + `_auto_strip_metadata` body (~80 stmts, lines 2022-2105). Target: 77.47% → ~87-92%. The auto-strip body has its own external dependency (`metadata_stripper.strip_file`) which will need a stub. The rename-with-suffix body parallels `_cross_source_overwrite_with_backup` structurally, so the test patterns from v1.7.91 will largely carry over.
