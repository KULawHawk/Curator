# v1.7.144 — Tier 4 ship 7: `services/hash_pipeline.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 4 sub-ship 7 of 8 (trimmed ceremony)
**Scope:** `services/hash_pipeline.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/hash_pipeline.py` | 64.42% | **100.00%** (+35.58%) |

158 statements, 50 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_services_hash_pipeline_coverage.py` (NEW, 16 tests):
- ppdeep import fallback chain (sys.modules sentinels + importlib.reload)
- process() isolated vs non-isolated branches
- Inode dedup: hardlink siblings + noinode keys
- _full_hash exception arm
- Cache hit early return
- Fuzzy hash path via monkeypatch on `_ppdeep_hash` module attribute
- _read_chunks: no-owner RuntimeError / short read / multi-chunk continuation / empty chunk
- _read_segment: happy path + no-owner
- _group_by_prefix_suffix: unique-prefix / same-prefix-different-suffix subgroups

No source changes.

## Notable iteration

ppdeep isn't installed in the test environment, so lines 302/309/315-317 (wants_fuzzy=True path) were unreachable. Fixed via module-attribute monkeypatch to inject a fake `_ppdeep_hash`.

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_services_hash_pipeline_coverage.py` (+~300, new, 16 tests)
- `docs/MID_SIZE_SERVICES_SWEEP_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.145** — `services/trash.py` (90 lines, 24.69% — largest + worst-covered). Final Tier 4 ship.
