# v1.7.156 — Round 3 Tier 3 ship 2: cli/main.py `scan` + `group` + `lineage`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 2 of 20-22 (trimmed ceremony)
**Scope:** `scan`, `group`, `lineage` commands + `_pick_primary` helper

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 14.05% | **19.19%** (+5.14%) |

~90 lines covered + 4 partial branches closed.

## What landed

`tests/unit/test_cli_scan_group_lineage_coverage.py` (NEW, 25 tests):
- **scan**: nonexistent-path error, human/JSON output, error-row branch via stubbed ScanService.scan, ignore-options
- **`_pick_primary`**: all 4 strategies + invalid raises
- **group**: empty (both modes), dry-run (both modes), apply (verifies trash), trash-error continues, edges-with-missing-hash skip, groups-with-one-live-file skip
- **lineage**: no-match, human (edges + no-edges), JSON, CSV (header + no-header + tsv)

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_scan_group_lineage_coverage.py` (+~410, new, 25 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.157** — `bundles_app` (4 subcommands ~150 lines).
