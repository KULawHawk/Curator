# v1.7.167 — Round 3 Tier 3 ship 13: cli/main.py `migrate` Part 2

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 13 of 20-22 (trimmed ceremony) — second half of v1.7.166/167 split
**Scope:** Phase 1 apply, Phase 2 (workers > 1) execution, `--resume` full execution, `_render_migration_report`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 64.70% | **68.43%** (+3.73%) |

Combined v1.7.166 + v1.7.167: **+10.61%** on cli/main.py from migrate alone.

## What landed

`tests/unit/test_cli_migrate_part2_coverage.py` (NEW, 16 tests):
- Phase 1 apply: no-eligible (human + JSON), happy (human + JSON), --keep-source (COPIED label), failed → exit 1, ValueError → exit 2, MigrationConflictError → exit 1, unexpected exception
- Phase 2: workers > 1 happy path, failed → exit 1, JSON with job_id
- --resume full: success path, with failures → exit 1
- _render_migration_report: 25-failure cap, skipped moves

## Notable iteration

Two test stubs initially used `lambda self, **kw:` for `create_job` but the real signature has positional `plan` + keyword-only `options/db_path_guard/include_caution`. Fixed.

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_migrate_part2_coverage.py` (+~340, new, 16 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.168** — `forecast` (~130 lines).
