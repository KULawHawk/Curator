# v1.7.190 — Round 4 Tier 2 ship 6 (FINAL): `gui/models.py` Part 3 + close → **100%**

**Date:** 2026-05-13
**Type:** Round 4 Tier 2 sub-ship 6 of 6 (FINAL) — trimmed ceremony
**Scope:** Close `gui/models.py` at 100% line + branch. **Tier 2 closes.**

## Coverage delta

| Module | Before (after Part 2) | After Part 3 |
|---|---|---|
| `gui/models.py` | 63.26% (508 stmts, 0 partial) | **100.00%** (774 stmts, 380 br, 0 missing, 0 partial) |

+266 new statements. 175 tests total across Parts 1+2+3 (54 + 52 + 69), all pass in ~5s.

## What landed

`tests/unit/test_gui_models_part3_coverage.py` (NEW, 69 tests):

### `_format_duration` helper (9 tests)
None / 0s / sub-minute / boundary at 60s / sub-hour / boundary at 3600s / multi-hour

### `ScanJobTableModel` (11 tests)
Full Qt protocol + three Files-column branches (`"80/100"` / `"50"` / `""`)

### `PendingReviewTableModel` (13 tests)
Default + custom thresholds (escalate/auto_confirm); edge_kind with `.value` Enum-like; edge_kind plain string fallback; file_repo None → `(uuid)` placeholder; file_repo exception → `(uuid)` placeholder; **path cache verified** (file_repo.get called once per unique UUID across multiple rows referencing the same file)

### `MigrationJobTableModel` (14 tests)
Full Qt protocol; data all 8 columns; tooltip on Src→Dst col (full src+dst paths); tooltip on Status col with/without error; tooltip on other columns → None

### `MigrationProgressTableModel` (18 tests)
Init with/without job_id; `set_job_id` triggers refresh; `set_job_id(None)` clears; full Qt protocol; data all 5 cols (incl. **single-char `…` ellipsis** for truncated verified_xxhash, distinct from `...` three-dot in FileTableModel); outcome None → empty; tooltip on Src Path col (full src+dst); tooltip on Outcome col with/without error

### `TestModuleExports` (1 test)
All 9 model classes present in `__all__`

## Pragma audit (Lesson #99 / Doctrine #17)

Predicted budget: **5-7 pragmas**.
Actual: **0**. The module's design — uniform Qt protocol across 9 models with try/except → empty fallback on repo failures — has no defensive boundaries that escape testing. **380/380 branches are real control-flow branches and all covered.**

This is the **third module** to close at 100% with zero new pragmas (after the 4 Round 3 Tier 4 signals modules collectively and v1.7.187's `lineage_view.py`). The pattern: **uniform-protocol Qt models with consistent repo-stub testability are pragma-light.**

## Coverage measurement

```bash
QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -m pytest \
    tests/unit/test_gui_models_part1_coverage.py \
    tests/unit/test_gui_models_part2_coverage.py \
    tests/unit/test_gui_models_part3_coverage.py \
    --cov=curator.gui.models --cov-branch --cov-report=term-missing
# 175 passed in 5.06s; 774 stmts, 0 miss, 380 branches, 0 partial → 100.00%
```

## Tier 2 closed

| Ship | Module | Outcome |
|---|---|---|
| v1.7.185 | scope plan | doc-only |
| v1.7.186 | `lineage_view.py` Part 1 | 0% → 65.67% |
| v1.7.187 | `lineage_view.py` Part 2 + close | **100%** (0 pragmas) |
| v1.7.188 | `models.py` Part 1 | 11.35% → 39.77% |
| v1.7.189 | `models.py` Part 2 | 39.77% → 63.26% |
| v1.7.190 | `models.py` Part 3 + close | **100%** (0 pragmas) |

**Tier 2 outcome: 2 modules closed at 100% line + branch with zero new pragmas in either.** Exactly the 6 ships scoped at v1.7.185. **Predicted pragma budget for these modules combined: 7-11. Actual: 0.** The Lesson #101 prediction was conservative; in practice, GUI Qt models trend pragma-light.

## Files

- `tests/unit/test_gui_models_part3_coverage.py` (NEW, 69 tests, +~580 lines)
- `docs/GUI_COVERAGE_ARC_SCOPE.md` (status tracker)

No source changes. No new lesson.

## Next

**Tier 2 status report → Jake.** Tier 3 (`gui/main_window.py` decomposition, 1,089 stmts, 5 ships) is opt-in per the handoff. **Stopping per handoff directive: "STOP and wait for Jake's confirmation before Tier 3."**
