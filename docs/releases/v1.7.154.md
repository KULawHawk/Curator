# v1.7.154 — Round 3 Tier 2 ship 3 (FINAL): `cli/mcp_orphans.py` to 100%

**Date:** 2026-05-13
**Type:** Round 3 Tier 2 ship 3 of 3 (trimmed ceremony — Tier 2 close)
**Scope:** `cli/mcp_orphans.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/mcp_orphans.py` | 64.84% | **100.00%** (+35.16%) |

142 statements, 40 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_cli_mcp_orphans_coverage.py` (NEW, 13 tests) closing:
- `_enumerate_curator_mcp_processes` body via `_FakeProc` + fake `psutil` module injection (sys.modules pattern)
- `_kill_orphans` body via MagicMock-based psutil.Process substitution (terminate/wait/kill side_effects)
- Command-level lines 279 (JSON mode + empty-orphans return) + 310 (JSON mode + kill payload)

No source changes.

## Tier 2 close

3 of 3 ships landed:
| Ship | Module | Result |
|---|---|---|
| v1.7.152 | scope plan | n/a (doc-only) |
| v1.7.153 | `cli/mcp_keys.py` | 78.11% → 100% |
| v1.7.154 | `cli/mcp_orphans.py` | 64.84% → 100% |

**154 ships total, 57 modules at 100%.**

## Lesson captured

No new lesson. The sys.modules fake-module-injection pattern is now used in 3 places (send2trash, pydrive2, psutil) — settled idiom.

## Files

- `tests/unit/test_cli_mcp_orphans_coverage.py` (+~285, new, 13 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line, tracker + Tier 2 CLOSE marker)

## Next

Tier 3 — cli/main.py decomposition (20-22 sub-ships v1.7.155-175).
