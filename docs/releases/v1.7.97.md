# v1.7.97 — Coverage Sweep 3/12: `services/watch.py` to 100%

**Date:** 2026-05-13
**Type:** Coverage Sweep sub-ship 3 of 12 (trimmed ceremony)
**Scope:** `services/watch.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/watch.py` | 97.77% | **100.00%** (+2.23%) |

## What landed

`tests/unit/test_watch_coverage.py` (NEW, 2 tests):

1. **`test_relative_to_source_value_error_returns_none`** — `_relative_to_source` with duplicate-sid `_active_roots`: when the wrong root is iterated first, `relative_to` raises ValueError → returns None (lines 361-362).
2. **`test_streaming_loop_skips_event_when_relative_to_source_returns_none`** — drives the streaming loop via fake_watchfiles + monkeypatched `_relative_to_source` returning None. The loop hits `if rel is None: continue` (line 285 + branch 284→next-iter).

No source changes. Reused the fake-watchfiles installation pattern from the existing test suite.

## Latent quirk noted

`_relative_to_source` returns None on the FIRST sid-matching root that fails its `relative_to` check, even if other roots with the same sid would succeed. Fine in practice (`_resolve_roots` produces unique-sid entries), but worth recording in case a future refactor changes that invariant. Not a bug to fix today.

## Lesson captured

No new lesson. The fake-watchfiles fixture pattern and the duplicate-sid setup both use established techniques. Honest logging.

## Files

- `tests/unit/test_watch_coverage.py` (+~130, new, 2 tests)
- `docs/COVERAGE_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.98** — `services/audit.py`. Handoff predicts ~15 min, 3 lines.
