# v1.7.122 — Tier 2 ship 6: `mcp/server.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 2 sub-ship 6 of 9 (trimmed ceremony)
**Scope:** `mcp/server.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `mcp/server.py` | 71.05% | **100.00%** (+28.95%) |

66 statements, 10 branches, 0 misses, 0 partials.

## What landed

`tests/unit/mcp/test_mcp_server_coverage.py` (NEW, 9 tests) — covers `_has_configured_keys` + `_load_keys_safe` (3 paths each) and 6 `main()` dispatch paths (stdio default, --no-auth loopback/non-loopback, auth-required with/without keys, non-loopback auth). All `main()` tests mock `build_runtime`, `create_server`, and `uvicorn.run` to exercise dispatch without starting servers.

No source changes.

## Lesson captured

No new lesson. Honest logging.

## Files

- `tests/unit/mcp/test_mcp_server_coverage.py` (+~190, new, 9 tests)
- `docs/PLUGINS_MCP_SWEEP_SCOPE.md` (+1 line)

## Next

**v1.7.123** — `config/__init__.py`.
