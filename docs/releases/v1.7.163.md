# v1.7.163 — Round 3 Tier 3 ship 9: cli/main.py `organize` + `organize-revert`

**Date:** 2026-05-13
**Type:** Round 3 Tier 3 sub-ship 9 of 20-22 (trimmed ceremony)
**Scope:** `organize`, `organize-revert`, + 4 helpers (_organize_plan_to_dict, _render_organize_plan, _stage_report_to_dict, _render_stage_report)

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/main.py` | 44.29% | **49.09%** (+4.80%) |

Partials: 22 → 9.

## What landed

`tests/unit/test_cli_organize_coverage.py` (NEW, 28 tests):
- 5 validation branches (type/target/stage/apply/enrich-mb)
- 5 --enrich-mb branches (incl. MusicBrainz import error + missing musicbrainzngs)
- Plan rendering: empty/populated × human/JSON × show-files on/off
- --stage / --apply happy + ValueError (exit 2) + JSON
- _render_stage_report: failures, skipped cap-at-5, apply/stage mode hints
- organize-revert: FileNotFoundError (exit 1), RuntimeError (exit 2), human + JSON

## Test infrastructure built

- `_empty_plan` / `_populated_plan(with_proposals)`
- `_stage_report(moved, skipped, failed, mode, with_errors)`
- `_revert_report(restored, skipped, failed, with_errors)`

Pattern: monkeypatch `OrganizeService.{plan,stage,apply,revert_stage}`.

No source changes. No new lesson.

## Files

- `tests/unit/test_cli_organize_coverage.py` (+~530, new, 28 tests)
- `docs/CLI_COVERAGE_ARC_SCOPE.md` (+1 line)

## Next

**v1.7.164** — `cleanup_app` (~250 lines).
