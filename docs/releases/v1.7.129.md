# v1.7.129 — Tier 3 ship 3: `storage/migrations.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 3 sub-ship 3 of 11 (trimmed ceremony)
**Scope:** `storage/migrations.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `storage/migrations.py` | 89.19% | **100.00%** (+10.81%) |

33 statements, 4 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_storage_migrations_coverage.py` (NEW, 2 tests):
- `applied_migrations(conn)` returns names of applied migrations against a real CuratorDB
- `apply_migrations` wraps a migration's exception in a `RuntimeError` that names the failing migration (injected via `monkeypatch.setattr(mig_mod, "MIGRATIONS", ...)`)

No source changes.

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_storage_migrations_coverage.py` (+~50, new, 2 tests)
- `docs/STORAGE_SWEEP_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.130** — `storage/repositories/source_repo.py` (6 lines).
