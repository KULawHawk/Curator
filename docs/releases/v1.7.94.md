# v1.7.94 — Coverage Sweep arc: scope plan

**Date:** 2026-05-13
**Type:** Doc-only ship opening a new arc (per Lesson #88)
**Scope:** `docs/COVERAGE_SWEEP_SCOPE.md` (NEW)

Opens the Coverage Sweep arc immediately following the closure of Migration Phase Gamma (v1.7.93b). The arc applies the apex-accuracy doctrine to the 12 services modules closest to 100% but not yet there.

## Why now

Migration Phase Gamma proved the doctrine on the hardest module (`migration.py`, 1031 stmts, 7-sub-ship arc). The stub vocabulary is built (7 reusable stubs across the test suite). The patterns are mature. The 12 target modules collectively have ~110 missing lines per the handoff baseline — less than a tenth of what migration.py started with. Cost-per-module is small; cumulative effect doubles the count of 100%-covered services modules.

## What landed

`docs/COVERAGE_SWEEP_SCOPE.md` — the arc plan. Sections:

- Why this arc (doctrine maturity, cost-benefit)
- The 12 sweep targets table (ordered by ascending effort per handoff)
- Explicitly out-of-scope modules (deferred to future arcs)
- Per-sub-ship process (trimmed ceremony, re-measure baseline before each ship per Lesson #93)
- Lesson capture (honest "no new lesson" entries are fine for doctrine-in-action sweeps)
- Reporting cadence (every 3 sub-ships)
- Resume/restart contract
- Status tracker (12 rows, all pending)
- Arc-level success criteria

## Doctrine notes

This is the **second multi-ship arc planned under the doctrine**. The first was Migration Phase Gamma (v1.7.88 scope plan); this is its successor. Applying Lesson #88 ("multi-ship arcs need explicit scope plans; the plan IS a ship") for the second time validates it as load-bearing pattern.

Character differs from Phase Gamma:
- **Phase Gamma was deep** — one giant module, structurally complex code, novel stub infrastructure required.
- **Coverage Sweep is wide** — 12 modules, one sub-ship each, mostly trivial-to-easy. Stub vocabulary already built. Systematic application of doctrine, not invention.

## Lesson captured

**No new lesson this ship.** This is the second instance of "scope plan opens a multi-ship arc" — Lesson #88 was captured at v1.7.88 specifically for the first instance. Applying the same pattern a second time is doctrine-in-action, not a new lesson. Honest logging per the v1.7.93a precedent (when a sub-ship lands without surfacing a fresh lesson, log it honestly rather than fabricate one).

## Files

- `docs/COVERAGE_SWEEP_SCOPE.md` (+~140, new)

No source changes. No tests added.

## Next

**v1.7.95** opens the actual sweeps with `services/forecast.py` (handoff: ~5 min, 1 uncovered line). Baseline re-measured at sub-ship start per Lesson #93.
