# v1.7.139 — Tier 4 ship 2: `storage/repositories/file_repo.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 4 sub-ship 2 of 8 (trimmed ceremony) — biggest single ship of Round 2
**Scope:** `storage/repositories/file_repo.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `storage/repositories/file_repo.py` | 43.60% | **100.00%** (+56.40%) |

194 statements, 56 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_storage_file_repo_coverage.py` (NEW, 36 tests) covering every uncovered method: upsert/mark_deleted/undelete/delete, find_by_md5, find_by_fuzzy_hash, find_candidates_by_size, query (flex_attrs filter), iter_all (including empty + batched), count(source_id), update_status (all branches), count_by_status, query_by_status, find_expiring_before.

No source changes.

## Notable iteration

Biggest single ship in Round 2 landed without needing Lesson #88 split: 96 lines spread across 14 small CRUD/query methods, each testable in 2-4 tests. Initial measurement caught 4 lines + 3 partials missing → added 4 more tests (undelete, empty iter_all, include_deleted=True for count_by_status and query_by_status). One first-try fix: `FileQuery(source_id=...)` → actual API is `source_ids=[...]`.

## Lesson captured

No new lesson. "Biggest single ship" landed cleanly because the test surface was uniform — same pattern repeated 36 times. Lesson #88 (split when scope >1.5x) targets work where novelty compounds, not bulk repetition.

## Files

- `tests/unit/test_storage_file_repo_coverage.py` (+~340, new, 36 tests)
- `docs/STORAGE_SWEEP_SCOPE.md` (+1 line, tracker)

## Storage subpackage CLOSED

With this ship, every storage module is at 100% line + branch (16 modules).

## Next

**v1.7.140** — Mid-Size Services Sweep scope plan (5 services modules: photo, cleanup, organize, hash_pipeline, trash).
