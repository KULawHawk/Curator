# v1.7.204 — Round 5 Tier 1 ship 6: `SourceAddDialog`

**Date:** 2026-05-13
**Type:** Round 5 Tier 1 sub-ship 6 of 8 — trimmed ceremony
**Scope:** Cover the form-based source create/edit dialog with dynamic schema-driven config widgets

## Coverage delta

| Module | Before | After |
|---|---|---|
| `gui/dialogs.py` | 73.22% | **84.96%** (+236 stmts) — crosses 80% |

## What landed

`tests/unit/test_gui_dialogs_source_add_coverage.py` (NEW, 28 tests + 1 skipped):

- Construction (6): basic add / edit mode / gdrive / None config / no plugins skipped / hook None entry
- Source-type switching (7): caps labels / invalid data / no-config plugin / array → QPlainTextEdit / bool → QCheckBox / default placeholder
- OK button state (2): disabled empty / enabled with source_id
- Submit (8): empty id / missing required / insert success / insert failure / update success / update failure / share visibility / preserves created_at / fallback to now()
- Collect config (4): checkbox / array (newline-split) / required array empty / skips empties
- Cancel button (1)

## Test infrastructure

`_make_runtime_with_plugins` factory builds the hook result list (key/value tuples grouped by plugin). Default plugins: local + gdrive.

Default-selection note: combo is sorted alphabetically (gdrive first). Local-widget tests explicitly switch via `findData("local")`.

## Files

- `tests/unit/test_gui_dialogs_source_add_coverage.py` (NEW, 28 tests + 1 skipped, +~470 lines)

No source changes. No new lesson.

## Next

**v1.7.205** — `TierDialog` (~450 stmts).
