# v1.7.138 — Tier 4 ship 1: `storage/repositories/lineage_repo.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 4 sub-ship 1 of 8 (trimmed ceremony)
**Scope:** `storage/repositories/lineage_repo.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `storage/repositories/lineage_repo.py` | 38.67% | **100.00%** (+61.33%) |

67 statements, 8 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_storage_lineage_repo_coverage.py` (NEW, 17 tests):
- insert on_conflict arms: ignore / replace / raise / FK-violation under ignore (line 72)
- delete + delete_for_file (rowcount)
- get / get_edges_from / get_edges_to / get_edges_for / get_edges_between (+ kind filter)
- list_by_kind (min_confidence + limit)
- query_by_confidence (range + limit)

Uses shared `repos`/`local_source` fixtures. No source changes.

## Notable iteration

Line 72 (the `return False` in the IntegrityError handler) initially missed because `INSERT OR IGNORE` suppresses PK conflicts entirely — the handler doesn't fire. The FK-violation test (`from_id`/`to_id` referencing non-existent files) does trigger an IntegrityError that `OR IGNORE` doesn't suppress, hitting the handler.

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_storage_lineage_repo_coverage.py` (+~200, new, 17 tests)
- `docs/STORAGE_SWEEP_SCOPE.md` (+~10 lines, Tier 4 follow-up)

## Next

**v1.7.139** — `storage/repositories/file_repo.py` (96 lines, biggest single ship in Round 2). May split per Lesson #88 if scope grows.
