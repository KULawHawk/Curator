# v1.7.142 — Tier 4 ship 5: `services/cleanup.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 4 sub-ship 5 of 8 (trimmed ceremony)
**Scope:** `services/cleanup.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `services/cleanup.py` | 87.53% | **100.00%** (+12.47%) |

367 statements, 122 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_services_cleanup_coverage.py` (NEW, 27 tests) covering all uncovered regions:
- duration_seconds None + computed arms (CleanupReport + ApplyReport)
- find_empty_dirs / find_junk_files os.walk + stat OSError
- find_broken_symlinks (entire method via Path.is_symlink/exists monkeypatch — Windows blocks real symlinks)
- find_duplicates query exception + None-xxhash skip
- _find_fuzzy_duplicates query exception, FuzzyIndexUnavailableError re-raise, unexpected init exception, add failure, query failure, neighbor-not-in-by_id
- apply safety check exception
- _delete_one EMPTY_DIR junk unlink swallow, BROKEN_SYMLINK, send2trash success (line 942), send2trash failure fallback, unknown CleanupKind ValueError
- _mark_index_deleted DUPLICATE_FILE not-found debug branch

## Notable iteration: minimal source change

Line 704's `if len(component_ids) > 1:` in `_find_fuzzy_duplicates` BFS is structurally unreachable on its False arm given the adjacency-build invariants. Added `# pragma: no branch` per Lesson #91. Second pragma in Round 2 (first was plugins/manager.py thread-race fallthrough).

## Lesson captured

No new lesson.

## Files

- `tests/unit/test_services_cleanup_coverage.py` (+~450, new, 27 tests)
- `src/curator/services/cleanup.py` (+1 inline pragma, justified comment)
- `docs/MID_SIZE_SERVICES_SWEEP_SCOPE.md` (+1 line, tracker)

## Next

**v1.7.143** — `services/organize.py` (56 lines + 10 partial branches).
