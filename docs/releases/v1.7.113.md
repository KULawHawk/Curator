# v1.7.113 — Round 2 Tier 1 ship 7: `cli/runtime.py` to 100%

**Date:** 2026-05-13
**Type:** Round 2 Tier 1 sub-ship 7 of 9 (trimmed ceremony)
**Scope:** `cli/runtime.py`

## Coverage delta

| Module | Before | After |
|---|---|---|
| `cli/runtime.py` | 91.61% | **100.00%** (+8.39%) |

129 statements, 14 branches, 0 misses, 0 partials.

## What landed

`tests/unit/test_cli_runtime_coverage.py` (NEW, 7 tests) — covers the default-config arm of `build_runtime`, the three "plugin not registered" defensives (all in one test via `pluggy.PluginManager.get_plugin` monkeypatch), and all four branches of `_build_fuzzy_index_if_available` (ImportError, FuzzyIndexUnavailableError, ValueError-on-add, empty-hash-skip, happy path).

No source changes.

## Lesson captured

No new lesson. Settled patterns. Honest logging.

## Files

- `tests/unit/test_cli_runtime_coverage.py` (+~170, new, 7 tests)

## Next

**v1.7.114** — `plugins/core/lineage_hash_dup.py`.
